# 📋 Описание этапов промпта AI Debugger

**Краткое описание:** Кто и что делает на каждом этапе отладки.

---

## Phase 0: Входной фильтр
**Кто:** RooTrace (оркестратор)  
**Что:** Оценивает достаточность данных
- Проверяет наличие описания проблемы, файлов, ошибок
- Если данных недостаточно → использует `mcp--roo-trace--get_problems()` или `@problems`
- Задает вопросы с кнопками (макс 3 подряд)

---

## Phase 0.1: TODO List
**Кто:** RooTrace  
**Что:** Создает список задач через `update_todo_list` (ОБЯЗАТЕЛЬНО ПЕРВЫМ!)
- Первый вызов инструмента ДОЛЖЕН быть `update_todo_list`
- Запрещено: любые другие действия до этого

---

## Phase 0.2: Разведка
**Кто:** Архитектор (через `new_task mode="architect")`  
**Что:** Анализирует проблему и находит подозрительные места
- Использует `codebase_search` и `read_file`
- Возвращает: FILE:COORDINATE:FUNCTION:REASON
- НЕ исправляет код, только разведка

---

## Phase 0.3: Получение summary от архитектора
**Кто:** RooTrace  
**Что:** Получает результаты разведки через `attempt_completion`
- Валидирует формат: FILE:COORDINATE:FUNCTION:REASON
- Сохраняет координаты для дальнейшей работы

---

## Phase 0.4: Pre-Flight Check
**Кто:** RooTrace САМ (не делегирует!)  
**Что:** Проверяет окружение перед началом
1. Статус сервера: `get_debug_status`
2. Сеть: `.debug_port`, Docker файлы, `docker ps`
3. Мост: smoke test (отправка тестового лога)
4. Диагностика: `mcp--roo-trace--get_problems()` или `@problems`
5. MCP tools: `list_mcp_tools`, проверка `sequentialthinking` и `memory`

---

## Phase 1: Формулирование гипотез
**Кто:** RooTrace  
**Что:** Создает гипотезы H1-H3 (минимум), H1-H5 (рекомендуется)
- На основе summary от архитектора
- Использует `sequentialthinking` если доступно
- Каждая гипотеза = отдельная задача в TODO

---

## [STRATEGY] DEBUG-STRATEGIST
**Кто:** RooTrace  
**Что:** Определяет точки наблюдения для гипотез
- ОБЯЗАТЕЛЬНО перед Phase 1.1
- Планирует где вставлять пробы

---

## [STRATEGY] SRE-SHIELD
**Кто:** RooTrace  
**Что:** Оценивает overhead проб и стратегию сэмплирования
- ОБЯЗАТЕЛЬНО перед Phase 1.1
- Проверяет влияние проб на производительность

---

## Phase 2: Network Discovery
**Кто:** RooTrace САМ  
**Что:** Обнаруживает сетевой endpoint
1. Читает `.debug_port` или `.debug_port`
2. Проверяет Docker файлы (`Dockerfile`, `docker-compose.yml`)
3. Запускает `docker ps`
4. Если Docker → проверяет bridge (`host.docker.internal` или `172.17.0.1`)
5. Определяет `FINAL_HOST` и `ACTUAL_PORT`
- Запрещено: хардкодить `localhost:51234`

---

## Phase 2.2: Smoke Test
**Кто:** RooTrace САМ  
**Что:** Проверяет работоспособность моста
- Отправляет тестовый лог на сервер
- Проверяет получение через `get_debug_status`
- Валидирует что сервер получает данные

---

## Phase 1.1: Вставка проб
**Кто:** Кодер (через `new_task mode="code")`  
**Что:** Вставляет отладочные пробы в код
- Использует `apply_diff` (Block Rewrite) - НЕ `inject_probes` для Python
- После КАЖДОЙ вставки проверяет диагностику: `mcp--roo-trace--get_problems(filePath=...)`
- Создает `.patch` файл после успешной проверки
- Использует `FINAL_HOST` и `ACTUAL_PORT` (не хардкодит)

---

## Phase 1.2: Получение summary от кодера
**Кто:** RooTrace  
**Что:** Получает результаты вставки проб через `attempt_completion`
- Проверяет что пробы вставлены
- Проверяет результаты линтера

---

## Phase 5: WAIT
**Кто:** Пользователь  
**Что:** Запускает код и воспроизводит баг
- RooTrace записывает timestamp запроса: `_run_request_timestamp`
- Вывод: "Ready. Run the app and trigger the bug. Say 'Logs ready' when done."
- Запрещено: использовать инструменты, показывать таймеры/кнопки

---

## Phase 6: Чтение логов
**Кто:** RooTrace САМ (не делегирует!)  
**Что:** Читает логи из файла `.rootrace/ai_debug_logs.json`
- Использует `read_file` (НЕ `read_runtime_logs` MCP tool)
- Фильтрует логи по timestamp (только после `_run_request_timestamp`)
- Анализирует данные для каждой гипотезы H1-H5
- Определяет: confirmed/rejected/missing
- Вывод: `DATA: [N] logs`, `H1: [confirmed/rejected/missing]`, `VERDICT: [причина]`

---

## Phase 7.1: Анализ логов
**Кто:** Архитектор (через `new_task mode="architect")`  
**Что:** Анализирует логи и предлагает решение
- Получает BLACKLIST предыдущих попыток из memory или `_debug_history`
- Предлагает НОВОЕ решение (не из blacklist)
- Формат: PROBLEM:SOLUTION:FILE:LINE:CHANGE:JUSTIFICATION

---

## Phase 7.2: Получение summary от архитектора
**Кто:** RooTrace  
**Что:** Получает анализ и проверяет уникальность решения
- Использует `sequentialthinking` для скрытой валидации (если доступно)
- Сравнивает с предыдущими решениями из memory
- Если решение повторяется → ошибка, запрос нового решения

---

## Phase 7.3: Исправление кода
**Кто:** Кодер (через `new_task mode="code")`  
**Что:** Исправляет код согласно решению архитектора
- Читает файл, вносит исправление
- **ОБЯЗАТЕЛЬНО:** Проверяет диагностику после исправления: `mcp--roo-trace--get_problems(filePath=...)`
- Если ошибки → исправляет, проверяет снова
- Создает `.patch` файл
- НЕ удаляет существующие пробы

---

## Phase 7.4: Получение summary от кодера
**Кто:** RooTrace  
**Что:** Получает результаты исправления через `attempt_completion`
- Проверяет что исправление выполнено
- Сохраняет в memory или `_debug_history`
- Увеличивает `_current_iteration += 1`

---

## Phase 7.5: Повторный запуск
**Кто:** Пользователь  
**Что:** Запускает код снова для проверки исправления
- RooTrace запрашивает запуск
- Обновляет `_run_request_timestamp`

---

## Phase 7.6: Повтор цикла
**Кто:** RooTrace  
**Что:** Повторяет Phase 7.1-7.5 если проблема не решена
- Цикл продолжается до решения или явного указания остановиться

---

## Phase 8: Очистка
**Кто:** RooTrace САМ  
**Что:** Удаляет пробы и временные файлы
- Разрешено ТОЛЬКО если:
  1. Пользователь явно сказал "FIXED"/"problem solved"
  2. Все гипотезы [confirmed] или [rejected] (нет [missing])
  3. Баг исправлен и проверен через логи
- Предпочтительно: SELECTIVE CLEANUP (удаление проб из конкретных файлов)
- Fallback: `clear_session` (только если все файлы нужно очистить)
- Удаляет `.patch` и `.bak` файлы

---

## Обязательные вехи Phase 7 (FIX)

Когда RooTrace доходит до исправления кода, TODO list ДОЛЖЕН содержать:

1. `[ARCHITECT]` - Архитектурный обзор (память, lifecycle, Big O)
2. `[QA]` - Картирование граничных случаев (минимум 3 сценария)
3. `[SRE]` - Проверка надежности (таймауты, пулы соединений, async safety)
4. `[IMPLEMENT]` - Изменения кода с пробами RooTrace
5. `[CRITIC]` - Проверка после исправления (логи, глобальные состояния)

---

## Ключевые правила

- **RooTrace САМ делает:** Pre-Flight Check, Network Discovery, Smoke Test, чтение логов, очистка
- **Делегирует:** Разведку → архитектору, вставку проб → кодеру, анализ логов → архитектору, исправление → кодеру
- **Запрещено:** Пропускать Phase 2, Phase 2.2, проверку диагностики после каждой вставки
- **Обязательно:** Проверка диагностики после КАЖДОЙ вставки пробы и после КАЖДОГО исправления
