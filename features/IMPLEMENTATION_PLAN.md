# 🚀 План внедрения функций Roo Code в RooTrace

**Дата создания:** 2024  
**Статус:** В разработке

## 📊 Обзор

Из документации `roodocs` используется только **~20-30%** функционала. Данный план описывает внедрение оставшихся критичных и важных функций.

---

## 🔴 КРИТИЧНЫЕ (Приоритет 1) - 1-2 недели

### 1. Интеграция диагностики (Diagnostics Integration)
**Статус:** ✅ Реализовано  
**Сложность:** Средняя  
**Время:** 3-5 дней (выполнено)

**Что это:**
- Интеграция с панелью Problems в VS Code
- Доступ к ошибкам/предупреждениям через `@problems` mention
- Автоматическое обнаружение новых ошибок после правок

**Зачем:**
- Автоматически видеть все ошибки/предупреждения проекта
- Предлагать исправления на основе диагностики
- Улучшить качество кода через интеграцию с линтерами

**Технические детали:**
- Использовать `vscode.languages.getDiagnostics()`
- Создать MCP tool `mcp--roo-trace--get_problems`
- Фильтрация по severity (Error, Warning)
- Интеграция в промпты для автоматического исправления

**Файлы для создания:**
- ✅ `src/diagnostics-handler.ts` - обработчик диагностики (создан)
- ✅ Обновить `src/mcp-handler.ts` - добавить tool (добавлен `mcp--roo-trace--get_problems`)
- ✅ Обновить `src/extension.ts` - добавлен HTTP endpoint `/diagnostics`
- ✅ Обновить `src/types.ts` - добавлен интерфейс `DiagnosticInfo`
- ⏳ Обновить `prompts/ai-debugger-prompt.en.md` - добавить использование (TODO)

---

### 2. Кастомные инструкции из файлов (Custom Instructions from Files)
**Статус:** ❌ Не реализовано  
**Сложность:** Средняя  
**Время:** 4-6 дней

**Что это:**
- Загрузка правил из `.roo/rules/` (workspace-wide)
- Загрузка из `.roo/rules-{mode}/` (mode-specific)
- Поддержка глобальных правил `~/.roo/rules/`
- Fallback на `.roorules` файлы
- **НОВОЕ:** Опциональная lazy loading (загрузка по требованию)

**Зачем:**
- Хранить инструкции в файлах (версионирование)
- Применять правила глобально или по проекту
- Организовать правила по режимам
- **Экономия токенов:** Не загружать все правила сразу, а указывать ссылки и загружать по требованию

**Технические детали:**
- Рекурсивное чтение директорий `.roo/rules/`
- Парсинг markdown/txt файлов
- Приоритет: workspace > global, mode-specific > generic
- Кэширование загруженных правил
- **НОВОЕ:** Режим lazy loading:
  - Вместо полного содержимого файлов добавлять ссылки: `See .roo/rules/coding-style.md for coding standards`
  - Загружать содержимое только при явном запросе или упоминании
  - MCP tool `mcp--roo-trace--load_rule` для загрузки конкретного правила
  - Автоматическая загрузка критичных правил (помеченных как `critical: true`)

**Режимы работы:**
1. **Eager Loading (по умолчанию):** Все правила загружаются сразу в system prompt (как в Roo Code)
2. **Lazy Loading (опционально):** Правила загружаются по требованию, в system prompt добавляются только ссылки

**Файлы для создания:**
- `src/rules-loader.ts` - загрузчик правил с поддержкой lazy loading
- `src/rules-cache.ts` - кэш загруженных правил
- Обновить `src/role-manager.ts` - интеграция загрузки
- Обновить `src/mcp-handler.ts` - добавить tool `load_rule`
- Обновить `src/extension.ts` - инициализация при старте

---

### 3. Защита от отравления контекста (Context Poisoning Prevention)
**Статус:** ❌ Не реализовано  
**Сложность:** Высокая  
**Время:** 5-7 дней

**Что это:**
- Мониторинг качества контекста в сессии
- Детекция аномалий (галлюцинации, некорректные данные)
- Автоматическая очистка/сброс сессии при обнаружении проблем
- Валидация данных перед добавлением в контекст

**Зачем:**
- Предотвратить деградацию работы ИИ из-за плохих данных
- Стабильность в длинных сессиях
- Критично для production использования

**Технические детали:**
- Анализ сообщений на наличие control characters
- Детекция повторяющихся/бессмысленных ответов
- Мониторинг tool usage patterns
- Автоматический сброс сессии при превышении порога аномалий
- Логирование инцидентов для анализа

**Файлы для создания:**
- `src/context-monitor.ts` - мониторинг контекста
- `src/context-validator.ts` - валидация данных
- `src/anomaly-detector.ts` - детекция аномалий
- Обновить `src/mcp-handler.ts` - интеграция валидации

---

### 2.5. Техники улучшения рассуждений (Reasoning Enhancement Techniques)
**Статус:** ❌ Не реализовано  
**Сложность:** Низкая-Средняя  
**Время:** 2-3 дня

**Что это:**
Четыре техники для улучшения качества рассуждений ИИ:
1. **"Oh!" Hack** — принудительное включение механизма самопроверки
2. **Society of Thought** — активация трех экспертных личностей для обсуждения
3. **Conflict of Perspectives** — создание самокритики и конфликта мнений
4. **Expertise Diversity Synthesizer** — объединение научного подхода и креативности

**Зачем:**
- Удвоить точность в сложных рассуждениях
- Избавиться от подхалимства в цепочке рассуждений
- Получить более качественные и проверенные решения
- Улучшить самокритику и обнаружение ошибок

**Технические детали:**
- Интеграция техник в system prompt через кастомные инструкции
- Опциональное включение/выключение каждой техники
- Настройка через `.roo/rules/reasoning-techniques.md`
- Применение автоматически при сложных задачах

**Реализация техник:**

1. **"Oh!" Hack:**
   ```
   В процессе рассуждения обязательно используй фразы вроде:
   - "Подождите, дайте проверю..."
   - "О, я упустил..."
   - "Но что если..."
   Если найдешь ошибку в своих рассуждениях, укажи на нее и исправь.
   ```

2. **Society of Thought:**
   ```
   Действуй как команда из трех экспертов:
   - Скептичный критик (ищет слабые места)
   - Креативный дизайнер (предлагает решения)
   - Дотошный аналитик (проверяет логику)
   Обсудите решение задачи, по очереди выдвигая аргументы и опровергая ошибки друг друга.
   ```

3. **Conflict of Perspectives:**
   ```
   Сначала предложи решение, а затем жестко раскритикуй его, найдя слабые места.
   Сформулируй финальный ответ на основе этой критики.
   ```

4. **Expertise Diversity Synthesizer:**
   ```
   Сначала рассмотри задачу с точки зрения педантичного профессора (ищет точность).
   Затем с точки зрения креативного изобретателя (ищет инновации).
   Синтезируй оба подхода в финальном решении.
   ```

**Файлы для создания:**
- `src/reasoning-enhancer.ts` - обработчик техник
- `.roo/rules/reasoning-techniques.md` - конфигурация техник (пример)
- Обновить `src/role-manager.ts` - интеграция в промпты
- Обновить `src/rules-loader.ts` - поддержка reasoning техник

**Примечание:** Эти техники можно применять как часть кастомных инструкций или как отдельный модуль, который активируется при сложных задачах.

---

### 4. Индексация кодовой базы (Codebase Indexing)
**Статус:** ❌ Не реализовано  
**Сложность:** Очень высокая  
**Время:** 7-10 дней

**Что это:**
- Семантический поиск по коду через AI embeddings
- Интеграция с Qdrant (векторная БД)
- Парсинг кода через Tree-sitter
- Инструмент `codebase_search` для Roo

**Зачем:**
- Находить код по смыслу, а не по точному совпадению
- Улучшить понимание кодовой базы ИИ
- Кросс-проектный поиск паттернов

**Технические детали:**
- Настройка Qdrant (локально или cloud)
- Интеграция embedding provider (Google Gemini/OpenAI)
- Парсинг через Tree-sitter для разных языков
- Инкрементальная индексация при изменениях
- MCP tool `mcp--roo-trace--codebase_search`

**Файлы для создания:**
- `src/codebase-indexer.ts` - индексатор
- `src/embedding-client.ts` - клиент для embeddings
- `src/qdrant-client.ts` - клиент Qdrant
- Обновить `src/mcp-handler.ts` - добавить tool
- `docs/CODEBASE_INDEXING.md` - документация настройки

**Зависимости:**
- Qdrant (Docker или cloud)
- Embedding provider API key
- Tree-sitter парсеры

---

## 🟡 ВАЖНЫЕ (Приоритет 2) - 2-3 недели

### 5. Навыки (Skills)
**Статус:** ❌ Не реализовано  
**Сложность:** Средняя  
**Время:** 4-6 дней

**Что это:**
- Загрузка инструкций по требованию из `.roo/skills/{skill-name}/SKILL.md`
- Парсинг frontmatter (name, description)
- Поддержка bundled файлов (скрипты, шаблоны)

**Зачем:**
- Специализированные инструкции для конкретных задач
- Не засоряет базовый промпт
- Переиспользование навыков между проектами

**Технические детали:**
- Сканирование `.roo/skills/` при старте
- Парсинг YAML frontmatter
- Матчинг описания с запросом пользователя
- Загрузка SKILL.md при активации
- Поддержка mode-specific skills (`.roo/skills-{mode}/`)

**Файлы для создания:**
- `src/skills-loader.ts` - загрузчик навыков
- `src/skills-matcher.ts` - матчинг запросов
- Обновить `src/role-manager.ts` - интеграция
- `docs/SKILLS.md` - документация создания навыков

---

### 6. Очередь сообщений (Message Queueing)
**Статус:** ❌ Не реализовано  
**Сложность:** Средняя  
**Время:** 3-5 дней

**Что это:**
- Отправка нескольких сообщений, пока Roo обрабатывает предыдущие
- FIFO очередь сообщений
- Редактирование/удаление сообщений в очереди
- Неявное одобрение действий для queued сообщений

**Зачем:**
- Не прерывать поток мыслей
- Отправлять уточнения без ожидания
- Улучшить UX при длительных операциях

**Технические детали:**
- Очередь сообщений в памяти
- UI для отображения queued messages
- Обработка очереди после завершения текущего запроса
- Автоматическое одобрение для queued сообщений

**Файлы для создания:**
- `src/message-queue.ts` - менеджер очереди
- Обновить UI компоненты (если есть)
- Обновить `src/mcp-handler.ts` - обработка очереди

**Примечание:** Это может быть функция Roo Code UI, а не MCP сервера. Нужно уточнить архитектуру.

---

### 7. Улучшение промптов (Enhance Prompt)
**Статус:** ❌ Не реализовано  
**Сложность:** Низкая-Средняя  
**Время:** 2-4 дня

**Что это:**
- Автоматическое улучшение промптов перед отправкой
- Кастомизируемый шаблон улучшения
- Использование истории разговора для контекста

**Зачем:**
- Делать запросы понятнее и точнее
- Улучшить качество ответов ИИ
- Экономия времени на формулировку

**Технические детали:**
- MCP tool `mcp--roo-trace--enhance_prompt`
- Настраиваемый промпт-шаблон
- Интеграция с последними N сообщениями
- Возврат улучшенного промпта пользователю

**Файлы для создания:**
- `src/prompt-enhancer.ts` - улучшение промптов
- Обновить `src/mcp-handler.ts` - добавить tool
- Конфигурация шаблона в настройках

---

## 🟢 ПОЛЕЗНЫЕ (Приоритет 3) - по необходимости

### 8. Параллельное чтение файлов (Concurrent File Reads)
**Статус:** ❌ Не реализовано  
**Сложность:** Низкая  
**Время:** 1-2 дня

**Что это:**
- Чтение до 100 файлов за один запрос
- Batch обработка файлов

**Зачем:**
- Быстрее собирать контекст из нескольких файлов
- Уменьшить количество round-trips

**Технические детали:**
- Обновить существующий `read_file` tool для поддержки массивов
- Параллельное чтение через Promise.all
- Лимит на количество файлов (настраиваемый)

**Файлы для обновления:**
- `src/mcp-handler.ts` - обновить read_file tool

**Примечание:** Это может быть уже реализовано в Roo Code. Нужно проверить.

---

### 9. Переопределение системного промпта (Footgun Prompting)
**Статус:** ❌ Не реализовано  
**Сложность:** Низкая  
**Время:** 1-2 дня

**Что это:**
- Полная замена системного промпта через `.roo/system-prompt-{mode}`
- Поддержка переменных ({{mode}}, {{language}}, {{workspace}})

**Зачем:**
- Полный контроль над поведением режима
- Продвинутая настройка для экспертов

**Технические детали:**
- Проверка наличия `.roo/system-prompt-{mode}` файла
- Подстановка переменных
- Замена стандартного промпта при наличии файла

**Файлы для создания:**
- `src/system-prompt-loader.ts` - загрузчик кастомного промпта
- Обновить `src/role-manager.ts` - интеграция

**⚠️ ВНИМАНИЕ:** Это "footgun" - может сломать функциональность при неправильном использовании.

---

## 📋 План внедрения по этапам

### Этап 1: Фундамент (Неделя 1-2)
1. ✅ Интеграция диагностики (3-5 дней) - **ВЫПОЛНЕНО**
2. ✅ Кастомные инструкции (4-6 дней)
3. ✅ Защита от отравления контекста (5-7 дней)
4. ✅ Техники улучшения рассуждений (2-3 дня)

**Итого:** 12-18 дней

### Этап 2: Расширение (Неделя 3-5)
4. ✅ Навыки (4-6 дней)
5. ✅ Очередь сообщений (3-5 дней)
6. ✅ Улучшение промптов (2-4 дня)

**Итого:** 9-15 дней

### Этап 3: Оптимизация (Неделя 6+)
7. ⚠️ Индексация кодовой базы (7-10 дней) - опционально
8. ⚠️ Параллельное чтение (1-2 дня)
9. ⚠️ Переопределение промпта (1-2 дня)

**Итого:** 9-14 дней (опционально)

---

## 🎯 Метрики успеха

- **Критичные функции:** 100% покрытие
- **Важные функции:** 80%+ покрытие
- **Полезные функции:** По запросу

---

## 📝 Заметки

- Все новые MCP tools должны следовать паттерну `mcp--roo-trace--{tool_name}`
- Обновлять промпты в `prompts/ai-debugger-prompt.en.md` при добавлении новых функций
- Тесты для всех новых функций обязательны
- Документация в `docs/` для пользовательских функций

---

## 🔗 Связанные документы

- `roodocs` - исходная документация Roo Code
- `docs/ARCHITECTURE.md` - архитектура проекта
- `docs/API.md` - API документация
