# 🚀 План внедрения функций Roo Code в RooTrace

**Дата создания:** 2024  
**Статус:** В разработке

## 📊 Обзор

Из документации `roodocs` используется только **~20-30%** функционала. Данный план описывает внедрение оставшихся критичных и важных функций.

---

## 🔴 КРИТИЧНЫЕ (Приоритет 1) - 1-2 недели

### 1. Интеграция диагностики (Diagnostics Integration)
**Статус:** ✅ Реализовано  
**Сложность:** Средняя  
**Время:** 3-5 дней (выполнено)

**Что это:**
- Интеграция с панелью Problems в VS Code
- Доступ к ошибкам/предупреждениям через `@problems` mention
- Автоматическое обнаружение новых ошибок после правок

**Зачем:**
- Автоматически видеть все ошибки/предупреждения проекта
- Предлагать исправления на основе диагностики
- Улучшить качество кода через интеграцию с линтерами

**Технические детали:**
- Использовать `vscode.languages.getDiagnostics()`
- Создать MCP tool `mcp--roo-trace--get_problems`
- Фильтрация по severity (Error, Warning)
- Интеграция в промпты для автоматического исправления

**Файлы для создания:**
- ✅ `src/diagnostics-handler.ts` - обработчик диагностики (создан)
- ✅ Обновить `src/mcp-handler.ts` - добавить tool (добавлен `mcp--roo-trace--get_problems`)
- ✅ Обновить `src/extension.ts` - добавлен HTTP endpoint `/diagnostics`
- ✅ Обновить `src/types.ts` - добавлен интерфейс `DiagnosticInfo`

**Интеграция в промпты (модули):**
- ✅ `.roo/rules-ai-debugger/roo-00-role.md` - описание инструмента в списке доступных MCP tools
- ✅ `.roo/rules-ai-debugger/roo-00-input-filter.md` - использование при оценке данных (Phase 0)
- ✅ `.roo/rules-ai-debugger/roo-04-preflight.md` - проверка диагностики в Pre-Flight Check (Phase 0.4)
- ✅ `.roo/rules-ai-debugger/code-03-linter-protocol.md` - основной модуль для проверки линтера после изменений
- ✅ `.roo/rules-ai-debugger/code-01-probe-insertion.md` - проверка после вставки проб
- ✅ `.roo/rules-ai-debugger/code-02-code-fix.md` - проверка после исправления кода
- ✅ `.roo/rules-ai-debugger/roo-01-todo-list.md` - упоминание в Phase 0.4 (Pre-Flight Check)

---

### 2. Кастомные инструкции из файлов (Custom Instructions from Files)
**Статус:** ✅ Реализовано  
**Сложность:** Средняя  
**Время:** 4-6 дней (выполнено)

**Что это:**
- Загрузка правил из `.roo/rules/` (workspace-wide)
- Загрузка из `.roo/rules-{mode}/` (mode-specific)
- Поддержка глобальных правил `~/.roo/rules/`
- Fallback на `.roorules` файлы
- **НОВОЕ:** Опциональная lazy loading (загрузка по требованию)

**Зачем:**
- Хранить инструкции в файлах (версионирование)
- Применять правила глобально или по проекту
- Организовать правила по режимам
- **Экономия токенов:** Не загружать все правила сразу, а указывать ссылки и загружать по требованию

**Технические детали:**
- Рекурсивное чтение директорий `.roo/rules/`
- Парсинг markdown/txt файлов
- Приоритет: workspace > global, mode-specific > generic
- Кэширование загруженных правил
- **НОВОЕ:** Режим lazy loading:
  - Вместо полного содержимого файлов добавлять ссылки: `See .roo/rules/coding-style.md for coding standards`
  - Загружать содержимое только при явном запросе или упоминании
  - MCP tool `mcp--roo-trace--load_rule` для загрузки конкретного правила
  - Автоматическая загрузка критичных правил (помеченных как `critical: true`)

**Режимы работы:**
1. **Eager Loading (по умолчанию):** Все правила загружаются сразу в system prompt (как в Roo Code)
2. **Lazy Loading (реализовано):** Правила загружаются по требованию, в system prompt добавляются только ссылки
   - Минимальный базовый промпт загружает только критичные модули (language, output, error-handling, role, validator)
   - Остальные модули загружаются по требованию через `mcp--roo-trace--load_rule`
   - Старый большой промпт (`ai-debugger-prompt.en.md`) больше не используется

**Файлы для создания:**
- ✅ `src/rules-loader.ts` - загрузчик правил с поддержкой lazy loading (создан)
- ✅ `src/rules-cache.ts` - кэш загруженных правил (создан)
- ✅ Обновить `src/role-manager.ts` - интеграция загрузки, минимальный базовый промпт вместо большого (выполнено)
- ✅ Обновить `src/mcp-handler.ts` - добавить tool `load_rule` (добавлен `mcp--roo-trace--load_rule`)
- ✅ Обновить `src/extension.ts` - добавлена функция `copyPromptModules()` для копирования модулей при активации (выполнено)
- ✅ Обновить `.vscodeignore` - включить промпты и модули в сборку VSIX (выполнено)
- ✅ Создано 40 модулей в `.roo/rules-ai-debugger/` для lazy loading (4 базовых, 15 RooTrace, 5 Architect, 14 Coder, 2 дополнительных)
- ✅ Обновлены модули `roo-01-todo-list.md` и `roo-10-cycle-manage.md` - добавлена защита от зацикливания

**Дополнительно:**
- ✅ Исправлен `.vscodeignore` для включения `.roo/roo-trace-rules/` в VSIX пакет (prompts/ удален, так как не используется)
- ✅ Все промпты и модули теперь входят в сборку расширения
- ✅ Добавлена функция `copyPromptModules()` в `src/extension.ts` - автоматическое копирование модулей в рабочую область при активации
- ✅ Старый большой промпт (`ai-debugger-prompt.en.md`) больше не загружается - заменен на минимальный базовый промпт
- ✅ Минимальный базовый промпт загружает только критичные модули (language, output, error-handling, role, validator)
- ✅ Остальные модули загружаются по требованию через `mcp--roo-trace--load_rule`
- ✅ `.roomodes` файл удаляется и пересоздается при каждой активации (не перезаписывает пользовательские настройки)
- ✅ Добавлена защита от зацикливания в `roo-01-todo-list.md` и `roo-10-cycle-manage.md`

---

### 3. Защита от отравления контекста (Context Poisoning Prevention)
**Статус:** ✅ Реализовано  
**Сложность:** Высокая  
**Время:** 5-7 дней (выполнено)

**Что это:**
- Мониторинг качества контекста в сессии
- Детекция аномалий (галлюцинации, некорректные данные)
- Автоматическая очистка/сброс сессии при обнаружении проблем
- Валидация данных перед добавлением в контекст

**Зачем:**
- Предотвратить деградацию работы ИИ из-за плохих данных
- Стабильность в длинных сессиях
- Критично для production использования

**Технические детали:**
- Анализ сообщений на наличие control characters
- Детекция повторяющихся/бессмысленных ответов
- Мониторинг tool usage patterns
- Автоматический сброс сессии при превышении порога аномалий
- Логирование инцидентов для анализа

**Файлы для создания:**
- ✅ `src/context-monitor.ts` - мониторинг контекста (создан)
- ✅ `src/context-validator.ts` - валидация данных (создан)
- ✅ `src/anomaly-detector.ts` - детекция аномалий (создан)
- ✅ Обновить `src/mcp-handler.ts` - интеграция валидации (выполнено)

**Реализованные функции:**
- ✅ Валидация входящих tool calls (проверка на control characters, подозрительные паттерны)
- ✅ Валидация исходящих tool responses (проверка структуры и содержимого)
- ✅ Детекция повторяющихся сообщений (защита от зацикливания)
- ✅ Детекция бессмысленных ответов (повторяющиеся символы, низкое разнообразие слов)
- ✅ Мониторинг использования инструментов (защита от злоупотребления)
- ✅ Детекция зацикливания действий
- ✅ Автоматический сброс сессии при превышении порога аномалий (настраиваемый порог)
- ✅ Логирование всех аномалий для анализа

---

### 2.5. Техники улучшения рассуждений (Reasoning Enhancement Techniques)
**Статус:** ✅ Реализовано  
**Сложность:** Низкая-Средняя  
**Время:** 2-3 дня (выполнено)

**Что это:**
Четыре техники для улучшения качества рассуждений ИИ:
1. **"Oh!" Hack** — принудительное включение механизма самопроверки
2. **Society of Thought** — активация трех экспертных личностей для обсуждения
3. **Conflict of Perspectives** — создание самокритики и конфликта мнений
4. **Expertise Diversity Synthesizer** — объединение научного подхода и креативности

**Зачем:**
- Удвоить точность в сложных рассуждениях
- Избавиться от подхалимства в цепочке рассуждений
- Получить более качественные и проверенные решения
- Улучшить самокритику и обнаружение ошибок

**Технические детали:**
- Интеграция техник в system prompt через кастомные инструкции
- Опциональное включение/выключение каждой техники
- Настройка через `.roo/rules/reasoning-techniques.md`
- Применение автоматически при сложных задачах

**Реализация техник:**

1. **"Oh!" Hack:**
   ```
   В процессе рассуждения обязательно используй фразы вроде:
   - "Подождите, дайте проверю..."
   - "О, я упустил..."
   - "Но что если..."
   Если найдешь ошибку в своих рассуждениях, укажи на нее и исправь.
   ```

2. **Society of Thought:**
   ```
   Действуй как команда из трех экспертов:
   - Скептичный критик (ищет слабые места)
   - Креативный дизайнер (предлагает решения)
   - Дотошный аналитик (проверяет логику)
   Обсудите решение задачи, по очереди выдвигая аргументы и опровергая ошибки друг друга.
   ```

3. **Conflict of Perspectives:**
   ```
   Сначала предложи решение, а затем жестко раскритикуй его, найдя слабые места.
   Сформулируй финальный ответ на основе этой критики.
   ```

4. **Expertise Diversity Synthesizer:**
   ```
   Сначала рассмотри задачу с точки зрения педантичного профессора (ищет точность).
   Затем с точки зрения креативного изобретателя (ищет инновации).
   Синтезируй оба подхода в финальном решении.
   ```

**Файлы для создания:**
- ✅ `src/reasoning-enhancer.ts` - обработчик техник (создан)
- ✅ `.roo/rules/reasoning-techniques.md` - конфигурация техник (создан)
- ✅ Обновить `src/role-manager.ts` - интеграция в промпты (выполнено)
- ✅ Добавлен метод `loadReasoningConfig()` для загрузки конфигурации из файла

**Реализованные функции:**
- ✅ Генерация промптов для всех четырех техник
- ✅ Загрузка конфигурации из `.roo/rules/reasoning-techniques.md` (workspace, mode-specific, global)
- ✅ Поддержка YAML frontmatter для конфигурации
- ✅ Дефолтная конфигурация (только "Oh!" Hack включена по умолчанию)
- ✅ Автоматическая интеграция в system prompt через `RoleManager`
- ✅ Оценка сложности задачи для автоматической активации техник
- ✅ Опциональное включение/выключение каждой техники

**Примечание:** Техники автоматически загружаются в system prompt при активации расширения. По умолчанию включена только техника "Oh!" Hack для базовой самопроверки. Остальные техники можно включить через конфигурационный файл.

---

### 4. Индексация кодовой базы (Codebase Indexing)
**Статус:** ✅ Используется встроенная система Roo Code  
**Сложность:** Низкая (интеграция)  
**Время:** 1 день (выполнено)

**Что это:**
- Семантический поиск по коду через AI embeddings
- Интеграция с Qdrant (векторная БД)
- Парсинг кода через Tree-sitter
- Инструмент `codebase_search` для Roo

**Зачем:**
- Находить код по смыслу, а не по точному совпадению
- Улучшить понимание кодовой базы ИИ
- Кросс-проектный поиск паттернов

**Технические детали:**
- **ВАЖНО:** Roo Code уже имеет встроенную систему индексирования кодовой базы
- Roo Code предоставляет `codebase_search` tool через свой MCP сервер
- Настройка выполняется через UI Roo Code (кнопка индексации в чате)
- Поддержка Qdrant (локально через Docker или cloud)
- Поддержка embedding providers: Google Gemini (бесплатно), OpenAI, Ollama
- Автоматический парсинг через Tree-sitter для разных языков
- Инкрементальная индексация при изменениях
- File watching и smart updates

**Реализация:**
- ✅ Roo Code уже предоставляет `codebase_search` tool - дополнительная реализация не требуется
- ✅ Промпты обновлены для использования `codebase_search` от Roo Code
- ✅ Документация: пользователи настраивают индексацию через UI Roo Code

**Настройка (для пользователей):**
1. В Roo Code: нажать на иконку индексации в правом нижнем углу чата
2. Настроить Qdrant (Docker: `docker run -d -p 6333:6333 qdrant/qdrant`)
3. Настроить Embedding Provider (Google Gemini - бесплатно)
4. Сохранить и запустить индексацию
5. После индексации использовать `codebase_search` в промптах

**Файлы:**
- ✅ Промпты обновлены для упоминания `codebase_search` tool
- ✅ Документация в `roodocs` (встроенная в Roo Code)

**Примечание:** Не нужно создавать свою систему индексирования - Roo Code уже предоставляет полнофункциональную систему с UI, настройками и автоматическим обновлением индекса.

---

## 🟡 ВАЖНЫЕ (Приоритет 2) - 2-3 недели

### 5. Навыки (Skills)
**Статус:** ✅ Используется встроенная система Roo Code  
**Сложность:** Низкая (интеграция)  
**Время:** 1 день (выполнено)

**Что это:**
- Загрузка инструкций по требованию из `.roo/skills/{skill-name}/SKILL.md`
- Парсинг frontmatter (name, description)
- Поддержка bundled файлов (скрипты, шаблоны)
- Автоматическое обнаружение и матчинг навыков

**Зачем:**
- Специализированные инструкции для конкретных задач
- Не засоряет базовый промпт
- Переиспользование навыков между проектами

**Технические детали:**
- **ВАЖНО:** Roo Code уже имеет встроенную систему Skills
- Roo Code автоматически сканирует `.roo/skills/` и `~/.roo/skills/` при старте
- Roo Code парсит YAML frontmatter из SKILL.md файлов
- Roo Code автоматически матчит описания навыков с запросами пользователя
- Roo Code загружает SKILL.md через `read_file` при активации навыка
- Поддержка mode-specific skills через `.roo/skills-{mode}/` директории
- Приоритет: project > global, mode-specific > generic

**Реализация:**
- ✅ Roo Code автоматически обнаруживает и загружает skills - дополнительная реализация не требуется
- ✅ Промпты могут упоминать использование skills для специализированных задач
- ✅ Документация: пользователи создают skills в `.roo/skills/` или `~/.roo/skills/`

**Создание Skills (для пользователей):**
1. Создать директорию: `.roo/skills/{skill-name}/` или `~/.roo/skills/{skill-name}/`
2. Создать файл `SKILL.md` с frontmatter:
   ```yaml
   ---
   name: skill-name
   description: Detailed description of when to use this skill
   ---
   # Instructions
   ...
   ```
3. Опционально: добавить bundled файлы (скрипты, шаблоны) в ту же директорию
4. Roo Code автоматически обнаружит и будет использовать skill при совпадении запроса

**Файлы:**
- ✅ Удален `src/skills-loader.ts` - не нужен, Roo Code уже имеет встроенную систему
- ✅ Промпты могут упоминать использование skills для специализированных задач
- ✅ Документация доступна в Roo Code (встроенная)

**Примечание:** Не нужно создавать свою систему загрузки skills - Roo Code уже предоставляет полнофункциональную систему с автоматическим обнаружением, матчингом и загрузкой по требованию.

---

### 6. Очередь сообщений (Message Queueing)
**Статус:** ✅ Реализовано
**Сложность:** Средняя
**Время:** 3-5 дней (выполнено)

**Что это:**
- Отправка нескольких сообщений, пока Roo обрабатывает предыдущие
- FIFO очередь сообщений
- Редактирование/удаление сообщений в очереди
- Неявное одобрение действий для queued сообщений

**Зачем:**
- Не прерывать поток мыслей
- Отправлять уточнения без ожидания
- Улучшить UX при длительных операциях

**Технические детали:**
- Очередь сообщений в памяти (класс `MessageQueue`)
- Интеграция с MCP handler для обработки очереди
- Автоматическое одобрение для queued сообщений (пропуск проверки одобрения)
- Поддержка основных операций: enqueue, dequeue, edit, delete

**Реализация:**
- ✅ Создан файл `src/message-queue.ts` с классом `MessageQueue` (FIFO очередь, редактирование, удаление, неявное одобрение)
- ✅ Обновлен `src/mcp-handler.ts` — интеграция очереди и пропуск проверки одобрения для queued сообщений
- ✅ Написаны тесты `tests/message-queue.test.ts` (16 тестов)
- ✅ Функция готова к использованию

**Примечание:** Архитектура определена — реализована как часть MCP сервера RooTrace, интеграция с UI через существующие механизмы Roo Code.

---

### 7. Улучшение промптов (Enhance Prompt)
**Статус:** ❌ Не реализовано  
**Сложность:** Низкая-Средняя  
**Время:** 2-4 дня

**Что это:**
- Автоматическое улучшение промптов перед отправкой
- Кастомизируемый шаблон улучшения
- Использование истории разговора для контекста

**Зачем:**
- Делать запросы понятнее и точнее
- Улучшить качество ответов ИИ
- Экономия времени на формулировку

**Технические детали:**
- MCP tool `mcp--roo-trace--enhance_prompt`
- Настраиваемый промпт-шаблон
- Интеграция с последними N сообщениями
- Возврат улучшенного промпта пользователю

**Файлы для создания:**
- `src/prompt-enhancer.ts` - улучшение промптов
- Обновить `src/mcp-handler.ts` - добавить tool
- Конфигурация шаблона в настройках

---

## 🟢 ПОЛЕЗНЫЕ (Приоритет 3) - по необходимости

### 8. Параллельное чтение файлов (Concurrent File Reads)
**Статус:** ✅ Реализовано
**Сложность:** Низкая
**Время:** 1-2 дня (выполнено)

**Что это:**
- Чтение до 100 файлов за один запрос
- Batch обработка файлов
- Поддержка массивов путей (`paths`) в инструменте `read_file`

**Зачем:**
- Быстрее собирать контекст из нескольких файлов
- Уменьшить количество round-trips
- Улучшить производительность при работе с большим количеством файлов

**Технические детали:**
- Обновлен инструмент `read_file` в `src/mcp-handler.ts` для поддержки массивов путей (`paths`)
- Реализовано параллельное чтение через `Promise.all` с лимитом до 100 файлов за запрос
- Сохранена обратная совместимость: одиночный файл по-прежнему читается через параметр `path`
- Добавлена обработка ошибок на уровне каждого файла (ошибка чтения одного файла не прерывает чтение остальных)

**Файлы для обновления:**
- ✅ `src/mcp-handler.ts` - обновлен read_file tool (поддержка `paths`)

**Реализованные функции:**
- ✅ Поддержка чтения до 100 файлов за один запрос
- ✅ Параллельное чтение через `Promise.all`
- ✅ Обработка ошибок на уровне файла
- ✅ Обратная совместимость с существующим API

---

### 9. Переопределение системного промпта (Footgun Prompting)
**Статус:** ❌ Не реализовано  
**Сложность:** Низкая  
**Время:** 1-2 дня

**Что это:**
- Полная замена системного промпта через `.roo/system-prompt-{mode}`
- Поддержка переменных ({{mode}}, {{language}}, {{workspace}})

**Зачем:**
- Полный контроль над поведением режима
- Продвинутая настройка для экспертов

**Технические детали:**
- Проверка наличия `.roo/system-prompt-{mode}` файла
- Подстановка переменных
- Замена стандартного промпта при наличии файла

**Файлы для создания:**
- `src/system-prompt-loader.ts` - загрузчик кастомного промпта
- Обновить `src/role-manager.ts` - интеграция

**⚠️ ВНИМАНИЕ:** Это "footgun" - может сломать функциональность при неправильном использовании.

---

## 📋 План внедрения по этапам

### Этап 1: Фундамент (Неделя 1-2)
1. ✅ Интеграция диагностики (3-5 дней) - **ВЫПОЛНЕНО**
2. ✅ Кастомные инструкции (4-6 дней) - **ВЫПОЛНЕНО**
3. ✅ Защита от отравления контекста (5-7 дней) - **ВЫПОЛНЕНО**
4. ✅ Техники улучшения рассуждений (2-3 дня) - **ВЫПОЛНЕНО**

**Итого:** 12-18 дней

### Этап 2: Расширение (Неделя 3-5)
4. ✅ Навыки (4-6 дней)
5. ✅ Очередь сообщений (3-5 дней)
6. ❌ Улучшение промптов (2-4 дня)

**Итого:** 9-15 дней

### Этап 3: Оптимизация (Неделя 6+)
7. ⚠️ Индексация кодовой базы (7-10 дней) - опционально
8. ✅ Параллельное чтение (1-2 дня) - **ВЫПОЛНЕНО**
9. ⚠️ Переопределение промпта (1-2 дня)

**Итого:** 9-14 дней (опционально)

---

## 🎯 Метрики успеха

- **Критичные функции:** 100% покрытие
- **Важные функции:** 80%+ покрытие
- **Полезные функции:** По запросу

---

## 📝 Заметки

- Все новые MCP tools должны следовать паттерну `mcp--roo-trace--{tool_name}`
- Обновлять модули в `.roo/roo-trace-rules/` при добавлении новых функций (prompts/ удален, используется модульная система)
- Тесты для всех новых функций обязательны
- Документация в `docs/` для пользовательских функций
- **ВАЖНО:** При добавлении новых модулей в `.roo/roo-trace-rules/` убедиться, что они включены в `.vscodeignore` для сборки VSIX

---

## 🔗 Связанные документы

- `roodocs` - исходная документация Roo Code
- `docs/ARCHITECTURE.md` - архитектура проекта
- `docs/API.md` - API документация
