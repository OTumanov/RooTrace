# Тесты RooTrace

## Обзор

Тесты для проекта RooTrace покрывают основные компоненты системы отладки:
- **SharedLogStorage** - централизованное хранилище логов
- **CodeInjector** - инъекция проб в код
- **MCP Handler** - обработка MCP запросов
- **HTTP Integration** - интеграция HTTP сервера с хранилищем
- **Session Manager** - управление сессиями отладки
- **File Lock Utils** - утилиты для файловых блокировок
- **Log Exporter** - экспорт логов в различные форматы
- **Extension Activation** - активация и деактивация расширения (НОВОЕ)
- **HTTP Server** - обработка HTTP запросов (НОВОЕ)
- **Rate Limiting** - ограничение частоты запросов (НОВОЕ)
- **Configuration Management** - управление конфигурацией (НОВОЕ)
- **VS Code Commands** - команды расширения (НОВОЕ)

## Структура тестов

```
tests/
├── shared-log-storage.test.ts      # Тесты хранилища логов (25 активных тестов) ✅
├── code-injector.test.ts            # Тесты инъекции проб (57+ тестов)
├── mcp-registration.test.ts         # Тесты регистрации MCP сервера
├── http-to-dashboard-integration.test.ts  # Интеграционные тесты HTTP → Storage → MCP (11 активных тестов) ✅
├── session-manager.test.ts          # Тесты менеджера сессий
├── file-lock-utils.test.ts          # Тесты файловых блокировок
├── log-exporter.test.ts             # Тесты экспорта логов
├── e2e-comprehensive.test.ts         # E2E тесты полного потока
├── extension-activation.test.ts      # Тесты активации/деактивации расширения (НОВОЕ)
├── http-server.test.ts               # Тесты HTTP сервера (НОВОЕ)
├── rate-limiting.test.ts             # Тесты rate limiting (НОВОЕ)
├── config-management.test.ts         # Тесты управления конфигурацией (НОВОЕ)
├── vscode-commands.test.ts           # Тесты VS Code команд (НОВОЕ)
├── helpers/
│   └── test-helpers.ts              # Вспомогательные функции для тестов (обновлено: фабрики, события)
└── README.md                        # Этот файл
```

## Запуск тестов

### Все тесты
```bash
npm test
```

### Конкретный файл
```bash
npm test -- tests/shared-log-storage.test.ts
```

### Конкретный тест
```bash
npm test -- --testNamePattern="должен добавлять и возвращать логи"
```

### С увеличенным таймаутом
```bash
npm test -- --testTimeout=15000
```

## Особенности тестирования

### Константы

Используются константы из `tests/constants.ts`:
- `LOG_SAVE_TIMEOUT` (300ms) - время на сохранение одного лога
- `FILE_SYNC_TIMEOUT` (500ms) - время на синхронизацию файла
- `HIGH_LOAD_TIMEOUT` (1000ms) - время на сохранение большого количества логов
- `WATCHER_TIMEOUT` (1000ms) - время на обработку fs.watchFile
- `INIT_DELAY` (50ms) - задержка для инициализации компонентов
- `SHORT_DELAY` (100ms) - короткая задержка для простых операций
- `MEDIUM_DELAY` (200ms) - средняя задержка для операций сохранения
- `LONG_DELAY` (500ms) - длинная задержка для сложных операций
- `LOCK_TEST_DELAY_*` - задержки для тестов файловых блокировок (10ms, 30ms, 50ms, 100ms)
- `MAX_RETRY_ATTEMPTS` (5) - количество повторных попыток
- `RETRY_DELAY` (200ms) - задержка между попытками

### Изоляция тестов

Тесты используют временные директории и очищают состояние перед каждым тестом:
- Каждый тест использует уникальную временную директорию (`tmpdir()`)
- Файлы создаются во временных директориях
- Состояние очищается в `beforeEach` через `await storage.clear()`
- Singleton'ы сбрасываются через `(Class as any).instance = undefined`
- Рабочая директория меняется через `process.chdir(testDir)` для каждого теста

**Пример изоляции (как в `shared-log-storage.test.ts`):**
```typescript
beforeEach(async () => {
  testDir = path.join(tmpdir(), `test-${Date.now()}-${Math.random()}`);
  fs.mkdirSync(testDir, { recursive: true });
  
  // Устанавливаем рабочую директорию
  process.chdir(testDir);
  
  // Сбрасываем singleton
  (SharedLogStorage as any).instance = undefined;
  
  // Создаем storage
  storage = SharedLogStorage.getInstance();
  await storage.clear();
});

afterEach(async () => {
  if (storage) {
    (storage as any).stopWatcher();
    await storage.clear();
  }
  (SharedLogStorage as any).instance = undefined;
  process.chdir(originalCwd);
  fs.rmSync(testDir, { recursive: true, force: true });
});
```

**Примечание:** В `http-to-dashboard-integration.test.ts` используются тестовые фабрики (`createTestStorage()`, `cleanupTestStorage()`), но основной подход - через `process.chdir()`.

### Асинхронные операции

Тесты используют события и хелпер `waitForLogsSaved()` вместо polling:
```typescript
import { waitForLogsSaved } from './helpers/test-helpers';
import { LOG_SAVE_TIMEOUT } from './constants';

// Выполнение операции
await storage.addLog(...);

// Ожидание через хелпер
await waitForLogsSaved(storage, expectedCount, LOG_SAVE_TIMEOUT);
```

Или с событиями:
```typescript
const logAddedHandler = (log: RuntimeLog) => {
  // Обработка события
};
storage.on('logAdded', logAddedHandler);

// Выполнение операции
await storage.addLog(...);

// Ожидание и очистка
await waitForLogsSaved(storage, 1, LOG_SAVE_TIMEOUT);
storage.removeListener('logAdded', logAddedHandler);
```

### Проверка rollback

Тесты для rollback явно проверяют все условия:
```typescript
expect(result.success).toBe(false);
expect(result.rollback).toBe(true);
expect(result.syntaxCheck?.passed).toBe(false);
expect(finalBytes).toEqual(originalBytes);
```

### Языки без компиляторов

Тесты для языков, требующих компиляторов (JSX, TSX, Python), корректно обрабатывают отсутствие компиляторов:
```typescript
if (!result.success && result.rollback) {
  expect(result.rollback).toBe(true);
  expect(content).toBe(originalContent);
  // Это ожидаемое поведение - тест проходит успешно
  return;
}
```

## Статистика тестов

### Текущее состояние
- **Всего тестов**: 150+ (добавлено ~56 новых тестов)
- **Проходящих**: 118+ (79%+)
- **Пропущенных**: 7 (помечены `test.skip` - нестабильны при полном запуске, но проходят при отдельном)
- **Падающих**: 10+ (требуют дополнительной изоляции)

### Новые тесты (критически важные компоненты)

#### ✅ Полностью проходящие тесты

1. **rate-limiting.test.ts** (10 тестов) ✅
   - Проверяет rate limiting для HTTP запросов
   - Тесты: разрешение запросов, блокировка при превышении, сброс счетчика, разные IP, настройки из конфигурации, x-forwarded-for header
   - Статус: **10/10 passed** (1.479s)
   - Время выполнения: ~1.5 секунды

2. **config-management.test.ts** (11 тестов) ✅
   - Проверяет управление конфигурацией (.ai_debug_config, .debug_port)
   - Тесты: создание конфигов, шифрование, чтение, удаление, несколько workspace, обработка ошибок
   - Статус: **11/11 passed** (1.474s)
   - Время выполнения: ~1.5 секунды

3. **vscode-commands.test.ts** (11 тестов) ✅
   - Проверяет VS Code команды расширения
   - Тесты: startServer, stopServer, clearLogs, openDashboard, cleanup, exportJSON, showUserInstructions, continueDebugging, markResolved, обработка ошибок
   - Статус: **11/11 passed** (1.28s)
   - Время выполнения: ~1.3 секунды

#### ⚠️ Требуют доработки

4. **extension-activation.test.ts** (12 тестов) ⚠️
   - Проверяет активацию и деактивацию расширения
   - Тесты: создание output channel, запуск HTTP сервера, регистрация MCP, создание сессии, синхронизация роли, обработка ошибок
   - Статус: **Компиляция успешна, тесты запускаются** (требуют больше времени из-за асинхронных операций)
   - Проблемы: тесты прерываются из-за длительных операций с серверами

5. **http-server.test.ts** (11 тестов) ⚠️
   - Проверяет HTTP сервер (POST /, GET /logs, CORS, OPTIONS, rate limiting, обработка ошибок)
   - Статус: **Компиляция успешна, требует доработки** (тесты с реальным HTTP сервером требуют больше времени)
   - Проблемы: тесты прерываются из-за создания реальных HTTP серверов

**Примечание**: Статистика может варьироваться в зависимости от порядка запуска тестов из-за состояния между тестами.

### Результаты по файлам (при отдельном запуске)

#### Существующие тесты
- `shared-log-storage.test.ts`: ✅ **25/25 активных** (исправлено 5 тестов через улучшение изоляции с уникальными временными директориями) 🎉
- `http-to-dashboard-integration.test.ts`: ✅ **11/11 активных** (исправлено 2 теста через улучшение изоляции) 🎉
- `code-injector.test.ts`: ✅ 57+ тестов
- `session-manager.test.ts`: ✅ 9 тестов
- `file-lock-utils.test.ts`: ✅ 6 тестов
- `mcp-registration.test.ts`: ✅ тесты регистрации MCP
- `log-exporter.test.ts`: ✅ тесты экспорта логов
- `e2e-comprehensive.test.ts`: ✅ E2E тесты

#### Новые тесты (2026)
- `rate-limiting.test.ts`: ✅ **10/10 passed** (1.479s)
- `config-management.test.ts`: ✅ **11/11 passed** (1.474s)
- `vscode-commands.test.ts`: ✅ **11/11 passed** (1.28s)
- `extension-activation.test.ts`: ⚠️ **12 тестов** (компиляция успешна, требуют больше времени)
- `http-server.test.ts`: ⚠️ **11 тестов** (компиляция успешна, требуют доработки)

### Пропущенные тесты

**Текущее состояние**: Большинство тестов исправлено через улучшение изоляции (2026-01-19)

**Оставшиеся пропущенные тесты** (через `test.skip`):
- Осталось ~1 тест, требующий дополнительной доработки

**Исправленные тесты** (2026-01-19):
- ✅ `shared-log-storage.test.ts`: исправлено 5 тестов (race conditions, синхронизация, высокая нагрузка)
- ✅ `http-to-dashboard-integration.test.ts`: исправлено 2 теста (MCP visibility, специальные символы)

**Причина исправления**: Использование уникальных временных директорий для каждого теста через `tmpdir()` и правильная изоляция через `process.chdir()` и сброс singleton'ов.

### Известные проблемы

~5+ тестов могут падать при полном запуске из-за недостаточной изоляции между тестами в других файлах. Рекомендуется применить тот же подход с тестовыми фабриками, что и в исправленных файлах.

## Вспомогательные функции и константы

### `tests/helpers/test-helpers.ts`

Содержит переиспользуемые функции:
- `createTestFile()` - создание тестовых файлов
- `cleanupTestFiles()` - очистка тестовых файлов
- `expectProbeInjected()` - проверка инъекции пробы
- `expectProbeRemoved()` - проверка удаления пробы
- `waitForLogsSaved()` - ожидание сохранения логов через события (используется вместо `setTimeout`)
- `createTestStorage()` - создание изолированного экземпляра SharedLogStorage для тестов (используется в `http-to-dashboard-integration.test.ts`)
- `cleanupTestStorage()` - очистка и остановка тестового storage (используется в `http-to-dashboard-integration.test.ts`)
- `waitForEvent()` - ожидание события на EventEmitter с таймаутом

### `tests/constants.ts`

Содержит константы для таймаутов и повторных попыток:
- `LOG_SAVE_TIMEOUT` - время на сохранение одного лога (300ms)
- `FILE_SYNC_TIMEOUT` - время на синхронизацию файла (500ms)
- `HIGH_LOAD_TIMEOUT` - время на сохранение большого количества логов (1000ms)
- `WATCHER_TIMEOUT` - время на обработку fs.watchFile (1000ms)
- `INIT_DELAY` - задержка для инициализации компонентов (50ms)
- `SHORT_DELAY` - короткая задержка для простых операций (100ms)
- `MEDIUM_DELAY` - средняя задержка для операций сохранения (200ms)
- `LONG_DELAY` - длинная задержка для сложных операций (500ms)
- `LOCK_TEST_DELAY_*` - задержки для тестов файловых блокировок
- `MAX_RETRY_ATTEMPTS` - количество повторных попыток (5)
- `RETRY_DELAY` - задержка между попытками (200ms)

## История изменений

### 2026-01-19: Критические исправления изоляции тестов

**Исправлено:**
- ✅ Исправлено 6 пропущенных тестов через улучшение изоляции
- ✅ Улучшена изоляция в `shared-log-storage.test.ts` (уникальные временные директории через `tmpdir()`)
- ✅ Улучшена изоляция в `http-to-dashboard-integration.test.ts`
- ✅ Исправлены синтаксические ошибки в тестах
- ✅ Добавлена функция `waitForEvent()` для ожидания событий

**Результаты:**
- Исправлено 6 пропущенных тестов (убраны `test.skip`)
- Улучшена изоляция через уникальные временные директории
- Исправлены ошибки компиляции

## Рекомендации

1. **Создавайте уникальные временные директории** для каждого теста через `tmpdir()`
2. **Используйте `process.chdir()`** для установки рабочей директории перед созданием singleton'ов
3. **Всегда очищайте состояние** перед тестами: `await storage.clear()`
4. **Сбрасывайте singleton'ы** через `(Class as any).instance = undefined` перед созданием новых экземпляров
5. **Используйте события** вместо polling для ожидания асинхронных операций (`waitForLogsSaved()`, `waitForEvent()`)
6. **Фильтруйте логи** по контексту для изоляции тестовых данных
7. **Используйте повторные попытки** для проверки асинхронных операций
8. **Увеличивайте таймауты** для операций с большим количеством данных
9. **Для HTTP тестов**: используйте моки или тестовые серверы с коротким временем жизни
10. **Для тестов активации**: увеличьте таймауты или используйте моки для длительных операций
11. **Восстанавливайте рабочую директорию** в `afterEach`/`afterAll`
