# План рефакторинга mcp-handler.ts

## Текущее состояние

- **Файл:** `src/mcp-handler.ts`
- **Размер:** 1942 строки
- **Проблема:** Монолитный файл с множеством ответственностей
- **Существующая структура:** `src/mcp-handler/` содержит только `tool-schemas.ts` и `types.ts`

### Существующие зависимости

**Импорты в `mcp-handler.ts`:**
- `./mcp-handler/tool-schemas` - MCP_TOOL_SCHEMAS
- `./code-injector` - injectProbe, getAllProbes, removeAllProbesFromFile, getServerUrl
- `./shared-log-storage` - SharedLogStorage, RuntimeLog, Hypothesis
- `./error-handler` - handleError, logInfo, logDebug
- `./types` - LogData
- `./rootrace-dir-utils` - getRootraceFilePath
- `./rules-loader` - RulesLoader
- `./context-monitor` - ContextMonitor
- `./message-queue` - MessageQueue
- `./utils` - getWorkspaceRoot, normalizeFilePath, findGitRoot

**Импорты из `mcp-handler.ts`:**
- `mcp-server.ts` → `./mcp-handler` (RooTraceMCPHandler)

**Паттерны проекта:**
- `code-injector/` - модульная структура с `index.ts` в корне
- `utils/` - плоская структура с `index.ts` в корне
- `code-injector.ts` - реэкспорт для обратной совместимости

## Цели рефакторинга

1. Разделить монолитный файл на логические модули
2. Улучшить читаемость и поддерживаемость
3. Упростить тестирование отдельных компонентов
4. Следовать паттернам, используемым в других модулях проекта (например, `code-injector/`)

## Предлагаемая структура

**Важно:** Следуем паттернам проекта (`code-injector/`, `utils/`):
- `index.ts` в корне модуля для централизованного экспорта
- Файл `mcp-handler.ts` на верхнем уровне для обратной совместимости (реэкспорт)

```
src/
├── mcp-handler.ts              # 🔄 Реэкспорт для обратной совместимости (как code-injector.ts)
│
└── mcp-handler/                # Основной модуль
    ├── index.ts                # ✅ Централизованный экспорт (как code-injector/index.ts)
    ├── handler.ts              # Основной класс-оркестратор (упрощенный)
    ├── tool-schemas.ts         # ✅ Уже существует
    ├── types.ts                # ✅ Уже существует
    │
    ├── handlers/               # Обработчики инструментов (tools)
    │   ├── index.ts            # Экспорт всех обработчиков
    │   ├── read-runtime-logs.ts
    │   ├── clear-logs.ts
    │   ├── get-debug-status.ts
    │   ├── clear-session.ts
    │   ├── inject-probes.ts
    │   ├── inject-multiple-probes.ts
    │   ├── show-user-instructions.ts
    │   ├── read-file.ts
    │   ├── get-problems.ts
    │   └── load-rule.ts
    │
    ├── resources/              # Обработчики ресурсов (MCP resources)
    │   ├── index.ts            # Экспорт всех обработчиков ресурсов
    │   ├── list-resources.ts
    │   └── read-resource.ts
    │
    ├── request-handlers/        # Обработчики базовых MCP запросов
    │   ├── index.ts            # Экспорт всех обработчиков
    │   ├── initialize.ts
    │   └── list-tools.ts
    │
    ├── security/               # Утилиты безопасности
    │   ├── index.ts            # Экспорт всех утилит
    │   ├── approval-checker.ts
    │   └── git-commit-checker.ts
    │
    ├── file-utils/             # Утилиты для работы с файлами
    │   ├── index.ts            # Экспорт всех утилит
    │   ├── path-normalizer.ts
    │   ├── git-utils.ts        # Обертка над utils/git-utils
    │   └── probe-finder.ts
    │
    ├── server-utils/           # Утилиты для работы с сервером
    │   ├── index.ts            # Экспорт всех утилит
    │   └── server-tester.ts
    │
    ├── injection-utils/         # Утилиты для инъекции проб
    │   ├── index.ts            # Экспорт всех утилит
    │   ├── retry-handler.ts
    │   └── validation.ts
    │
    └── logging/                # Логирование MCP запросов
        ├── index.ts            # Экспорт всех утилит
        └── mcp-logger.ts
```

**Структура экспортов (следуя паттерну `code-injector/index.ts`):**

```typescript
// src/mcp-handler/index.ts
export { RooTraceMCPHandler, startRooTraceMCP, rooTraceMCP } from './handler';
export * from './types';
export { MCP_TOOL_SCHEMAS } from './tool-schemas';
// ... реэкспорт из подмодулей по необходимости
```

```typescript
// src/mcp-handler.ts (для обратной совместимости)
export { RooTraceMCPHandler, startRooTraceMCP, rooTraceMCP } from './mcp-handler';
export * from './mcp-handler/types';
export { MCP_TOOL_SCHEMAS } from './mcp-handler/tool-schemas';
```

## Детальный план разбиения

### 1. Обработчики инструментов (`handlers/`)

Каждый обработчик инструмента выносится в отдельный файл с единым интерфейсом:

```typescript
// handlers/base-handler.ts
export interface ToolHandler {
  handle(args: any, context: HandlerContext): Promise<CallToolResult>;
}

export interface HandlerContext {
  sharedStorage: SharedLogStorage;
  contextMonitor: ContextMonitor;
  messageQueue: MessageQueue;
  getWorkspaceRoot: () => string;
  normalizeFilePath: (path: string) => string;
  // ... другие зависимости
}
```

**Файлы:**
- `read-runtime-logs.ts` (~50 строк) - обработка read_runtime_logs
- `clear-logs.ts` (~30 строк) - обработка clear_logs
- `get-debug-status.ts` (~60 строк) - обработка get_debug_status
- `clear-session.ts` (~150 строк) - обработка clear_session
- `inject-probes.ts` (~200 строк) - обработка inject_probes
- `inject-multiple-probes.ts` (~200 строк) - обработка inject_multiple_probes
- `show-user-instructions.ts` (~90 строк) - обработка show_user_instructions
- `read-file.ts` (~110 строк) - обработка read_file
- `get-problems.ts` (~100 строк) - обработка get_problems
- `load-rule.ts` (~65 строк) - обработка load_rule

### 2. Обработчики ресурсов (`resources/`)

- `list-resources.ts` (~60 строк) - формирование списка ресурсов
- `read-resource.ts` (~120 строк) - чтение конкретного ресурса

### 3. Обработчики базовых запросов (`request-handlers/`)

- `initialize.ts` (~30 строк) - Initialize request
- `list-tools.ts` (~20 строк) - ListTools request

### 4. Утилиты безопасности (`security/`)

- `approval-checker.ts` (~50 строк) - проверка разрешений на чтение логов
- `git-commit-checker.ts` (~60 строк) - проверка git commit перед редактированием

### 5. Утилиты файлов (`file-utils/`)

- `path-normalizer.ts` (~20 строк) - нормализация путей, проверка Python файлов
- `git-utils.ts` (~10 строк) - обертка над utils/git-utils
- `probe-finder.ts` (~55 строк) - поиск файлов с пробами

### 6. Утилиты сервера (`server-utils/`)

- `server-tester.ts` (~120 строк) - тестирование сервера через write/read

### 7. Утилиты инъекции (`injection-utils/`)

- `retry-handler.ts` (~40 строк) - retry механизм для инъекции проб
- `validation.ts` (~100 строк) - валидация параметров инъекции

### 8. Логирование (`logging/`)

- `mcp-logger.ts` (~30 строк) - логирование MCP запросов/ответов

### 9. Основной класс (`handler.ts`)

Упрощенный класс-оркестратор (~200-300 строк):
- Инициализация сервера
- Регистрация обработчиков запросов
- Делегирование вызовов инструментов соответствующим обработчикам
- Управление жизненным циклом сервера

## Приоритизация задач

### 📊 Сводная таблица приоритетов

| Категория | Количество задач | Критичность | Время выполнения |
|-----------|------------------|-------------|------------------|
| 🔴 Блокирующие | 5 | Критично | ~30-45 мин |
| 🟠 Критические | 18 | Высокая | ~3-4 часа |
| 🟡 Средние | 4 | Средняя | ~1 час |
| 🟢 Опциональные | 4 | Низкая | ~1-2 часа |
| **ИТОГО** | **31** | - | **~5-7 часов** |

---

### 🔴 БЛОКИРУЮЩИЕ задачи (без них нельзя двигаться дальше)

Эти задачи должны быть выполнены в первую очередь, так как от них зависят все остальные:

1. **Создать структуру директорий**
   - Создать все необходимые папки: `handlers/`, `resources/`, `request-handlers/`, `security/`, `file-utils/`, `server-utils/`, `injection-utils/`, `logging/`
   - Без структуры нельзя создавать файлы

2. **Создать базовые интерфейсы и типы**
   - `handlers/base-handler.ts` - интерфейсы `ToolHandler`, `HandlerContext`
   - Расширить `types.ts` при необходимости
   - Без интерфейсов нельзя создавать обработчики

3. **Создать основной класс `handler.ts`**
   - Минимальная версия с регистрацией обработчиков
   - Без него не будет работать MCP сервер

4. **Создать `mcp-handler/index.ts` (централизованный экспорт)**
   - Экспорт основного класса и типов
   - Без него не будет работать импорт из модуля

5. **Создать `mcp-handler.ts` на верхнем уровне (реэкспорт)**
   - Реэкспорт для обратной совместимости
   - Без него сломается `mcp-server.ts` → `./mcp-handler`

**Порядок выполнения блокирующих задач:**
1. Структура директорий
2. Базовые интерфейсы
3. Минимальный `handler.ts` (только скелет)
4. `index.ts` и реэкспорт
5. Затем можно переходить к остальным задачам

---

### 🟠 КРИТИЧЕСКИЕ задачи (необходимы для полноценной работы)

Эти задачи критичны для функционирования системы, но не блокируют начало работы:

#### Группа A: Утилиты (используются многими обработчиками)

1. **`logging/mcp-logger.ts`** - логирование MCP запросов
   - Используется везде для логирования
   - Приоритет: высокий

2. **`file-utils/path-normalizer.ts`** - нормализация путей, проверка Python файлов
   - Используется в inject_probes, inject_multiple_probes, read_file, get_problems
   - Приоритет: высокий

3. **`file-utils/git-utils.ts`** - обертка над utils/git-utils
   - Используется в git-commit-checker
   - Приоритет: средний

4. **`security/approval-checker.ts`** - проверка разрешений на чтение логов
   - Используется в read_runtime_logs
   - Приоритет: высокий

5. **`security/git-commit-checker.ts`** - проверка git commit перед редактированием
   - Используется в inject_probes, inject_multiple_probes
   - Приоритет: высокий

6. **`injection-utils/retry-handler.ts`** - retry механизм для инъекции проб
   - Используется в inject_probes, inject_multiple_probes
   - Приоритет: высокий

7. **`injection-utils/validation.ts`** - валидация параметров инъекции
   - Используется в inject_probes, inject_multiple_probes
   - Приоритет: высокий

8. **`file-utils/probe-finder.ts`** - поиск файлов с пробами
   - Используется в clear_session
   - Приоритет: средний

9. **`server-utils/server-tester.ts`** - тестирование сервера
   - Используется в get_debug_status
   - Приоритет: средний

#### Группа B: Обработчики базовых MCP запросов

10. **`request-handlers/initialize.ts`** - Initialize request
    - Обязательный для MCP протокола
    - Приоритет: критический

11. **`request-handlers/list-tools.ts`** - ListTools request
    - Обязательный для MCP протокола
    - Приоритет: критический

#### Группа C: Обработчики ресурсов

12. **`resources/list-resources.ts`** - формирование списка ресурсов
    - Используется в MCP протоколе
    - Приоритет: высокий

13. **`resources/read-resource.ts`** - чтение конкретного ресурса
    - Используется в MCP протоколе
    - Приоритет: высокий

#### Группа D: Обработчики инструментов (по приоритету использования)

14. **`handlers/read-runtime-logs.ts`** - обработка read_runtime_logs
    - Часто используемый инструмент
    - Приоритет: критический

15. **`handlers/inject-probes.ts`** - обработка inject_probes
    - Основной инструмент для отладки
    - Приоритет: критический

16. **`handlers/inject-multiple-probes.ts`** - обработка inject_multiple_probes
    - Важный инструмент для массовой инъекции
    - Приоритет: высокий

17. **`handlers/clear-session.ts`** - обработка clear_session
    - Важный инструмент для очистки
    - Приоритет: высокий

18. **`handlers/get-debug-status.ts`** - обработка get_debug_status
    - Полезный инструмент для проверки состояния
    - Приоритет: средний

19. **`handlers/read-file.ts`** - обработка read_file
    - Часто используемый инструмент
    - Приоритет: высокий

20. **`handlers/get-problems.ts`** - обработка get_problems
    - Полезный инструмент для диагностики
    - Приоритет: средний

21. **`handlers/load-rule.ts`** - обработка load_rule
    - Используется для загрузки правил
    - Приоритет: средний

22. **`handlers/clear-logs.ts`** - обработка clear_logs
    - Простой инструмент
    - Приоритет: средний

23. **`handlers/show-user-instructions.ts`** - обработка show_user_instructions
    - UI инструмент
    - Приоритет: средний

---

### 🟡 СРЕДНИЕ задачи (важны для качества, но не критичны)

24. **Создать `index.ts` в каждом подмодуле**
    - `handlers/index.ts` - экспорт всех обработчиков
    - `resources/index.ts` - экспорт обработчиков ресурсов
    - `request-handlers/index.ts` - экспорт обработчиков запросов
    - `security/index.ts` - экспорт утилит безопасности
    - `file-utils/index.ts` - экспорт файловых утилит
    - `server-utils/index.ts` - экспорт серверных утилит
    - `injection-utils/index.ts` - экспорт утилит инъекции
    - `logging/index.ts` - экспорт логирования
    - Улучшает организацию, но не критично для работы

25. **Оптимизация структуры экспортов в `mcp-handler/index.ts`**
    - Реэкспорт из подмодулей через их index.ts
    - Улучшает читаемость импортов

26. **Рефакторинг основного класса `handler.ts`**
    - Упрощение до оркестрации
    - Удаление дублирующегося кода
    - Оптимизация импортов

27. **Проверка всех инструментов вручную**
    - Тестирование каждого инструмента после рефакторинга
    - Проверка всех ресурсов
    - Проверка базовых запросов

---

### 🟢 ОПЦИОНАЛЬНЫЕ задачи (можно сделать позже)

28. **Обновить существующие тесты**
    - Адаптировать тесты под новую структуру
    - Создать тесты для новых модулей
    - Можно сделать после основного рефакторинга

29. **Дополнительная документация**
    - JSDoc комментарии для всех публичных API
    - README для модуля mcp-handler
    - Описание архитектуры

30. **Оптимизация импортов**
    - Проверка циклических зависимостей
    - Оптимизация tree-shaking
    - Минимизация bundle size

31. **Рефакторинг для улучшения производительности**
    - Кэширование результатов
    - Ленивая загрузка модулей
    - Оптимизация частых операций

---

## Рекомендуемый порядок выполнения

### Фаза 1: Блокирующие задачи (обязательно сначала)
1. Структура директорий
2. Базовые интерфейсы (`handlers/base-handler.ts`)
3. Минимальный `handler.ts` (только скелет с регистрацией)
4. `mcp-handler/index.ts` (базовый экспорт)
5. `mcp-handler.ts` (реэкспорт для совместимости)

### Фаза 2: Критические утилиты (группа A)
6. `logging/mcp-logger.ts`
7. `file-utils/path-normalizer.ts`
8. `security/approval-checker.ts`
9. `security/git-commit-checker.ts`
10. `injection-utils/retry-handler.ts`
11. `injection-utils/validation.ts`
12. `file-utils/git-utils.ts`
13. `file-utils/probe-finder.ts`
14. `server-utils/server-tester.ts`

### Фаза 3: Базовые MCP обработчики (группа B)
15. `request-handlers/initialize.ts`
16. `request-handlers/list-tools.ts`

### Фаза 4: Ресурсы (группа C)
17. `resources/list-resources.ts`
18. `resources/read-resource.ts`

### Фаза 5: Критические инструменты (группа D - приоритетные)
19. `handlers/read-runtime-logs.ts`
20. `handlers/inject-probes.ts`
21. `handlers/inject-multiple-probes.ts`
22. `handlers/clear-session.ts`
23. `handlers/read-file.ts`

### Фаза 6: Остальные инструменты (группа D - остальные)
24. `handlers/get-debug-status.ts`
25. `handlers/get-problems.ts`
26. `handlers/load-rule.ts`
27. `handlers/clear-logs.ts`
28. `handlers/show-user-instructions.ts`

### Фаза 7: Средние задачи
29. Создать `index.ts` во всех подмодулях
30. Оптимизация экспортов
31. Рефакторинг `handler.ts` (упрощение)
32. Ручное тестирование всех компонентов

### Фаза 8: Опциональные задачи (по необходимости)
33. Обновление тестов
34. Дополнительная документация
35. Оптимизация импортов
36. Производительность

## Преимущества новой структуры

1. **Модульность:** Каждый компонент в отдельном файле
2. **Тестируемость:** Легко тестировать отдельные обработчики
3. **Читаемость:** Меньше кода в каждом файле (~50-200 строк)
4. **Поддерживаемость:** Легче находить и изменять код
5. **Переиспользование:** Утилиты можно использовать в других местах
6. **Масштабируемость:** Легко добавлять новые инструменты

## Оценка сложности

- **Минимальное время (блокирующие + критические):** ~3.5-4.5 часа
- **Полное время (все задачи):** ~5-7 часов
- **Риски:** Низкие (рефакторинг без изменения логики)
- **Тестирование:** Требуется проверка всех инструментов после рефакторинга

### Минимальный набор для работы

Для того чтобы система заработала после рефакторинга, необходимо выполнить:
- ✅ Все блокирующие задачи (5)
- ✅ Критические утилиты (9)
- ✅ Базовые MCP обработчики (2)
- ✅ Ресурсы (2)
- ✅ Критические инструменты (5)

**Итого минимум:** ~3.5-4 часа работы

## Зависимости между модулями

```
mcp-handler.ts (верхний уровень - реэкспорт)
└── mcp-handler/
    ├── index.ts (централизованный экспорт)
    ├── handler.ts (основной класс)
    │   ├── request-handlers/ (initialize, list-tools)
    │   ├── resources/ (list-resources, read-resource)
    │   ├── handlers/ (все инструменты)
    │   │   ├── security/ (approval-checker, git-commit-checker)
    │   │   ├── file-utils/ (path-normalizer, git-utils, probe-finder)
    │   │   ├── server-utils/ (server-tester)
    │   │   ├── injection-utils/ (retry-handler, validation)
    │   │   └── logging/ (mcp-logger)
    │   └── tool-schemas.ts, types.ts
    └── [подмодули с index.ts для группировки]
```

**Импорты из других модулей:**
- `mcp-server.ts` → `./mcp-handler` (должен продолжать работать через реэкспорт)
- `mcp-handler.ts` → `./mcp-handler/tool-schemas` (внутренний импорт)

## Примечания

- **Обратная совместимость:** 
  - `mcp-server.ts` импортирует `RooTraceMCPHandler` из `./mcp-handler` - это должно продолжать работать
  - Файл `mcp-handler.ts` на верхнем уровне будет реэкспортировать из `mcp-handler/`
  
- **Паттерны проекта:**
  - Следовать структуре `code-injector/` с `index.ts` в корне
  - Использовать централизованный экспорт через `index.ts`
  - Подмодули могут иметь свои `index.ts` для группировки экспортов
  
- **Типы:**
  - Типы из `types.ts` можно расширить, но не удалять существующие
  - Новые типы добавлять в `types.ts` или в соответствующие подмодули
  
- **Стиль кода:**
  - TypeScript, async/await, error handling
  - JSDoc комментарии для публичных API
  - Следовать существующим конвенциям именования
