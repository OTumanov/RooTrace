# 🎯 ПОЛНЫЙ ПУТЬ АГЕНТА: ОТ НАЧАЛА ДО КОНЦА

**Версия:** Оптимизированная для ADHD (минимизация переключений контекста, визуальные якоря)

---

## 📊 ВИЗУАЛЬНАЯ КАРТА ПУТИ (ОПТИМИЗИРОВАННАЯ)

```
┌─────────────────────────────────────────────────────────────┐
│ ПОДГОТОВКА (Phase 0.x)                                      │
├─────────────────────────────────────────────────────────────┤
│ Phase 0    → Входной фильтр (RooTrace)                      │
│ Phase 0.1  → TODO List (RooTrace) ⚡ ПЕРВЫЙ вызов инструмента│
│ Phase 0.5  → Network Discovery (RooTrace) ⚡ РАНЬШЕ!         │
│ Phase 0.6  → Smoke Test (RooTrace) ⚡ РАНЬШЕ!                │
│ Phase 0.2  → Разведка (Архитектор) → подозрительные места  │
│             + черновые точки интереса                       │
│ Phase 0.3  → Получение summary (RooTrace)                   │
│ Phase 0.4  → Pre-Flight Check (RooTrace)                    │
└─────────────────────────────────────────────────────────────┘
┌─────────────────────────────────────────────────────────────┐
│ ФОРМУЛИРОВАНИЕ И СТРАТЕГИЯ (Phase 1.x)                      │
├─────────────────────────────────────────────────────────────┤
│ Phase 1    → Формулирование гипотез (RooTrace САМ)         │
│             → использует черновые точки                     │
│ Phase 1.5   → Стратегия инъекции (Архитектор) ⚡ явная передача контекста   │
│             → H1 -> Файл:Линия -> Что логируем              │
│ Phase 1.6   → Получение стратегии (RooTrace)                │
│ Phase 1.1   → Вставка проб (Кодер)                          │
│             → использует стратегию + FINAL_HOST              │
│ Phase 1.2   → Получение summary от кодера (RooTrace)        │
└─────────────────────────────────────────────────────────────┘
┌─────────────────────────────────────────────────────────────┐
│ СБОР ДАННЫХ (Phase 5-6)                                      │
├─────────────────────────────────────────────────────────────┤
│ Phase 5    → WAIT (Пользователь запускает код)              │
│ Phase 6    → Чтение логов (RooTrace САМ)                    │
│             → фильтрация по timestamp                       │
└─────────────────────────────────────────────────────────────┘
┌─────────────────────────────────────────────────────────────┐
│ ИСПРАВЛЕНИЕ (Phase 7.x) - ЦИКЛ                               │
├─────────────────────────────────────────────────────────────┤
│ Phase 7.1  → Анализ логов (Архитектор) → с BLACKLIST         │
│ Phase 7.2  → Получение решения (RooTrace)                    │
│ Phase 7.3  → Исправление (Кодер)                             │
│ Phase 7.4  → Получение summary (RooTrace)                    │
│ Phase 7.5  → Повторный запуск (Пользователь)                │
│ Phase 7.6  → Повтор цикла (если не решено)                   │
└─────────────────────────────────────────────────────────────┘
┌─────────────────────────────────────────────────────────────┐
│ ЗАВЕРШЕНИЕ (Phase 8)                                         │
├─────────────────────────────────────────────────────────────┤
│ Phase 8    → Очистка (RooTrace)                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 🔍 ДЕТАЛЬНЫЙ РАЗБОР КАЖДОЙ ФАЗЫ

### Phase 0: Входной фильтр
**Кто:** RooTrace  
**Что:** Оценивает достаточность данных
- Проверяет наличие описания проблемы, файлов, ошибок
- Если данных недостаточно → использует `get_problems()` или `@problems`
- Задает вопросы с кнопками (макс 3 подряд)

**ADHD-оптимизация:** ✅ Быстрая проверка, без долгих размышлений

---

### Phase 0.1: TODO List
**Кто:** RooTrace  
**Что:** Создает список задач через `update_todo_list` (ОБЯЗАТЕЛЬНО ПЕРВЫМ!)
- Первый вызов инструмента ДОЛЖЕН быть `update_todo_list`
- Запрещено: любые другие действия до этого

**ADHD-оптимизация:** ✅ Визуальный якорь - видно весь путь сразу

---

### Phase 0.5: Network Discovery ⚡ ПЕРЕМЕЩЕНО РАНЬШЕ
**Кто:** RooTrace САМ  
**Что:** Обнаруживает сетевой endpoint
- Читает `.debug_port` (51234 по умолчанию)
- Проверяет Docker файлы (`Dockerfile`, `docker-compose.yml`)
- Запускает `docker ps`
- Если Docker → проверяет bridge (`host.docker.internal` или `172.17.0.1`)
- Определяет `FINAL_HOST` и `ACTUAL_PORT`

**ПОЧЕМУ РАНЬШЕ:** Чтобы Архитектор в Phase 0.2 мог сразу предложить черновые точки с учетом сети, а не гадать.

**ADHD-оптимизация:** ✅ Один раз определили - используем везде

---

### Phase 0.6: Smoke Test
**Кто:** RooTrace САМ  
**Что:** Проверяет работоспособность моста
- Отправляет тестовый лог на сервер
- Проверяет получение через `get_debug_status`
- Валидирует что сервер получает данные
- **НОВОЕ:** Явное поведение при неудачном Smoke Test - ERROR с остановкой и инструкциями пользователю

**ADHD-оптимизация:** ✅ Быстрая проверка, сразу знаем что все работает. Явное поведение при ошибках.

---

### Phase 0.2: Разведка
**Кто:** Архитектор (через `new_task mode="architect")`  
**Что:** Анализирует проблему и находит подозрительные места
- Использует `codebase_search` и `read_file`
- Возвращает: FILE:COORDINATE:FUNCTION:REASON
- **ОБЯЗАТЕЛЬНО:** Также возвращает черновые точки интереса (минимум 3 точки) - теперь обязательны, не опциональны!
- **КРИТИЧНО:** REASON должен содержать цитаты кода/логов (минимум 3-5 строк кода или точная цитата лога)
- НЕ исправляет код, только разведка

**ADHD-оптимизация:** ✅ Архитектор сразу предлагает черновые точки - меньше переключений контекста, ускоряет Phase 1. Обязательность гарантирует наличие точек.

---

### Phase 0.3: Получение summary от архитектора
**Кто:** RooTrace  
**Что:** Получает результаты разведки через `attempt_completion`
- Валидирует формат: FILE:COORDINATE:FUNCTION:REASON
- **КРИТИЧНО:** Проверяет REASON по чек-листу валидации
- Если REASON не проходит валидацию → запрашивает уточнение у Архитектора
- Сохраняет координаты + черновые точки интереса (если есть)

**ADHD-оптимизация:** ✅ Валидация сразу, без отложенных проблем

---

### Phase 0.4: Pre-Flight Check
**Кто:** RooTrace САМ (не делегирует!)  
**Что:** Проверяет окружение перед началом
1. Статус сервера: `get_debug_status`
2. Сеть: уже знаем FINAL_HOST и ACTUAL_PORT из Phase 0.5
3. Мост: уже проверили в Phase 0.6
4. Диагностика: `get_problems()` или `@problems`
5. MCP tools: `list_mcp_tools`, проверка `sequentialthinking` и `memory`

**ADHD-оптимизация:** ✅ Используем уже известные данные (FINAL_HOST, ACTUAL_PORT)

---

### Phase 1: Формулирование гипотез
**Кто:** RooTrace САМ (не делегирует!)  
**Что:** Создает гипотезы H1-H3 (минимум), H1-H5 (рекомендуется)
- На основе summary от архитектора
- **НОВОЕ:** Использует черновые точки интереса от архитектора (если есть) для быстрого формулирования
- Использует `sequentialthinking` если доступно
- Каждая гипотеза = отдельная задача в TODO

**ADHD-оптимизация:** ✅ Используем готовые черновые точки - меньше размышлений

---

### Phase 1.5: Стратегия инъекции проб ⚡ ПЕРЕИМЕНОВАНО
**Кто:** Архитектор (через `new_task mode="architect")`  
**Что:** Находит точки инъекции проб для каждой гипотезы H1-H5
- Получает уже сформулированные гипотезы от оркестратора
- **ВАЖНО:** Получает явный контекст из Phase 0.2-0.3 через message в `new_task` (явная передача контекста, не stateful-процесс)
- Определяет конкретные места в коде (файл:строка:функция) для каждой гипотезы
- Определяет что логировать (переменные, состояния, значения)
- Определяет стратегию [DEBUG-STRATEGIST] для каждой точки (краткое обоснование ЗАЧЕМ логируются данные)
- Определяет стратегию [SRE-SHIELD] для каждой точки
- Возвращает: **H1 -> Файл:Линия -> Что логируем** (ADHD-френдли формат)

**ADHD-оптимизация:** ✅ Явная передача контекста через message - нет переключений, формат легко сканируется, гарантированная полнота данных

---

### Phase 1.6: Получение стратегии от архитектора ⚡ ПЕРЕИМЕНОВАНО
**Кто:** RooTrace  
**Что:** Получает стратегию инъекции проб через `attempt_completion`
- Валидирует формат: H1 -> Файл:Линия -> Что логируем, [DEBUG-STRATEGIST], [SRE-SHIELD]
- Сохраняет стратегию для Phase 1.1
- **КРИТИЧНО:** Summary приходит АВТОМАТИЧЕСКИ, не вызывай `attempt_completion` сам!

**ADHD-оптимизация:** ✅ Формат легко сканируется, сразу видно маппинг гипотеза -> код

---

### Phase 1.1: Вставка проб
**Кто:** Кодер (через `new_task mode="code")`  
**Что:** Вставляет отладочные пробы в код
- Получает стратегию от архитектора (Phase 1.6): H1 -> Файл:Линия -> Что логируем
- **НОВОЕ:** Выбирает метод инъекции согласно таблице правил по языкам (Block Rewrite для чувствительных к форматированию, точечная инъекция для гибких)
- После КАЖДОЙ вставки проверяет диагностику: `get_problems(filePath=...)`
- **НОВОЕ:** Если диагностика показывает ошибки - исправляет немедленно, не продолжает без исправления
- Создает `.patch` файл после успешной проверки
- Использует `FINAL_HOST` и `ACTUAL_PORT` (уже известны из Phase 0.5)

**ADHD-оптимизация:** ✅ Все данные готовы (FINAL_HOST, стратегия) - кодер просто выполняет. Явные правила выбора метода для каждого языка.

---

### Phase 1.2: Получение summary от кодера
**Кто:** RooTrace  
**Что:** Получает результаты вставки проб через `attempt_completion`
- Проверяет что пробы вставлены
- Проверяет результаты линтера
- **НОВОЕ:** Устанавливает `_run_request_timestamp` (раньше, чтобы не потерять логи при раннем запуске)

**ADHD-оптимизация:** ✅ Быстрая валидация, без долгих размышлений. Timestamp установлен раньше - защита от потери логов.

---

### Phase 5: WAIT
**Кто:** Пользователь  
**Что:** Запускает код и воспроизводит баг
- **ВАЖНО:** `_run_request_timestamp` уже установлен в Phase 1.2 (раньше, чтобы не потерять логи)
- Если timestamp еще не установлен → установить сейчас
- Вывод: "Ready. Run the app and trigger the bug. Say 'Logs ready' when done."
- Запрещено: использовать инструменты, показывать таймеры/кнопки

**ADHD-оптимизация:** ✅ Четкая инструкция пользователю, без лишнего. Timestamp установлен раньше - защита от потери логов.

---

### Phase 6: Чтение логов
**Кто:** RooTrace САМ (не делегирует!)  
**Что:** Читает логи из файла `.rootrace/ai_debug_logs.json`
- Использует `read_file` (НЕ `read_runtime_logs` MCP tool)
- **КРИТИЧНО:** Фильтрует логи по timestamp (только после `_run_request_timestamp`)
- **НОВОЕ:** Обработка отсутствующих логов - явное сообщение и запрос повторного запуска
- Анализирует данные для каждой гипотезы H1-H5
- **НОВОЕ:** Использует детерминированные критерии для определения статуса: confirmed/rejected/missing
- Вывод: `DATA: [N] logs`, `H1: [confirmed/rejected/missing]`, `VERDICT: [причина]`

**ADHD-оптимизация:** ✅ Четкий формат вывода, сразу видно статус каждой гипотезы. Детерминированные критерии вместо субъективных решений.

---

### Phase 7.1: Анализ логов
**Кто:** Архитектор (через `new_task mode="architect")`  
**Что:** Анализирует логи и предлагает решение
- Получает BLACKLIST предыдущих попыток из memory или `_debug_history`
- **НОВОЕ:** Валидация перед переходом к Phase 7.2 - BLACKLIST должен быть сформирован и передан
- Предлагает НОВОЕ решение (не из blacklist)
- Формат: PROBLEM:SOLUTION:FILE:LINE:CHANGE:JUSTIFICATION

**ADHD-оптимизация:** ✅ Архитектор получает все данные сразу (логи + blacklist). Валидация гарантирует передачу BLACKLIST.

---

### Phase 7.2: Получение summary от архитектора
**Кто:** RooTrace  
**Что:** Получает анализ и проверяет уникальность решения
- Использует `sequentialthinking` для скрытой валидации (если доступно)
- **НОВОЕ:** Детерминированный алгоритм сравнения решений по ключевым полям (FILE, LINE, CHANGE)
- Сравнивает с предыдущими решениями из memory
- Если решение повторяется → ошибка, запрос нового решения

**ADHD-оптимизация:** ✅ Автоматическая валидация, без ручной проверки. Детерминированный алгоритм сравнения вместо субъективной оценки.

---

### Phase 7.3: Исправление кода
**Кто:** Кодер (через `new_task mode="code")`  
**Что:** Исправляет код согласно решению архитектора
- Читает файл, вносит исправление
- **ОБЯЗАТЕЛЬНО:** Проверяет диагностику после исправления: `get_problems(filePath=...)`
- Если ошибки → исправляет, проверяет снова
- **НОВОЕ:** Алгоритм обработки конфликта проб и исправления (временное удаление → исправление → восстановление)
- Создает `.patch` файл
- НЕ удаляет существующие пробы

**ADHD-оптимизация:** ✅ Четкие инструкции, без двусмысленностей. Алгоритм обработки конфликтов.

---

### Phase 7.4: Получение summary от кодера
**Кто:** RooTrace  
**Что:** Получает результаты исправления через `attempt_completion`
- Проверяет что исправление выполнено
- Сохраняет в memory или `_debug_history`
- Увеличивает `_current_iteration += 1`

**ADHD-оптимизация:** ✅ Автоматическое обновление истории

---

### Phase 7.5: Повторный запуск
**Кто:** Пользователь  
**Что:** Запускает код снова для проверки исправления
- RooTrace запрашивает запуск
- Обновляет `_run_request_timestamp`

**ADHD-оптимизация:** ✅ Четкая инструкция пользователю

---

### Phase 7.6: Повтор цикла
**Кто:** RooTrace  
**Что:** Повторяет Phase 7.1-7.5 если проблема не решена
- **НОВОЕ:** Многоуровневая система критериев "проблема решена" (обязательные + рекомендуемые)
- Цикл продолжается до решения или явного указания остановиться

**ADHD-оптимизация:** ✅ Автоматический цикл, без ручного управления. Детерминированные критерии для определения решения проблемы.

---

### Phase 8: Очистка
**Кто:** RooTrace САМ  
**Что:** Удаляет пробы и временные файлы
- Разрешено ТОЛЬКО если:
  1. Пользователь явно сказал "FIXED"/"problem solved"
  2. Все гипотезы [confirmed] или [rejected] (нет [missing])
  3. Баг исправлен и проверен через логи
- **НОВОЕ:** Поведение при явном запросе очистки - предупреждение и запрос подтверждения
- Предпочтительно: SELECTIVE CLEANUP (удаление проб из конкретных файлов)
- Fallback: `clear_session` (только если все файлы нужно очистить)
- Удаляет `.patch` и `.bak` файлы

**ADHD-оптимизация:** ✅ Строгие условия - нет преждевременной очистки. Уважение выбора пользователя с предупреждением.

---

## 🚨 ОБНАРУЖЕННЫЕ ПРОБЛЕМЫ И РЕКОМЕНДАЦИИ

### 1. ✅ ИСПРАВЛЕНО: Конфликт нумерации Phase 2
**Было:** Phase 2 использовался дважды (Network Discovery и Стратегия)

**Исправлено:**
- Phase 2 (Network Discovery) → **Phase 0.5** (Network Discovery) ✅
- Phase 2.2 (Smoke Test) → **Phase 0.6** (Smoke Test) ✅
- Phase 2 (Стратегия) → **Phase 1.5** (Стратегия инъекции проб) ✅
- Phase 2.1 (Получение стратегии) → **Phase 1.6** (Получение стратегии) ✅

**Почему:** Network Discovery должен быть РАНЬШЕ, чтобы сразу знать FINAL_HOST для всех последующих фаз. Логичная нумерация: 0.x → 1.x → 5+ (нет пропусков).

---

### 2. ✅ ИСПРАВЛЕНО: Порядок фаз для ADHD-оптимизации
**Было (проблемный):**
```
0 → 0.1 → 2 → 2.2 → 0.2 → 0.3 → 0.4 → 1 → 2 → 2.1 → 1.1 → 1.2 → 5 → 6 → 7.x → 8
```

**Стало (оптимизированный):**
```
0 → 0.1 → 0.5 → 0.6 → 0.2 → 0.3 → 0.4 → 1 → 1.5 → 1.6 → 1.1 → 1.2 → 5 → 6 → 7.x → 8
```

**Преимущества:**
- ✅ Логичная последовательность (0.x → 1.x → 5+) - нет пропусков
- ✅ Network Discovery раньше - сразу знаем FINAL_HOST для всех фаз
- ✅ Легче запомнить и отслеживать (ADHD-френдли)
- ✅ Минимизация переключений контекста

---

### 3. ✅ ОПТИМИЗИРОВАНО: Передача контекста между фазами
**Было:** Архитектор как stateless-функция, вызывался дважды, риск "забыть" детали

**Стало:** 
- Архитектор получает явный контекст из Phase 0.2-0.3 через message в `new_task`
- В Phase 1.5 он использует переданный контекст для быстрого поиска точек инъекции
- Формат "H1 -> Файл:Линия -> Что логируем" делает связь явной
- [DEBUG-STRATEGIST] содержит краткое обоснование ЗАЧЕМ логируются данные

**ADHD-оптимизация:** ✅ Визуальный якорь - сразу видно связь гипотеза -> код. Экономия токенов и снижение риска "забыть" детали.

---

### 4. ✅ РЕАЛИЗОВАНО: Формат вывода для ADHD
**Текущий формат (оптимизированный):**
```
H1 -> main.py:42 -> log: user_id, session_state
  [DEBUG-STRATEGIST]: проверяем мутацию стейта перед вызовом API
  [SRE-SHIELD]: функция вызывается редко, overhead минимальный
```

**Преимущества:**
- ✅ Формат "H1 -> Файл:Линия -> Что логируем" - визуальный якорь
- ✅ [DEBUG-STRATEGIST] с обоснованием ЗАЧЕМ - быстро вспомнить цель пробы
- ✅ Легко сканируется - сразу видно маппинг гипотеза -> код

**Рекомендация (опционально):** Можно добавить краткое описание гипотезы:
```
H1: [краткое описание] -> main.py:42 -> log: user_id, session_state
```
Но текущий формат уже достаточно хорош для ADHD - связь гипотеза -> код очевидна.

---

## 📋 ИТОГОВАЯ ОПТИМИЗИРОВАННАЯ СХЕМА

### Подготовка (Phase 0.x)
1. **Phase 0**: Входной фильтр
2. **Phase 0.1**: TODO List
3. **Phase 0.5**: Network Discovery ⚡ РАНЬШЕ
4. **Phase 0.6**: Smoke Test
5. **Phase 0.2**: Разведка (Архитектор) → подозрительные места + черновые точки
6. **Phase 0.3**: Получение summary
7. **Phase 0.4**: Pre-Flight Check

### Формулирование и стратегия (Phase 1.x)
8. **Phase 1**: Формулирование гипотез (RooTrace САМ) → использует черновые точки
9. **Phase 1.5**: Стратегия инъекции (Архитектор) → H1 -> Файл:Линия -> Что логируем
10. **Phase 1.6**: Получение стратегии
11. **Phase 1.1**: Вставка проб (Кодер) → использует стратегию + FINAL_HOST
12. **Phase 1.2**: Получение summary от кодера

### Сбор данных (Phase 5-6)
13. **Phase 5**: WAIT (Пользователь запускает код)
14. **Phase 6**: Чтение логов (RooTrace САМ) → фильтрация по timestamp

### Исправление (Phase 7.x)
15. **Phase 7.1**: Анализ логов (Архитектор) → с BLACKLIST
16. **Phase 7.2**: Получение решения
17. **Phase 7.3**: Исправление (Кодер)
18. **Phase 7.4**: Получение summary
19. **Phase 7.5**: Повторный запуск (Пользователь)
20. **Phase 7.6**: Повтор цикла (если не решено)

### Завершение (Phase 8)
21. **Phase 8**: Очистка (RooTrace)

---

## 🎯 КЛЮЧЕВЫЕ ADHD-ОПТИМИЗАЦИИ

1. **Network Discovery раньше** → сразу знаем FINAL_HOST для всех фаз
2. **Черновые точки интереса** → Архитектор сразу предлагает места для проб
3. **Формат "H1 -> Файл:Линия -> Что логируем"** → визуальный якорь
4. **[DEBUG-STRATEGIST] с обоснованием ЗАЧЕМ** → быстро вспомнить цель пробы
5. **Логичная нумерация** → 0.x → 1.x → 5+ (нет пропусков)
6. **Явная передача контекста** → контекст передается через message в new_task между Phase 0.2 и 1.5

---

**Последнее обновление:** 2026-01-24
