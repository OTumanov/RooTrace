# 💡 ВАРИАНТЫ РЕШЕНИЯ ОБНАРУЖЕННЫХ ПРОБЛЕМ

**Дата:** 2026-01-24  
**Основано на:** `AGENT_SEQUENCE_ANALYSIS.md`

---

## 🎯 ПРОБЛЕМА 1: Противоречие "stateful-процесс" vs изолированный контекст

**Где:** Phase 1.5, документация говорит о "stateful-процессе" для архитектора  
**Проблема:** `new_task` создает изолированный контекст, архитектор не "помнит" Phase 0.2

### Вариант 1.1: Уточнить документацию (РЕКОМЕНДУЕТСЯ)
**Сложность:** Низкая  
**Время:** 1-2 часа

**Решение:**
- Заменить термин "stateful-процесс" на "явная передача контекста через message"
- Обновить `COMPLETE_WORKFLOW.md`: убрать упоминание stateful, добавить явное описание передачи контекста
- Обновить `roo-05-delegate-analysis.md`: добавить секцию "ЯВНАЯ ПЕРЕДАЧА КОНТЕКСТА" с примером

**Изменения:**
```markdown
# В COMPLETE_WORKFLOW.md, Phase 1.5:
**Было:** Архитектор уже в контексте после Phase 0.2 (разведка) - может быстро найти точки
**Стало:** Архитектор получает явный контекст из Phase 0.2-0.3 через message в new_task - может быстро найти точки
```

**Плюсы:**
- ✅ Быстро реализуется
- ✅ Устраняет противоречие
- ✅ Не требует изменений в коде

**Минусы:**
- ❌ Не улучшает функциональность, только документацию

---

### Вариант 1.2: Добавить явный шаблон передачи контекста
**Сложность:** Средняя  
**Время:** 2-4 часа

**Решение:**
- Создать обязательный шаблон в `roo-05-delegate-analysis.md`:
```markdown
**ОБЯЗАТЕЛЬНЫЙ ШАБЛОН ПЕРЕДАЧИ КОНТЕКСТА:**

КОНТЕКСТ ИЗ PHASE 0.2-0.3 (ОБЯЗАТЕЛЬНО ВКЛЮЧИТЬ):
${architectSummary.coordinates.map(c => 
  `ФАЙЛ:${c.file}
КООРДИНАТА:${c.line}
ФУНКЦИЯ:${c.function}
ПРИЧИНА:${c.reason}`
).join('\n---\n')}

ЧЕРНОВЫЕ ТОЧКИ ИНТЕРЕСА (если есть):
${roughPoints.map(p => `${p.file}:${p.line}:${p.function} - ${p.description}`).join('\n')}
```

**Плюсы:**
- ✅ Гарантирует передачу всего контекста
- ✅ Снижает риск потери данных
- ✅ Улучшает качество работы архитектора

**Минусы:**
- ❌ Требует обновления всех мест, где вызывается new_task для архитектора

---

### Вариант 1.3: Валидация наличия контекста (КОМБИНИРОВАННЫЙ)
**Сложность:** Средняя  
**Время:** 3-5 часов

**Решение:**
- Комбинация вариантов 1.1 и 1.2
- Добавить валидацию в `roo-05-delegate-analysis.md`: перед вызовом `new_task` проверить наличие всех обязательных полей контекста
- Если контекст неполный → ERROR и запрос повторной передачи

**Плюсы:**
- ✅ Устраняет противоречие
- ✅ Гарантирует передачу контекста
- ✅ Защита от ошибок

**Минусы:**
- ❌ Требует больше времени на реализацию

**РЕКОМЕНДАЦИЯ:** Вариант 1.3 (комбинированный) - максимальная надежность

---

## 🎯 ПРОБЛЕМА 2: Отсутствие четких критериев достаточности данных

**Где:** Phase 0 (Входной фильтр)  
**Проблема:** Нет явного алгоритма определения достаточности данных

### Вариант 2.1: Чек-лист достаточности (РЕКОМЕНДУЕТСЯ)
**Сложность:** Низкая  
**Время:** 1-2 часа

**Решение:**
- Добавить в `roo-00-input-filter.md` явный чек-лист:
```markdown
**ЧЕК-ЛИСТ ДОСТАТОЧНОСТИ ДАННЫХ:**

ОБЯЗАТЕЛЬНО:
- ✅ Описание проблемы (минимум 1 предложение)

ХОТЯ БЫ ОДИН ИЗ:
- ✅ Упоминание файла/функции/класса
- ✅ Ошибка из диагностики (get_problems или @problems)
- ✅ Лог ошибки/предупреждения
- ✅ Стек трейс или traceback

ЕСЛИ НЕДОСТАТОЧНО:
1. Используй get_problems() или @problems для получения диагностики
2. Задай вопрос пользователю (макс 3 подряд)
3. Если после 3 вопросов данных все еще недостаточно → ERROR и запрос дополнительной информации
```

**Плюсы:**
- ✅ Быстро реализуется
- ✅ Детерминированный алгоритм
- ✅ Защита от бесконечных циклов

**Минусы:**
- ❌ Может быть слишком строгим для некоторых случаев

---

### Вариант 2.2: Взвешенная система баллов
**Сложность:** Средняя  
**Время:** 3-4 часа

**Решение:**
- Создать систему баллов для оценки достаточности:
```markdown
**СИСТЕМА ОЦЕНКИ ДОСТАТОЧНОСТИ:**

ОПИСАНИЕ ПРОБЛЕМЫ:
- Краткое (1-2 предложения): +1 балл
- Подробное (3+ предложений): +2 балла

ФАЙЛЫ/ФУНКЦИИ:
- Упоминание файла: +1 балл
- Упоминание функции/класса: +2 балла
- Указание конкретной строки: +3 балла

ОШИБКИ/ЛОГИ:
- Ошибка из диагностики: +2 балла
- Лог ошибки: +2 балла
- Стек трейс: +3 балла

МИНИМУМ ДЛЯ ПРОДОЛЖЕНИЯ: 3 балла
РЕКОМЕНДУЕТСЯ: 5+ баллов
```

**Плюсы:**
- ✅ Гибкая система
- ✅ Учитывает качество данных
- ✅ Позволяет продолжить даже с минимальными данными

**Минусы:**
- ❌ Сложнее для понимания
- ❌ Требует больше времени на реализацию

---

### Вариант 2.3: Автоматический сбор данных
**Сложность:** Высокая  
**Время:** 5-8 часов

**Решение:**
- Автоматически собирать данные из всех доступных источников:
  1. `get_problems()` - диагностика VS Code
  2. `codebase_search` - поиск связанного кода по описанию проблемы
  3. `read_file` - чтение упомянутых файлов
  4. Анализ истории изменений (git)

**Плюсы:**
- ✅ Максимальная автоматизация
- ✅ Минимум вопросов пользователю
- ✅ Высокое качество данных

**Минусы:**
- ❌ Сложная реализация
- ❌ Может быть избыточным для простых случаев
- ❌ Риск "перегрузки" контекста

**РЕКОМЕНДАЦИЯ:** Вариант 2.1 (чек-лист) - простой и эффективный

---

## 🎯 ПРОБЛЕМА 3: Отсутствие механизма принудительного соблюдения правил

**Где:** Phase 0.1 (TODO list первым), Phase 7.1 (BLACKLIST)  
**Проблема:** Нет механизма принудительного соблюдения правил

### Вариант 3.1: Валидация перед переходом к следующей фазе
**Сложность:** Средняя  
**Время:** 4-6 часов

**Решение:**
- Создать модуль валидации `roo-00-phase-validator.md`:
```markdown
**ВАЛИДАЦИЯ ПЕРЕД ПЕРЕХОДОМ К СЛЕДУЮЩЕЙ ФАЗЕ:**

PHASE 0.1 → 0.2:
- ✅ TODO list создан через update_todo_list
- ✅ TODO list содержит минимум 3 задачи
- ❌ ЗАПРЕЩЕНО: Переход к Phase 0.2 без TODO list

PHASE 7.1 → 7.2:
- ✅ BLACKLIST сформирован (даже если пустой)
- ✅ BLACKLIST передан в message для архитектора
- ❌ ЗАПРЕЩЕНО: Переход к Phase 7.2 без передачи BLACKLIST
```

- Добавить проверку в начале каждой фазы

**Плюсы:**
- ✅ Гарантирует соблюдение правил
- ✅ Явные ошибки при нарушении
- ✅ Легко расширяется для новых правил

**Минусы:**
- ❌ Требует обновления всех фаз
- ❌ Может замедлить работу

---

### Вариант 3.2: Система штрафов с блокировкой
**Сложность:** Средняя  
**Время:** 3-5 часов

**Решение:**
- Расширить существующую систему штрафов:
```markdown
**КРИТИЧЕСКИЕ ШТРАФЫ (БЛОКИРУЮТ ПЕРЕХОД):**

- Пропуск TODO list в Phase 0.1: +50 баллов (CRITICAL FAILURE, БЛОКИРУЕТ Phase 0.2)
- Пропуск BLACKLIST в Phase 7.1: +50 баллов (CRITICAL FAILURE, БЛОКИРУЕТ Phase 7.2)

**ПРАВИЛО:** Если накоплено 50+ баллов критических штрафов → ERROR и остановка до исправления
```

**Плюсы:**
- ✅ Использует существующую систему
- ✅ Явная блокировка при нарушении
- ✅ Сохраняет совместимость

**Минусы:**
- ❌ Зависит от системы штрафов
- ❌ Может быть обойдена, если штрафы не применяются строго

---

### Вариант 3.3: Pre-flight валидация в начале каждой фазы
**Сложность:** Высокая  
**Время:** 6-10 часов

**Решение:**
- Создать универсальный механизм валидации:
```typescript
// Псевдокод
function validatePhaseTransition(currentPhase, nextPhase) {
  const requirements = getPhaseRequirements(nextPhase);
  const currentState = getCurrentState();
  
  for (const requirement of requirements) {
    if (!checkRequirement(requirement, currentState)) {
      throw new ValidationError(
        `Cannot proceed to ${nextPhase}: ${requirement.message}`
      );
    }
  }
}
```

**Плюсы:**
- ✅ Универсальный механизм
- ✅ Легко расширяется
- ✅ Можно использовать в коде (если будет реализация)

**Минусы:**
- ❌ Требует реализации в коде (если нужна автоматизация)
- ❌ Сложнее для промптов

**РЕКОМЕНДАЦИЯ:** Вариант 3.1 (валидация перед переходом) - баланс простоты и эффективности

---

## 🎯 ПРОБЛЕМА 4: Отсутствие четких правил выбора метода инъекции

**Где:** Phase 1.1 (Вставка проб)  
**Проблема:** Нет явного правила для языков кроме Python

### Вариант 4.1: Таблица правил по языкам (РЕКОМЕНДУЕТСЯ)
**Сложность:** Низкая  
**Время:** 1-2 часа

**Решение:**
- Добавить в `code-01-probe-insertion.md` явную таблицу:
```markdown
**ПРАВИЛА ВЫБОРА МЕТОДА ИНЪЕКЦИИ:**

| Язык | Метод | Инструмент | Причина |
|------|-------|------------|---------|
| Python | Block Rewrite | `apply_diff` | Python чувствителен к отступам, Block Rewrite безопаснее |
| Go | Block Rewrite | `apply_diff` | Go требует точного форматирования |
| JavaScript/TypeScript | Точечная инъекция | `inject_probes` | JS/TS более гибкие к форматированию |
| Java | Block Rewrite | `apply_diff` | Java требует точного форматирования |
| Rust | Block Rewrite | `apply_diff` | Rust требует точного форматирования |
| C++ | Block Rewrite | `apply_diff` | C++ требует точного форматирования |
| PHP | Точечная инъекция | `inject_probes` | PHP более гибкий к форматированию |
| Ruby | Точечная инъекция | `inject_probes` | Ruby более гибкий к форматированию |
| C# | Block Rewrite | `apply_diff` | C# требует точного форматирования |
| Swift | Block Rewrite | `apply_diff` | Swift требует точного форматирования |
| Kotlin | Block Rewrite | `apply_diff` | Kotlin требует точного форматирования |

**ОБЩЕЕ ПРАВИЛО:**
- Если язык чувствителен к отступам/форматированию → Block Rewrite (`apply_diff`)
- Если язык гибкий к форматированию → Точечная инъекция (`inject_probes`)
```

**Плюсы:**
- ✅ Быстро реализуется
- ✅ Явные правила для каждого языка
- ✅ Легко понять и следовать

**Минусы:**
- ❌ Требует обновления при добавлении новых языков

---

### Вариант 4.2: Автоматическое определение метода
**Сложность:** Высокая  
**Время:** 8-12 часов

**Решение:**
- Создать функцию определения метода на основе анализа файла:
```typescript
// Псевдокод
function determineInjectionMethod(filePath: string): 'block_rewrite' | 'point_injection' {
  const language = detectLanguage(filePath);
  const fileContent = readFile(filePath);
  
  // Анализ чувствительности к форматированию
  const indentSensitive = analyzeIndentationSensitivity(language, fileContent);
  
  return indentSensitive ? 'block_rewrite' : 'point_injection';
}
```

**Плюсы:**
- ✅ Автоматическое определение
- ✅ Адаптируется к особенностям файла
- ✅ Не требует ручного выбора

**Минусы:**
- ❌ Сложная реализация
- ❌ Требует анализа кода
- ❌ Может ошибаться в edge cases

---

### Вариант 4.3: Конфигурационный файл
**Сложность:** Средняя  
**Время:** 3-4 часа

**Решение:**
- Создать файл `.roo/injection-rules.json`:
```json
{
  "languages": {
    "python": {
      "method": "block_rewrite",
      "tool": "apply_diff",
      "reason": "Python чувствителен к отступам"
    },
    "javascript": {
      "method": "point_injection",
      "tool": "inject_probes",
      "reason": "JavaScript гибкий к форматированию"
    }
  }
}
```

**Плюсы:**
- ✅ Легко настраивается
- ✅ Можно переопределить для конкретного проекта
- ✅ Не требует изменений в коде

**Минусы:**
- ❌ Требует чтения файла конфигурации
- ❌ Дополнительная сложность

**РЕКОМЕНДАЦИЯ:** Вариант 4.1 (таблица правил) - простой и эффективный

---

## 🎯 ПРОБЛЕМА 5: Отсутствие четких критериев для confirmed/rejected/missing

**Где:** Phase 6 (Чтение логов)  
**Проблема:** Субъективное определение статуса гипотез

### Вариант 5.1: Детальные критерии для каждого статуса (РЕКОМЕНДУЕТСЯ)
**Сложность:** Низкая  
**Время:** 2-3 часа

**Решение:**
- Добавить в `roo-09-read-logs.md` явные критерии:
```markdown
**КРИТЕРИИ ОПРЕДЕЛЕНИЯ СТАТУСА ГИПОТЕЗ:**

**CONFIRMED (подтверждена):**
- ✅ Логи содержат данные, которые ПОДТВЕРЖДАЮТ гипотезу
- ✅ Значения переменных/состояний соответствуют ожиданиям из гипотезы
- ✅ Нет данных, которые ПРОТИВОРЕЧАТ гипотезе
- **Пример:** H1: "Geometry равен None" → логи показывают `geometry: None`

**REJECTED (опровергнута):**
- ✅ Логи содержат данные, которые ОПРОВЕРГАЮТ гипотезу
- ✅ Значения переменных/состояний НЕ соответствуют ожиданиям из гипотезы
- ✅ Есть явные доказательства, что гипотеза неверна
- **Пример:** H1: "Geometry равен None" → логи показывают `geometry: <Mesh object>`

**MISSING (недостаточно данных):**
- ✅ Логи НЕ содержат данных для проверки гипотезы
- ✅ Проба не сработала (не было вызова функции/достижения точки)
- ✅ Данные есть, но не релевантны для гипотезы
- **Пример:** H1: "Geometry равен None" → проба не сработала (функция не вызвана)

**АЛГОРИТМ ОПРЕДЕЛЕНИЯ:**
1. Найди все логи, связанные с гипотезой (по probe_id или координатам)
2. Извлеки значения переменных/состояний из логов
3. Сравни с ожиданиями из гипотезы:
   - Если соответствует → CONFIRMED
   - Если противоречит → REJECTED
   - Если данных нет → MISSING
```

**Плюсы:**
- ✅ Быстро реализуется
- ✅ Детерминированный алгоритм
- ✅ Легко понять и следовать

**Минусы:**
- ❌ Может быть слишком упрощенным для сложных случаев

---

### Вариант 5.2: Взвешенная система оценки
**Сложность:** Средняя  
**Время:** 4-6 часов

**Решение:**
- Создать систему баллов для оценки статуса:
```markdown
**СИСТЕМА ОЦЕНКИ СТАТУСА:**

ЗА КАЖДЫЙ ЛОГ, ПОДТВЕРЖДАЮЩИЙ ГИПОТЕЗУ: +2 балла
ЗА КАЖДЫЙ ЛОГ, ОПРОВЕРГАЮЩИЙ ГИПОТЕЗУ: -2 балла
ЗА ОТСУТСТВИЕ ЛОГОВ: 0 баллов

СТАТУС:
- CONFIRMED: сумма баллов >= 2
- REJECTED: сумма баллов <= -2
- MISSING: сумма баллов = 0 или |сумма| < 2
```

**Плюсы:**
- ✅ Учитывает множественные логи
- ✅ Более гибкая система
- ✅ Учитывает силу доказательств

**Минусы:**
- ❌ Сложнее для понимания
- ❌ Требует больше вычислений

---

### Вариант 5.3: Использование AI для анализа
**Сложность:** Высокая  
**Время:** 8-12 часов

**Решение:**
- Использовать `sequentialthinking` или другой AI инструмент для анализа:
```markdown
**ИСПОЛЬЗОВАНИЕ AI ДЛЯ АНАЛИЗА:**

1. Собери все логи для гипотезы
2. Используй sequentialthinking для анализа:
   "Проанализируй логи и определи, подтверждают ли они гипотезу H1: [описание]"
3. Извлеки статус из результата анализа
```

**Плюсы:**
- ✅ Более точный анализ
- ✅ Учитывает контекст
- ✅ Может обрабатывать сложные случаи

**Минусы:**
- ❌ Требует доступности AI инструментов
- ❌ Медленнее
- ❌ Может быть непредсказуемым

**РЕКОМЕНДАЦИЯ:** Вариант 5.1 (детальные критерии) - простой и эффективный, можно дополнить вариантом 5.2 для сложных случаев

---

## 🎯 ПРОБЛЕМА 6: Отсутствие четких критериев "проблема решена"

**Где:** Phase 7.6 (Повтор цикла)  
**Проблема:** Нет явных критериев для определения решения проблемы

### Вариант 6.1: Многоуровневая система критериев (РЕКОМЕНДУЕТСЯ)
**Сложность:** Средняя  
**Время:** 3-4 часа

**Решение:**
- Добавить в `roo-10-cycle-manage.md` явные критерии:
```markdown
**КРИТЕРИИ ОПРЕДЕЛЕНИЯ "ПРОБЛЕМА РЕШЕНА":**

**ОБЯЗАТЕЛЬНО (все должны быть выполнены):**
1. ✅ Пользователь явно сказал "FIXED"/"problem solved"/"решено"
2. ✅ Логи показывают, что проблема больше не воспроизводится:
   - Нет ошибок, которые были в исходной проблеме
   - Значения переменных/состояний корректны
   - Нет аномалий в поведении
3. ✅ Все гипотезы имеют статус [confirmed] или [rejected] (нет [missing])

**РЕКОМЕНДУЕТСЯ (для повышения уверенности):**
4. ✅ Исправление проверено минимум 2 раза (2 итерации цикла)
5. ✅ Нет новых ошибок после исправления
6. ✅ Производительность в норме (если проблема была связана с производительностью)

**АЛГОРИТМ ПРОВЕРКИ:**
1. Проверь обязательные критерии (1-3)
2. Если все обязательные выполнены → проблема решена
3. Если хотя бы один обязательный не выполнен → продолжить цикл
4. Рекомендуемые критерии используются для повышения уверенности, но не блокируют завершение
```

**Плюсы:**
- ✅ Детерминированный алгоритм
- ✅ Учитывает мнение пользователя
- ✅ Проверяет логи и гипотезы

**Минусы:**
- ❌ Может быть слишком строгим (требует все обязательные)

---

### Вариант 6.2: Взвешенная система баллов
**Сложность:** Средняя  
**Время:** 4-5 часов

**Решение:**
- Создать систему баллов:
```markdown
**СИСТЕМА ОЦЕНКИ РЕШЕНИЯ ПРОБЛЕМЫ:**

ПОДТВЕРЖДЕНИЕ ПОЛЬЗОВАТЕЛЯ:
- Явное "FIXED"/"решено": +10 баллов
- Неявное (нет жалоб): +5 баллов

ЛОГИ:
- Проблема не воспроизводится: +10 баллов
- Нет новых ошибок: +5 баллов
- Производительность в норме: +5 баллов

ГИПОТЕЗЫ:
- Все [confirmed] или [rejected]: +10 баллов
- Есть [missing]: -5 баллов

ИТЕРАЦИИ:
- Проверено 2+ раза: +5 баллов
- Проверено 1 раз: 0 баллов

МИНИМУМ ДЛЯ ЗАВЕРШЕНИЯ: 25 баллов
РЕКОМЕНДУЕТСЯ: 30+ баллов
```

**Плюсы:**
- ✅ Гибкая система
- ✅ Учитывает множественные факторы
- ✅ Позволяет завершить даже при неполных данных

**Минусы:**
- ❌ Сложнее для понимания
- ❌ Может завершить раньше времени

---

### Вариант 6.3: Явный запрос подтверждения у пользователя
**Сложность:** Низкая  
**Время:** 1-2 часа

**Решение:**
- После каждой итерации Phase 7.6 явно спрашивать пользователя:
```markdown
**ЗАПРОС ПОДТВЕРЖДЕНИЯ:**

После анализа логов и исправления, явно спроси пользователя:
"Проблема решена? (да/нет)"

Если "да" → переход к Phase 8
Если "нет" → продолжение цикла
```

**Плюсы:**
- ✅ Простое решение
- ✅ Пользователь всегда в курсе
- ✅ Быстро реализуется

**Минусы:**
- ❌ Требует взаимодействия с пользователем
- ❌ Может быть раздражающим при множественных итерациях

**РЕКОМЕНДАЦИЯ:** Вариант 6.1 (многоуровневая система) - баланс автоматизации и контроля

---

## 🎯 ДОПОЛНИТЕЛЬНЫЕ ПРОБЛЕМЫ

### Проблема 7: Отсутствие четкого поведения при неудачном Smoke Test

**Где:** Phase 0.6  
**Проблема:** Нет описания поведения при неудачном Smoke Test

### Вариант 7.1: Явная остановка с ошибкой (РЕКОМЕНДУЕТСЯ)
**Сложность:** Низкая  
**Время:** 1 час

**Решение:**
- Добавить в `roo-07-smoke-test.md`:
```markdown
**ПОВЕДЕНИЕ ПРИ НЕУДАЧНОМ SMOKE TEST:**

ЕСЛИ Smoke Test провалился:
1. ❌ ERROR: "RooTrace server не отвечает. Проверьте, что сервер запущен."
2. ❌ Остановка выполнения
3. ❌ Запрет перехода к Phase 0.2
4. ✅ Инструкции пользователю:
   - Проверьте, что RooTrace server запущен
   - Проверьте FINAL_HOST и ACTUAL_PORT
   - Проверьте сетевые настройки (Docker bridge, firewall)
```

**Плюсы:**
- ✅ Быстро реализуется
- ✅ Явная ошибка
- ✅ Полезные инструкции

**Минусы:**
- ❌ Останавливает выполнение (но это правильно)

---

### Проблема 8: Отсутствие четкого поведения при отсутствии логов

**Где:** Phase 6  
**Проблема:** Нет описания поведения при отсутствии логов

### Вариант 8.1: Явное сообщение и запрос повторного запуска
**Сложность:** Низкая  
**Время:** 1 час

**Решение:**
- Добавить в `roo-09-read-logs.md`:
```markdown
**ПОВЕДЕНИЕ ПРИ ОТСУТСТВИИ ЛОГОВ:**

ЕСЛИ файл .rootrace/ai_debug_logs.json не существует или пуст:
1. ⚠️ WARNING: "Логи не найдены. Возможно, код не был запущен или пробы не сработали."
2. ✅ Проверь:
   - Был ли код запущен после Phase 5?
   - Сработали ли пробы (проверь синтаксис)?
   - Правильный ли FINAL_HOST и ACTUAL_PORT?
3. ✅ Запроси пользователя запустить код снова
4. ❌ НЕ переходи к Phase 7.1 без логов
```

**Плюсы:**
- ✅ Быстро реализуется
- ✅ Полезные инструкции
- ✅ Защита от продолжения без данных

---

### Проблема 9: Отсутствие описания поведения при конфликте проб и исправления

**Где:** Phase 7.3  
**Проблема:** Нет описания поведения при конфликте проб и исправления

### Вариант 9.1: Временное удаление и восстановление проб
**Сложность:** Средняя  
**Время:** 2-3 часа

**Решение:**
- Добавить в `code-02-code-fix.md`:
```markdown
**ПОВЕДЕНИЕ ПРИ КОНФЛИКТЕ ПРОБ И ИСПРАВЛЕНИЯ:**

ЕСЛИ проба находится на строке, которую нужно изменить:
1. ✅ Временно удали пробу (сохрани ее код)
2. ✅ Внеси исправление
3. ✅ Вставь пробу обратно (или новую, если старая не подходит)
4. ✅ Проверь диагностику после всех изменений

ВАЖНО: Пробы должны остаться в файле после исправления (если не указано иное)
```

**Плюсы:**
- ✅ Сохраняет пробы
- ✅ Позволяет исправлять код
- ✅ Четкий алгоритм

---

### Проблема 10: Отсутствие описания поведения при явном запросе очистки

**Где:** Phase 8  
**Проблема:** Нет описания поведения при явном запросе очистки от пользователя

### Вариант 10.1: Предупреждение и выполнение запроса
**Сложность:** Низкая  
**Время:** 1 час

**Решение:**
- Добавить в `roo-11-cleanup.md`:
```markdown
**ПОВЕДЕНИЕ ПРИ ЯВНОМ ЗАПРОСЕ ОЧИСТКИ:**

ЕСЛИ пользователь явно просит очистку до выполнения условий:
1. ⚠️ WARNING: "Очистка запрошена до завершения отладки. Это может привести к потере данных."
2. ✅ Спроси подтверждение: "Вы уверены? (да/нет)"
3. ✅ Если "да" → выполнить очистку
4. ✅ Если "нет" → отменить запрос
```

**Плюсы:**
- ✅ Быстро реализуется
- ✅ Защита от случайной очистки
- ✅ Уважает выбор пользователя

---

## 📊 ИТОГОВАЯ ТАБЛИЦА РЕКОМЕНДАЦИЙ

| Проблема | Рекомендуемое решение | Сложность | Время | Приоритет |
|----------|----------------------|-----------|------|-----------|
| 1. Stateful vs изолированный контекст | Вариант 1.3 (комбинированный) | Средняя | 3-5ч | Высокий |
| 2. Критерии достаточности данных | Вариант 2.1 (чек-лист) | Низкая | 1-2ч | Высокий |
| 3. Принудительное соблюдение правил | Вариант 3.1 (валидация) | Средняя | 4-6ч | Высокий |
| 4. Правила выбора метода инъекции | Вариант 4.1 (таблица) | Низкая | 1-2ч | Средний |
| 5. Критерии confirmed/rejected/missing | Вариант 5.1 (детальные критерии) | Низкая | 2-3ч | Высокий |
| 6. Критерии "проблема решена" | Вариант 6.1 (многоуровневая система) | Средняя | 3-4ч | Высокий |
| 7. Неудачный Smoke Test | Вариант 7.1 (явная остановка) | Низкая | 1ч | Средний |
| 8. Отсутствие логов | Вариант 8.1 (сообщение и запрос) | Низкая | 1ч | Средний |
| 9. Конфликт проб и исправления | Вариант 9.1 (временное удаление) | Средняя | 2-3ч | Низкий |
| 10. Явный запрос очистки | Вариант 10.1 (предупреждение) | Низкая | 1ч | Низкий |

**ОБЩЕЕ ВРЕМЯ НА РЕАЛИЗАЦИЮ:** 18-28 часов

**ПРИОРИТЕТ РЕАЛИЗАЦИИ:**
1. **Высокий приоритет (критические):** Проблемы 1, 2, 3, 5, 6
2. **Средний приоритет (важные):** Проблемы 4, 7, 8
3. **Низкий приоритет (желательные):** Проблемы 9, 10

---

**Последнее обновление:** 2026-01-24
