/**
 * Константы для RooTrace
 */

/**
 * Константы для файлового watcher'а
 */
export const WATCHER_CONFIG = {
  /** Интервал проверки изменений файла в миллисекундах */
  CHECK_INTERVAL_MS: 200,
  /** Задержка debounce для обработки изменений в миллисекундах */
  DEBOUNCE_DELAY_MS: 100,
  /** Максимальное время ожидания для debounce в миллисекундах */
  MAX_DEBOUNCE_DELAY_MS: 500
} as const;

/**
 * Константы для хранилища логов
 */
export const STORAGE_CONFIG = {
  /** Максимальное количество логов по умолчанию */
  DEFAULT_MAX_LOGS: 1000,
  /** Минимальное количество логов */
  MIN_LOGS: 100,
  /** Максимальное количество логов */
  MAX_LOGS: 10000
} as const;

/**
 * Константы для rate limiting
 */
export const RATE_LIMIT_CONFIG = {
  /** Максимальное количество запросов по умолчанию */
  DEFAULT_MAX_REQUESTS: 100,
  /** Минимальное количество запросов */
  MIN_REQUESTS: 10,
  /** Максимальное количество запросов */
  MAX_REQUESTS: 1000,
  /** Окно времени по умолчанию в миллисекундах (1 минута) */
  DEFAULT_WINDOW_MS: 60000,
  /** Минимальное окно времени в миллисекундах */
  MIN_WINDOW_MS: 1000,
  /** Максимальное окно времени в миллисекундах (5 минут) */
  MAX_WINDOW_MS: 300000
} as const;

/**
 * Константы для проверки синтаксиса
 */
export const SYNTAX_CHECK_CONFIG = {
  /** Таймаут проверки синтаксиса по умолчанию в миллисекундах */
  DEFAULT_TIMEOUT_MS: 10000,
  /** Минимальный таймаут в миллисекундах */
  MIN_TIMEOUT_MS: 1000,
  /** Максимальный таймаут в миллисекундах */
  MAX_TIMEOUT_MS: 60000
} as const;

/**
 * Константы для HTTP сервера
 */
export const SERVER_CONFIG = {
  /** Порт по умолчанию */
  DEFAULT_PORT: 51234,
  /** Минимальный порт */
  MIN_PORT: 1024,
  /** Максимальный порт */
  MAX_PORT: 65535
} as const;

/**
 * Константы для метрик производительности
 */
export const METRICS_CONFIG = {
  /** Интервал обновления метрик в миллисекундах */
  UPDATE_INTERVAL_MS: 5000,
  /** Размер окна для расчета среднего времени ответа */
  RESPONSE_TIME_WINDOW: 100
} as const;

/**
 * Константы для retry механизмов
 */
export const RETRY_CONFIG = {
  /** Количество попыток по умолчанию */
  DEFAULT_MAX_ATTEMPTS: 3,
  /** Базовая задержка между попытками в миллисекундах */
  BASE_DELAY_MS: 100,
  /** Максимальная задержка между попытками в миллисекундах */
  MAX_DELAY_MS: 5000
} as const;
