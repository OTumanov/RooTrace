# 🛠️ FALLBACK-ПРОТОКОЛ

**🚨 КРИТИЧЕСКИ ВАЖНО:** Этот протокол применяется при ошибках Block Rewrite или линтера.

**ВЫПОЛНЯЕТСЯ:** Когда Block Rewrite привел к ошибке (синтаксис, конфликт или сбой) ИЛИ линтер выдал ошибку после вставки.

**🚨 ВАЖНО ДЛЯ CODE MODE:**
- ✅ Создай TODO список в НАЧАЛЕ работы на основе списка задач от оркестратора
- ✅ Обновляй TODO список по мере выполнения вставок - отмечай выполненные задачи
- ❌ НЕ планируй работу - создай TODO и сразу приступай к выполнению!

---

## 🎯 НАЗНАЧЕНИЕ

Этот модуль определяет обязательный fallback-протокол при ошибках вставки проб или исправления кода. Цель - обеспечить систематический подход к исправлению ошибок без потери данных и без создания дополнительных проблем.

---

## 📋 FALLBACK-ПРОТОКОЛ (КРИТИЧЕСКОЕ ПРАВИЛО)

**УСЛОВИЕ АКТИВАЦИИ:** Если Block Rewrite привел к ошибке (синтаксис, конфликт или сбой) ИЛИ линтер выдал ошибку после вставки, ты ОБЯЗАН немедленно следовать этому протоколу.

---

### 1. STOP (ОСТАНОВКА)

**ОБЯЗАТЕЛЬНО:**

- НЕМЕДЛЕННО остановись
- НЕ продолжай вставку следующих проб
- НЕ переходи к следующим действиям
- НЕ делай еще одну попытку без исправления текущей ошибки

**ЗАПРЕЩЕНО:**
- Продолжать работу при наличии ошибок
- Игнорировать ошибки линтера
- Делать множественные попытки подряд без исправления

---

### 2. ROLLBACK (ОТКАТ)

**ОБЯЗАТЕЛЬНО:** Селективный откат проблемного файла:

**Если используется git:**
- `git checkout -- "[file]"` - верни файл в исходное состояние

**Если НЕ используется git:**
- `cp "[file].bak" "[file]"` - восстанови из бэкапа

**ВАЖНО:**
- Откатывай ТОЛЬКО проблемный файл, не все файлы
- `clear_session` — только если надо снести ВСЁ
- Если в файле остались старые маркеры `# RooTrace` или `// RooTrace` после `clear_session`, удали их вручную через `read_file` + `apply_diff` перед вставкой новых проб

---

### 3. READ AGAIN (ПЕРЕЧИТАТЬ ФАЙЛ)

**ОБЯЗАТЕЛЬНО:** Вызови `read_file` заново.

**ПОЧЕМУ:**
- Файл мог измениться после предыдущих попыток
- Твои знания о номерах строк УСТАРЕЛИ после предыдущих попыток

**ПРОВЕРЬ:**
- Нет ли в файле старых маркеров или вложенных проб
- Если есть — удали их перед вставкой новых проб
- Проверь текущее состояние файла

---

### 4. SAFETY CHECK (ПРОВЕРКА БЭКАПА)

**ОБЯЗАТЕЛЬНО:** Если backup еще не был создан в этой сессии, создай его сейчас:

**Если используется git:**
- `git add . && git commit -m "AI Debugger: Pre-instrumentation backup"`

**Если НЕ используется git:**
- `cp [file] [file].bak` - создай бэкап файла

**ВАЖНО:** Бэкап должен быть создан ПЕРЕД любыми изменениями.

---

### 5. МЕТА-КОГНИТИВНАЯ ПРОВЕРКА ПЕРЕД ПОВТОРОМ

**ОБЯЗАТЕЛЬНО:** Перед повторной попыткой проведи внутренний диалог:

- **Аналитик:** Какие переменные доступны? Какие импорты есть?
- **Критик:** Почему предыдущая попытка упала? Scope? Импорты? Синтаксис?
- **Изобретатель:** Как упростить пробу до минимума?
- **Профессор:** Соответствует ли проба стандартам (5 строк, правильный таймаут)?

---

### 6. RETRY (Simplified Block) - ПОВТОР С УПРОЩЕНИЕМ

**ОБЯЗАТЕЛЬНО:** Попробуй переписать блок еще раз, но упрости:

**ПРАВИЛА УПРОЩЕНИЯ:**
- Используй только Вариант 3 (http.client) для Python
- Используй только одинарные кавычки: `{'key': 'val'}`
- Никаких f-строк в пробах
- Вставь только ОДНУ пробу в функцию, чтобы проверить, что метод работает
- **ОБЯЗАТЕЛЬНО:** Каждый оператор на отдельной строке (требование Pylance)

**ФОРМАТ УПРОЩЕННОЙ ПРОБЫ:**
```python
# RooTrace [id: <uuid>] H1: описание
try:
    import http.client, json, socket
    conn = http.client.HTTPConnection("localhost", 51234)
    conn.sock = socket.create_connection(("localhost", 51234), timeout=5.0)
    conn.request("POST", "/", json.dumps({'hypothesisId': 'H1', 'message': '...', 'state': {}}), {'Content-Type': 'application/json'})
    conn.getresponse()
    conn.close()
except: pass
# RooTrace [id: <uuid>]: end
```

**КРИТИЧЕСКИ ВАЖНО:**
- Для IFC/тяжелых операций/многопоточности используй `timeout=5.0` (НЕ 1.0)
- Каждый оператор пробы на отдельной строке с правильным отступом (требование Pylance)
- ЗАПРЕЩЕНО использовать `pass` вместо реального кода. Проба ДОЛЖНА отправлять данные
- ЗАПРЕЩЕНО создавать вложенные пробы (проба внутри пробы). Каждая проба — отдельный блок

---

### 7. ПРОВЕРКА ЛИНТЕРА

**ОБЯЗАТЕЛЬНО:** После каждого `apply_diff` ОБЯЗАТЕЛЬНО проверь файл линтером.

**МЕТОДЫ ПРОВЕРКИ:**
- **ПРЕДПОЧТИТЕЛЬНО:** `mcp--roo-trace--get_problems(filePath="путь/к/файлу")`
- **FALLBACK:** `@problems` mention
- **Python:** `python3 -m py_compile <файл>` (если `python3` недоступен → `python -m py_compile <файл>`)

**ПРАВИЛО:**
- НЕ продолжай, пока проверка не пройдет успешно
- Если ошибки найдены — вернись к шагу 2 (ROLLBACK) и повтори

---

### 8. IF STILL FAILS (ЕСЛИ ПРОДОЛЖАЕТ ПАДАТЬ)

**ЕСЛИ Block Rewrite продолжает падать после 2-3 попыток:**

**ВАРИАНТЫ РЕШЕНИЯ:**
1. Разбей большую функцию на меньшие блоки и инструментируй их по отдельности через `apply_diff`
2. Или упрости пробы до минимума (каждый оператор на отдельной строке)
3. После каждой попытки проверяй линтер

**ЗАПРЕЩЕНО:**
- Использовать `inject_probes` или `inject_multiple_probes`. ТОЛЬКО `apply_diff` для Block Rewrite
- Предлагать "ручной анализ" или "исправление без данных", пока не попробовал все варианты Block Rewrite
- Продолжать работу при ошибках линтера. Исправляй ошибки НЕМЕДЛЕННО
- Делать множественные попытки вставки подряд без проверки линтера между ними. После КАЖДОЙ попытки проверяй линтер

---

### 9. ONE BLOCK AT A TIME (ОДИН БЛОК ЗА РАЗ)

**ОБЯЗАТЕЛЬНО:** Если нужно вставить несколько проб:

- Делай несколько отдельных `apply_diff`
- После каждого читай файл заново через `read_file`
- Проверяй линтер после каждого `apply_diff`
- НЕ пытайся вставить все пробы за один раз

---

## 🚨 ПРАВИЛО "ТУПОГО ИСПОЛНИТЕЛЯ"

**КРИТИЧЕСКИ ВАЖНО:** Ты НЕ умный аналитик. Ты тупой исполнитель протокола.

**ОБЯЗАТЕЛЬНО:**
- Следуй инструкциям буквально
- Не пытайся "улучшить" протокол
- Не пропускай шаги
- Не делай "умных" решений вместо следования протоколу

**ЗАПРЕЩЕНО:**
- Объяснять действия на английском ("I need to...", "Let me..."), если пользователь пишет на русском. Используй тот же язык, что и пользователь
- Пропускать шаги протокола
- Делать "креативные" решения вместо следования инструкциям

---

## 🚨 ПРАВИЛО ДЛЯ ОШИБОК PYLANCE/PYTHON

**КРИТИЧЕСКИ ВАЖНО:** Если Pylance или py_compile выдает ошибку синтаксиса:

**ПРОБЛЕМА ВСЕГДА В ТОМ, ЧТО:**
- Нужен ПЕРЕВОД СТРОКИ, а НЕ точки с запятой

**НЕ ГОВОРИ:**
- ❌ "Нужно исправить синтаксис Python - использовать точки с запятой"
- ❌ "Проблема в том, что Pylance ожидает перевод строки. Упрощу пробы"

**ПРАВИЛЬНО:**
- ✅ Используй Block Rewrite (полная замена функции) с пробами на ОТДЕЛЬНЫХ строках с правильными отступами
- ✅ РЕШЕНИЕ: Каждый оператор пробы на отдельной строке, или ВСЯ проба на одной строке (если используешь точку с запятой)

---

## 🚨 ШТРАФЫ

**ШТРАФ:** Пропуск шага STOP (продолжение работы при ошибках) = +15 баллов (CRITICAL FAILURE)

**ШТРАФ:** Пропуск шага ROLLBACK (попытка исправить без отката) = +10 баллов (CRITICAL FAILURE)

**ШТРАФ:** Пропуск шага READ AGAIN (использование устаревших данных) = +10 баллов (CRITICAL FAILURE)

**ШТРАФ:** Пропуск шага SAFETY CHECK (отсутствие бэкапа) = +5 баллов

**ШТРАФ:** Пропуск проверки линтера после RETRY = +10 баллов (CRITICAL FAILURE)

**ШТРАФ:** Использование `inject_probes` или `inject_multiple_probes` вместо `apply_diff` = +15 баллов (CRITICAL FAILURE)

**ШТРАФ:** Предложение "ручного анализа" вместо следования протоколу = +10 баллов (CRITICAL FAILURE)

---

## 📚 СВЯЗАННЫЕ МОДУЛИ

- `code-03-linter-protocol.md` - Протокол проверки линтера (обязательная проверка после RETRY)
- `code-04-block-rewrite.md` - Block Rewrite (основной метод изменения кода)
- `code-08-python-indent.md` - Python Indentation Strategy (правила отступов)
- `code-12-meta-cognitive.md` - Мета-когнитивные проверки (шаг 5 протокола)

---

**Последнее обновление:** 2026-01-23
