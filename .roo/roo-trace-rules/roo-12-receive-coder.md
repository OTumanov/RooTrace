# 📥 PHASE 1.2: ПРИЕМКА SUMMARY ОТ КОДЕРА (ПОСЛЕ ВСТАВКИ ПРОБ)

**🚨 КРИТИЧЕСКИ ВАЖНО:** Ты получаешь только summary, не весь контекст подзадачи.

**ВЫПОЛНЯЕТСЯ:** ПОСЛЕ Phase 1.1 (делегирование вставки проб), ПЕРЕД Phase 5 (WAIT).

---

## 🎯 НАЗНАЧЕНИЕ

Phase 1.2 - это приемка и валидация summary от кодера после вставки проб. Цель - убедиться, что все пробы вставлены корректно и готовы к использованию.

---

## 📋 ОБЯЗАТЕЛЬНЫЕ ШАГИ

### 1. Получение summary

**🚨🚨🚨 КРИТИЧЕСКИ ВАЖНО: Ты НЕ вызываешь `attempt_completion` сам! 🚨🚨🚨**

**ИСТОЧНИК:** Summary приходит АВТОМАТИЧЕСКИ от подзадачи кодера, когда кодер завершает свою работу через `attempt_completion` в своей подзадаче.

**КАК ЭТО РАБОТАЕТ В ROO CODE:**
1. Ты создал подзадачу через `new_task(mode="code", message="...")`
2. Твоя задача приостанавливается, кодер работает в изолированном контексте подзадачи
3. Кодер вызывает `attempt_completion(result="...")` в своей подзадаче
4. Roo Code автоматически возобновляет твою задачу и передает summary в твой контекст
5. Summary уже доступен в твоем контексте - НЕ нужно вызывать `attempt_completion` самому!

**🚨 ЗАПРЕЩЕНО:**
- ❌ Вызывать `attempt_completion` самому - это делает кодер в своей подзадаче!
- ❌ "Зафиксировать результаты" через `attempt_completion` - summary уже пришел от кодера!
- ❌ Создавать свой summary - используй summary от кодера, который уже в контексте!

**✅ ОБЯЗАТЕЛЬНО:**
- ✅ Используй summary, который уже доступен в твоем контексте после завершения подзадачи кодера
- ✅ Summary содержит текст, который кодер передал в `attempt_completion(result="...")`
- ✅ Валидируй формат полученного summary

---

### 2. Валидация формата summary

**ОБЯЗАТЕЛЬНО:** Проверь наличие всех полей:

**ФОРМАТ:** Summary содержит структурированные данные:
```
ВСТАВЛЕНЫ ПРОБЫ:
- H1: [файл]:[строка] - [статус: успешно/ошибка]
- H2: [файл]:[строка] - [статус: успешно/ошибка]
- H3: [файл]:[строка] - [статус: успешно/ошибка]
...

ДИАГНОСТИКА:
- [файл]: [OK/ERRORS/WARNINGS] - [детали если есть ошибки]

PATCH ФАЙЛЫ:
- [путь к .patch файлу]
- [путь к .patch файлу]
```

**ОБЯЗАТЕЛЬНЫЕ ПОЛЯ:**
- **ВСТАВЛЕНЫ ПРОБЫ:** Список всех вставленных проб с файлами и строками - ОБЯЗАТЕЛЬНО
- **ДИАГНОСТИКА:** Результаты проверки диагностики для каждого файла - ОБЯЗАТЕЛЬНО
- **PATCH ФАЙЛЫ:** Список созданных .patch файлов - ОБЯЗАТЕЛЬНО

**ВАЛИДАЦИЯ:**
1. **Наличие подтверждения вставки проб:**
   - ✅ Минимум H1-H3 должны иметь подтверждение вставки
   - ❌ ЗАПРЕЩЕНО: Принимать summary без подтверждения вставки проб
   - ❌ ЗАПРЕЩЕНО: Принимать summary с ошибками вставки без исправления

2. **Наличие результатов диагностики:**
   - ✅ Каждый файл должен иметь результат диагностики (OK/ERRORS/WARNINGS)
   - ❌ ЗАПРЕЩЕНО: Принимать summary без результатов диагностики
   - ❌ ЗАПРЕЩЕНО: Принимать summary с ERRORS без исправления

**ЕСЛИ ФОРМАТ НЕ СООТВЕТСТВУЕТ:**
- ❌ ERROR: "Summary от кодера не соответствует формату. Требуется: список вставленных проб, результаты диагностики, список .patch файлов"
- ❌ Запроси повтор через `new_task` с уточнением формата
- ❌ НЕ продолжай к Phase 5 без валидного summary

---

### 3. Установка timestamp для фильтрации логов

**🚨 КРИТИЧЕСКИ ВАЖНО: УСТАНОВКА _run_request_timestamp РАНЬШЕ**

**ОБЯЗАТЕЛЬНО:** Установи `_run_request_timestamp` сразу после получения summary от кодера (в Phase 1.2), а не в Phase 5.

**ПОЧЕМУ РАНЬШЕ:**
- Если пользователь запустит код ДО того, как RooTrace перейдет в Phase 5, логи будут потеряны
- Установка timestamp в Phase 1.2 гарантирует, что все логи после вставки проб будут учтены

**ФОРМАТ:**
```javascript
// Установи timestamp сразу после валидации summary
_run_request_timestamp = new Date().toISOString();
// Или
_run_request_timestamp = Date.now();
```

**ВАЖНО:** Этот timestamp будет использован в Phase 6 для фильтрации логов (только логи после этого времени).

---

### 4. Парсинг summary

**ОБЯЗАТЕЛЬНО:** Извлеки структурированные данные:

- Список вставленных проб (H1-H5 с файлами и строками)
- Результаты диагностики для каждого файла
- Список созданных .patch файлов

**ФОРМАТ ХРАНЕНИЯ:**
- Сохрани summary в локальной переменной для использования в Phase 5-6

---

### 5. Обновление TODO

**ОБЯЗАТЕЛЬНО:**
- Отметь: `[DONE] Пробы вставлены кодера`
- Добавь задачу: `Phase 5: WAIT (пользователь запускает код)`

---

### 6. Валидация содержания

**ОБЯЗАТЕЛЬНО:** Проверь качество summary:

- Если summary пустой → ❌ ERROR и запроси повтор
- Если пробы не вставлены для H1-H3 → ❌ ERROR и запроси повтор
- Если диагностика показывает ERRORS → ❌ ERROR и запроси исправление
- Если все ОК → ✅ Переходи к Phase 5

**КРИТЕРИИ КАЧЕСТВА:**
- Минимум 3 пробы вставлены (H1-H3)
- Все файлы прошли диагностику (OK или WARNINGS, не ERRORS)
- Созданы .patch файлы для всех измененных файлов

---

## 🚨 ШТРАФЫ

**ШТРАФ:** Продолжение без валидации формата summary = +15 баллов (CRITICAL FAILURE)

**ШТРАФ:** Продолжение без валидации содержания summary = +10 баллов (CRITICAL FAILURE)

**ШТРАФ:** Принятие summary без подтверждения вставки проб = +15 баллов (CRITICAL FAILURE)

**ШТРАФ:** Принятие summary с ERRORS в диагностике = +20 баллов (CRITICAL FAILURE)

**ШТРАФ:** Пропуск установки _run_request_timestamp в Phase 1.2 = +10 баллов (CRITICAL FAILURE - риск потери логов)

**ШТРАФ:** Продолжение к Phase 5 при пустом summary = +20 баллов (CRITICAL FAILURE)

---

## 📚 СВЯЗАННЫЕ МОДУЛИ

**🚨🚨🚨 КРИТИЧЕСКИ ВАЖНО: ИМЯ ИНСТРУМЕНТА ДОСЛОВНОЕ! 🚨🚨🚨**

**Для загрузки модулей СКОПИРУЙ КОМАНДЫ БУКВА В БУКВУ (НЕ ИЗМЕНЯЙ ИМЯ ИНСТРУМЕНТА!):**

- **СКОПИРУЙ:** `load_rule(rulePath="code-01-probe-insertion.md")` - Phase 1.1: Делегирование вставки проб
- **СКОПИРУЙ:** `load_rule(rulePath="roo-08-wait.md")` - Phase 5: WAIT (следующий шаг)
- **СКОПИРУЙ:** `load_rule(rulePath="00-base-advanced.md")` - Валидация форматов

---

**Последнее обновление:** 2026-01-24
