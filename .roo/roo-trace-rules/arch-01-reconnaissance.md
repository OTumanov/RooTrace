# 🔍 ДЕТАЛЬНЫЙ ПРОТОКОЛ РАЗВЕДКИ

**🚨🚨🚨 КРИТИЧЕСКИ ВАЖНО: ЗАТКНИ ЕБАЛЬНИК И ДЕЛАЙ! 🚨🚨🚨**

**ЗАПРЕЩЕНО:**
- ❌ "I'll start by...", "Now I'll...", "Let me...", "First I'll..." - сразу вызывай инструменты!
- ❌ Любые рассуждения, объяснения перед действием
- ❌ Текстовые блоки длиннее 1 строки перед вызовом инструмента
- ❌ Описание того, что ты собираешься сделать - просто делай!

**РАЗРЕШЕНО:**
- ✅ Только вызов инструментов БЕЗ объяснений
- ✅ Краткие статусы (1 строка максимум)

**ШТРАФ:** Рассуждения и объяснения перед действием = +30 points (CRITICAL FAILURE)

**🎯 НАЗНАЧЕНИЕ:** Этот модуль содержит детальные инструкции для архитектора при выполнении разведки (Phase 0.2).

**КОГДА ИСПОЛЬЗУЕТСЯ:** Загружается автоматически при делегировании разведки через `new_task(mode="architect")` в Phase 0.2.

---

## 🎯 ЦЕЛЬ РАЗВЕДКИ

Твоя единственная цель — разведка. Составь список файлов и подозрительных мест.

**ЗАПРЕЩЕНО:**
- ❌ Исправлять код, вставлять пробы, или делать любые изменения
- ❌ Задавать вопросы о требованиях, целевой производительности, ожиданиях пользователя
- ❌ Спрашивать "сколько времени должно занимать", "какой размер файлов", "какие требования"
- ❌ Переключаться в другой режим при ошибках инструментов
- ❌ Прекращать разведку из-за ошибок - используй другие инструменты

**ОБЯЗАТЕЛЬНО:**
- ✅ Искать код, который работает медленно (по описанию проблемы)
- ✅ Анализировать существующий код и находить подозрительные места
- ✅ Продолжать разведку даже если один инструмент не работает - используй другие инструменты
- ✅ Только анализ и составление списка координат

**ЧТО ДЕЛАТЬ ПРИ ОШИБКАХ ИНСТРУМЕНТОВ:**
- Если `ask_followup_question` не работает → НЕ задавай вопросы, просто продолжай разведку через `codebase_search` и `search_files`
- Если один инструмент не работает → используй другие инструменты из списка
- Если все инструменты не работают → используй `read_file` для чтения файлов, которые упомянуты в задаче
- НИКОГДА не прекращай разведку из-за ошибок - найди другой способ получить информацию

---

## 📋 ОБЯЗАТЕЛЬНЫЕ ШАГИ

### 1. Понимание структуры проекта

**ОБЯЗАТЕЛЬНО:** Начни с понимания структуры проекта:

**Используй `list_files` для понимания структуры проекта:**
- **Параметры:**
  - `path` (обязательный): Путь к директории для просмотра содержимого (относительно workspace root)
  - `recursive` (опциональный): `true` для рекурсивного просмотра, `false` или пропустить для только верхнего уровня
- **Что делает:**
  - Показывает все файлы и директории в указанном месте
  - Может работать рекурсивно или только на верхнем уровне
  - Игнорирует node_modules, .git и другие большие директории в рекурсивном режиме
  - Уважает правила .gitignore в рекурсивном режиме
  - Уважает правила .rooignore: файлы, игнорируемые `.rooignore`, пропускаются или помечаются символом 🔒
  - Если файл помечен 🔒, он недоступен для чтения или записи через инструменты Roo
- **Когда использовать:**
  - В начале разведки для понимания структуры проекта
  - Для поиска конфигурационных файлов (например, .ai_debug_config, .debug_port)
  - Для навигации по кодовой базе перед более глубоким анализом
  - Для проверки организации проекта
- **Примеры:**
  - `list_files(path=".")` - список файлов в корневой директории
  - `list_files(path="src", recursive=true)` - рекурсивный список всех файлов в src
  - `list_files(path=".rootrace")` - список файлов в директории .rootrace

**Используй `list_code_definition_names` для быстрого понимания структуры кодовой базы:**
- **Параметры:**
  - `path` (обязательный): Путь к директории для получения структурного обзора (относительно workspace root)
- **Что делает:**
  - Извлекает классы, функции, методы, интерфейсы и другие определения из исходных файлов
  - Показывает номера строк и исходный код для каждого определения
  - Обрабатывает только файлы на верхнем уровне указанной директории (не рекурсивно)
- **Когда использовать:**
  - В начале разведки для быстрого понимания архитектуры проекта
  - Для получения обзора структуры перед более глубоким анализом
  - Для идентификации важных компонентов кодовой базы
- **Примеры:**
  - `list_code_definition_names(path="src")` - получить структуру директории src
  - `list_code_definition_names(path=".")` - получить структуру корневой директории

---

### 2. Поиск подозрительных мест

**🚨 КРИТИЧЕСКИ ВАЖНО:** Не просто броди по структуре проекта! Ты должен ЦЕЛЕНАПРАВЛЕННО искать код, связанный с проблемой.

**СТРАТЕГИЯ ПОИСКА:**

1. **Сначала определи ключевые слова из описания проблемы:**
   - Извлеки ключевые термины: "вырезание сборок", "assembly", "cut", "subset", "performance", "медленно"
   - Определи типы проблем: "performance", "slow", "bottleneck", "optimization"
   - Найди упоминания конкретных операций: "cutAssembly", "extractAssembly", "processAssembly"

2. **Используй `codebase_search` для семантического поиска по смыслу:**
   - ✅ **ПРАВИЛЬНО:** "функция вырезания сборок из IFC файла"
   - ✅ **ПРАВИЛЬНО:** "медленная обработка сборок при вырезании"
   - ✅ **ПРАВИЛЬНО:** "производительность при обработке assembly"
   - ❌ **НЕПРАВИЛЬНО:** "код" или "функции" (слишком общо)

3. **Используй `search_files` для поиска конкретных паттернов:**
   - Ищи функции по именам: `def.*assembly`, `def.*cut`, `def.*extract`
   - Ищи классы: `class.*Assembly`, `class.*Cutter`
   - Ищи вызовы: `cutAssembly`, `extractAssembly`, `processAssembly`

4. **Читай файлы, которые явно связаны с проблемой:**
   - Если в задаче упомянут файл/эндпоинт (например, `/upload`, `cut_ifc.py`) - начни с него
   - Если есть patch файлы с именами функций - это подсказка, где искать

**ОБЯЗАТЕЛЬНО:** Используй различные методы поиска для нахождения подозрительных мест:

**Используй `search_files` для поиска паттернов кода по регулярным выражениям:**
- **Параметры:**
  - `path` (обязательный): Путь к директории для поиска (относительно workspace root). Поиск ограничен workspace.
  - `regex` (обязательный): Регулярное выражение для поиска (использует синтаксис Rust regex)
  - `file_pattern` (опциональный): Glob паттерн для фильтрации файлов (например, '*.ts' для TypeScript файлов)
  - `respect_gitignore` (опциональный): Учитывать ли правила .gitignore (по умолчанию: true)
- **Что делает:**
  - Выполняет поиск по регулярным выражениям в нескольких файлах одновременно
  - Использует высокопроизводительный Ripgrep для быстрого поиска
  - Показывает контекст вокруг каждого совпадения (1 строка до и после)
  - Фильтрует файлы по типу через glob паттерны
  - Предоставляет номера строк для удобной навигации
- **Примеры:**
  - `search_files(path="src", regex="function processData", file_pattern="*.js")` - поиск функции в JavaScript файлах
  - `search_files(path=".", regex="TODO|FIXME", file_pattern="*.ts")` - поиск TODO комментариев в TypeScript файлах

**Используй `codebase_search` для семантического поиска релевантного кода:**
- **Что такое Codebase Indexing:** Система семантического поиска, которая использует AI embeddings для преобразования кода в поисковые векторы и хранит их в векторной базе данных Qdrant. Это позволяет находить код по смыслу, а не только по ключевым словам.
- **Параметры:**
  - `query` (обязательный): Естественный язык запроса, описывающий что ищешь (концептуально, не по ключевым словам)
  - `path` (опциональный): Путь к директории для ограничения области поиска
- **🚨 КРИТИЧЕСКИ ВАЖНО:** Используй `codebase_search` ПЕРВЫМ делом для поиска кода, связанного с проблемой!
- **Формат запросов (Best Practices):**
  - ✅ **ХОРОШО:** Концептуальные и специфичные запросы на естественном языке, связанные с проблемой
    - "функция вырезания сборок из IFC файла"
    - "медленная обработка assembly при вырезании"
    - "производительность при обработке сборок"
    - "оптимизация вырезания subset из IFC"
    - "обработка ошибок в API вызовах"
    - "валидация пользовательского ввода"
    - "user authentication logic"
  - ❌ **ПЛОХО:** Слишком общие запросы или точный синтаксис
    - "код"
    - "функции"
    - "const getUser" (используй "function to fetch user from database" вместо этого)
- **Примеры запросов для типичных проблем:**
  - **Performance проблемы:** "медленная функция обработки", "производительность при обработке данных", "оптимизация алгоритма"
  - **Assembly/Cutting проблемы:** "вырезание сборок", "обработка assembly", "создание subset из IFC"
  - **Database проблемы:** "медленные запросы к базе данных", "оптимизация SQL запросов"
- **Интерпретация результатов:**
  - Similarity score 0.8-1.0: Высокая релевантность, скорее всего именно то, что нужно
  - Similarity score 0.6-0.8: Хорошие совпадения с сильным концептуальным сходством
  - Similarity score 0.4-0.6: Потенциально релевантные, но требуют проверки
  - Ниже 0.4: Отфильтрованы как слишком непохожие

---

### 3. Анализ подозрительных файлов

**ОБЯЗАТЕЛЬНО:** Прочитай и проанализируй подозрительные файлы:

**Используй `read_file` для анализа подозрительных файлов:**
- **Токен-экономия (КРИТИЧЕСКИ ВАЖНО):**
  - ✅ **ПРАВИЛЬНО:** Читать только нужные функции/диапазоны строк через `line_range` или `start_line`/`end_line`
  - ❌ **НЕПРАВИЛЬНО:** Читать весь файл, если нужна только одна функция
  - ✅ **ПРАВИЛЬНО:** Использовать мультифайловое чтение для связанных файлов одновременно
- **Мультифайловое чтение (Concurrent File Reads):**
  - Читай до 100 файлов в одном запросе
  - Приоритизируй наиболее критичные файлы и читай их в одном батче
  - Используй для связанных файлов, которые нужны для понимания контекста задачи
- **Примеры:**
  - `read_file(path="src/main.py", start_line=10, end_line=50)` - чтение конкретного диапазона
  - `read_file(args=[{file: {path: "src/file1.py"}}, {file: {path: "src/file2.py"}}])` - чтение нескольких файлов

---

### 4. Составление структурированного списка

**ОБЯЗАТЕЛЬНО:** Составь структурированный список: файлы → функции → строки

**Для каждого подозрительного места определи:**
- Файл (полный путь)
- Координата (номер строки)
- Функция (имя функции/метода)
- Причина (краткое обоснование с цитатами кода/логов)

---

## 📤 ФОРМАТ ВЫВОДА (attempt_completion)

**🚨🚨🚨 КРИТИЧЕСКИ ВАЖНО: ОБЯЗАТЕЛЬНО используй `attempt_completion` для возврата результатов! 🚨🚨🚨**

**ЗАПРЕЩЕНО:**
- ❌ Использовать `switch_mode` - это НЕ твоя задача
- ❌ Просить пользователя переключиться в другой режим
- ❌ Передавать задачу кодеру - ты должен вернуть summary оркестратору
- ❌ Создавать todo list для реализации - ты только разведка

**ОБЯЗАТЕЛЬНО:** Используй инструмент `attempt_completion` с параметром `result` - это ЕДИНСТВЕННЫЙ способ вернуть результаты

**ФОРМАТ result:** Краткое, но полное резюме в структурированном формате:
```
ФАЙЛ:путь/к/файлу.py
КООРДИНАТА:строка:номер
ФУНКЦИЯ:имя_функции
ПРИЧИНА:краткое обоснование с цитатами кода/логов (минимум 3-5 строк кода или точная цитата лога)
---
[Повторить для каждого подозрительного места]
```

**ТРЕБОВАНИЯ:**
- Только координаты и причины - НИЧЕГО ДРУГОГО
- Без лишних приветствий или filler-текста
- Профессиональный, прямой тон
- Легко сканируется и понимается
- **КРИТИЧЕСКИ ВАЖНО:** Если ты не предоставишь координаты в формате ФАЙЛ:КООРДИНАТА:ФУНКЦИЯ:ПРИЧИНА, оркестратор отклонит твой отчет!

**ВАЛИДАЦИЯ ПРИЧИНЫ (критически важно):**
- ✅ Цитата кода должна быть оформлена как code block: ```language\nкод\n``` или `код`
- ✅ Цитата лога должна быть оформлена как code block или в кавычках
- ❌ ЗАПРЕЩЕНО: Принимать REASON без обратных кавычек в цитатах
- ❌ ЗАПРЕЩЕНО: Принимать REASON где код/лог описан словами без цитаты
- **Проверка:** REASON должен содержать минимум один code block (``` или `) или строку в кавычках

---

## 🚨 ШТРАФЫ

**🚨 CRITICAL FAILURE:** Использование `switch_mode` вместо `attempt_completion` = +20 баллов (CRITICAL FAILURE)
**🚨 CRITICAL FAILURE:** Передача задачи кодеру вместо возврата summary = +20 баллов (CRITICAL FAILURE)
**ШТРАФ:** Исправление кода вместо разведки = +15 баллов (CRITICAL FAILURE)
**ШТРАФ:** Предоставление summary не в формате ФАЙЛ:КООРДИНАТА:ФУНКЦИЯ:ПРИЧИНА = +15 баллов (CRITICAL FAILURE)
**ШТРАФ:** Предоставление REASON без цитат кода/логов = +10 баллов (CRITICAL FAILURE)

---

## 📚 СВЯЗАННЫЕ МОДУЛИ

- `arch-00-role.md` - Роль архитектора (общие принципы работы)
- `arch-03-format-recon.md` - Формат вывода для разведки (детальная спецификация формата)
- `00-help-operations.md` - Справочник по инструментам поиска и чтения файлов

---

**Последнее обновление:** 2026-01-23
