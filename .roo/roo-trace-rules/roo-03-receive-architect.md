# 📥 PHASE 0.3: ПРИЕМКА SUMMARY ОТ АРХИТЕКТОРА

**🚨 КРИТИЧЕСКИ ВАЖНО:** Ты получаешь только summary, не весь контекст подзадачи.

**ВЫПОЛНЯЕТСЯ:** ПОСЛЕ Phase 0.2 (делегирование разведки), ПЕРЕД Phase 0.4 (Pre-Flight Check).

---

## 🎯 НАЗНАЧЕНИЕ

Phase 0.3 - это приемка и валидация summary от архитектора. Цель - получить структурированный список подозрительных мест в формате ФАЙЛ:КООРДИНАТА:ФУНКЦИЯ:ПРИЧИНА для дальнейшей работы.

---

## 📋 ОБЯЗАТЕЛЬНЫЕ ШАГИ

### 1. Получение summary

**🚨🚨🚨 КРИТИЧЕСКИ ВАЖНО: Ты НЕ вызываешь `attempt_completion` сам! 🚨🚨🚨**

**ИСТОЧНИК:** Summary приходит АВТОМАТИЧЕСКИ от подзадачи архитектора, когда архитектор завершает свою работу через `attempt_completion` в своей подзадаче.

**КАК ЭТО РАБОТАЕТ В ROO CODE:**
1. Ты создал подзадачу через `new_task(mode="architect", message="...")`
2. Твоя задача приостанавливается, архитектор работает в изолированном контексте подзадачи
3. Архитектор вызывает `attempt_completion(result="...")` в своей подзадаче
4. Roo Code автоматически возобновляет твою задачу и передает summary в твой контекст
5. Summary уже доступен в твоем контексте - НЕ нужно вызывать `attempt_completion` самому!
6. Summary - это текст, который архитектор передал в параметре `result` при вызове `attempt_completion`

**🚨 ЗАПРЕЩЕНО:**
- ❌ Вызывать `attempt_completion` самому - это делает архитектор в своей подзадаче!
- ❌ "Зафиксировать результаты разведки" через `attempt_completion` - summary уже пришел от архитектора!
- ❌ Создавать свой summary - используй summary от архитектора, который уже в контексте!

**✅ ОБЯЗАТЕЛЬНО:**
- ✅ Используй summary, который уже доступен в твоем контексте после завершения подзадачи архитектора
- ✅ Summary содержит текст, который архитектор передал в `attempt_completion(result="...")`
- ✅ Валидируй формат полученного summary

**ЗАПРЕЩЕНО:**
- ❌ Вызывать `attempt_completion` самому - это делает архитектор в своей подзадаче!
- ❌ "Зафиксировать результаты разведки" через `attempt_completion` - summary уже пришел от архитектора!
- ❌ Создавать свой summary - используй summary от архитектора!

**ОБЯЗАТЕЛЬНО:**
- ✅ Проверь, что подзадача архитектора завершена
- ✅ Используй summary, который пришел от архитектора автоматически
- ✅ Валидируй формат полученного summary

**ФОРМАТ:** Summary содержит структурированные данные в формате:
```
ФАЙЛ:путь/к/файлу.py
КООРДИНАТА:строка:номер
ФУНКЦИЯ:имя_функции
ПРИЧИНА:краткое обоснование с цитатами кода/логов (минимум 3-5 строк кода или точная цитата лога)
---
[Повторить для каждого подозрительного места]
```

---

### 2. Валидация формата summary

**ОБЯЗАТЕЛЬНО:** Проверь наличие всех полей для каждого места:

- **ФАЙЛ:** (путь к файлу) - ОБЯЗАТЕЛЬНО
- **КООРДИНАТА:** (номер строки) - ОБЯЗАТЕЛЬНО
- **ФУНКЦИЯ:** (имя функции) - ОБЯЗАТЕЛЬНО
- **ПРИЧИНА:** (краткое обоснование) - ОБЯЗАТЕЛЬНО

**ВАЛИДАЦИЯ REASON (критически важно):**

1. **Наличие обратных кавычек (code blocks):**
   - ✅ Цитата кода должна быть оформлена как code block: ```language\nкод\n``` или `код`
   - ✅ Цитата лога должна быть оформлена как code block или в кавычках
   - ❌ ЗАПРЕЩЕНО: Принимать REASON без обратных кавычек в цитатах
   - ❌ ЗАПРЕЩЕНО: Принимать REASON где код/лог описан словами без цитаты
   - **Проверка:** REASON должен содержать минимум один code block (``` или `) или строку в кавычках

2. **Соответствие логам (если в описании проблемы есть лог ошибки):**
   - ✅ Если в описании проблемы упомянут лог ошибки, REASON должен содержать цитату этого лога или связанного кода
   - ❌ ЗАПРЕЩЕНО: Принимать REASON с общими фразами ("possible issue", "might be wrong") без конкретных цитат

**ЕСЛИ ФОРМАТ НЕ СООТВЕТСТВУЕТ:**
- ❌ ERROR: "Summary от архитектора не соответствует формату. Требуется: ФАЙЛ:КООРДИНАТА:ФУНКЦИЯ:ПРИЧИНА"
- ❌ Запроси повтор через `new_task` с уточнением формата
- ❌ НЕ продолжай к Phase 0.4 без валидного summary

---

### 3. Парсинг summary

**ОБЯЗАТЕЛЬНО:** Извлеки структурированные данные:

- Список файлов (ФАЙЛ:)
- Координаты (КООРДИНАТА:)
- Имена функций (ФУНКЦИЯ:)
- Причины (ПРИЧИНА:)

**ФОРМАТ ХРАНЕНИЯ:**
- Сохрани summary для использования в Phase 1 (формулирование гипотез)
- НЕ читай файлы заново - используй summary от архитектора

---

### 4. Запись в memory (если доступно)

**ЕСЛИ memory доступен (Deep Debug Mode):**
- Используй `mcp_memory_add_observations` для записи summary
- Наблюдение: `"Reconnaissance summary: ${result}"`
- Теги: `["debug_session", "reconnaissance"]`

**ЕСЛИ memory НЕ доступен:**
- Пропусти этот шаг
- Сохрани summary в локальной переменной для использования в Phase 1

---

### 5. Обновление TODO

**ОБЯЗАТЕЛЬНО:**
- Отметь: `[DONE] Разведка получена`
- Добавь задачи на основе summary:
  - `Phase 0.4: Pre-Flight Check (RooTrace делает сам)`
  - `Phase 1: Сформулировать гипотезы H1-H3 на основе разведки`

---

### 6. Валидация содержания

**ОБЯЗАТЕЛЬНО:** Проверь качество summary:

- Если summary пустой → ❌ ERROR и запроси повтор
- Если список файлов пустой → ❌ ERROR и запроси повтор
- Если все ОК → ✅ Переходи к Phase 0.4

**КРИТЕРИИ КАЧЕСТВА:**
- Минимум 3 подозрительных места (рекомендуется 5-10)
- Каждое место должно иметь все 4 поля (ФАЙЛ, КООРДИНАТА, ФУНКЦИЯ, ПРИЧИНА)
- REASON должен содержать цитаты кода/логов, а не общие фразы

---

## 🚨 ШТРАФЫ

**ШТРАФ:** Продолжение без валидации формата summary = +15 баллов (CRITICAL FAILURE)

**ШТРАФ:** Продолжение без валидации содержания summary = +10 баллов (CRITICAL FAILURE)

**ШТРАФ:** Принятие summary с REASON без цитат кода/логов = +10 баллов (CRITICAL FAILURE)

**ШТРАФ:** Продолжение к Phase 0.4 при пустом summary = +20 баллов (CRITICAL FAILURE)

---

## 📚 СВЯЗАННЫЕ МОДУЛИ

**🚨🚨🚨 КРИТИЧЕСКИ ВАЖНО: ИМЯ ИНСТРУМЕНТА ДОСЛОВНОЕ! 🚨🚨🚨**

**Для загрузки модулей СКОПИРУЙ КОМАНДЫ БУКВА В БУКВУ (НЕ ИЗМЕНЯЙ ИМЯ ИНСТРУМЕНТА!):**

- **СКОПИРУЙ:** `load_rule(rulePath="roo-02-delegate-recon.md")` - Phase 0.2: Делегирование разведки
- **СКОПИРУЙ:** `load_rule(rulePath="roo-04-preflight.md")` - Phase 0.4: Pre-Flight Check
- **СКОПИРУЙ:** `load_rule(rulePath="roo-05-hypotheses.md")` - Phase 1: Формулирование гипотез
- **СКОПИРУЙ:** `load_rule(rulePath="arch-03-format-recon.md")` - Формат вывода для разведки
- **СКОПИРУЙ:** `load_rule(rulePath="00-base-advanced.md")` - Валидация форматов summary (входит в объединенный модуль)
- **СКОПИРУЙ:** `load_rule(rulePath="00-help-operations.md")` - Справочник по `attempt_completion`

---

**Последнее обновление:** 2026-01-23
