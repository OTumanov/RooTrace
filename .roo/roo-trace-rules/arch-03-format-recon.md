# 📋 ФОРМАТ ВЫВОДА ДЛЯ РАЗВЕДКИ

**🚨 КРИТИЧЕСКИ ВАЖНО:** Этот модуль определяет строгий формат вывода для разведки архитектора. Несоответствие формату приведет к отклонению summary.

**🚨🚨🚨 КРИТИЧЕСКИ ВАЖНО: ВСЕГДА используй `attempt_completion` для возврата результатов! НИКОГДА не используй `switch_mode`! 🚨🚨🚨**

**ВЫПОЛНЯЕТСЯ:** При создании summary через `attempt_completion` после разведки (Phase 0.3).

**ЗАПРЕЩЕНО:**
- ❌ Использовать `switch_mode` - это НЕ твоя задача
- ❌ Просить пользователя переключиться в другой режим
- ❌ Передавать задачу кодеру - ты должен вернуть summary оркестратору

---

## 🎯 НАЗНАЧЕНИЕ

Этот модуль определяет детальную спецификацию формата вывода для разведки архитектора. Цель - обеспечить структурированный и валидируемый обмен данными между архитектором и оркестратором.

---

## 📋 ОБЯЗАТЕЛЬНЫЙ ФОРМАТ

### Структура summary

**ФОРМАТ:**
```
ФАЙЛ:путь/к/файлу.py
КООРДИНАТА:строка:номер
ФУНКЦИЯ:имя_функции
ПРИЧИНА:краткое обоснование с цитатами кода/логов (минимум 3-5 строк кода или точная цитата лога)
---
[Повторить для каждого подозрительного места]
```

### Пример правильного формата

```
ФАЙЛ:src/mesh_extractor.py
КООРДИНАТА:120
ФУНКЦИЯ:extract_meshes
ПРИЧИНА:Функция обрабатывает большой список без проверки на пустоту. В логах видно, что `eligible_part_ids` может быть пустым:
```python
for part_id in eligible_part_ids:
    mesh = extract_mesh_for_part(part_id)
```
Если `eligible_part_ids` пуст, функция ничего не делает, но пробы не отправляются.
---
ФАЙЛ:src/mesh_extractor.py
КООРДИНАТА:145
ФУНКЦИЯ:_extract_mesh_for_part
ПРИЧИНА:Функция использует `model.by_id()` без обработки исключений. В логах видна ошибка:
```
KeyError: 'IFC_ENTITY_12345'
```
Код:
```python
product = model.by_id(ifc_id)
```
Нет проверки на существование entity перед использованием.
---
```

---

## 📋 ОБЯЗАТЕЛЬНЫЕ ПОЛЯ

### 1. ФАЙЛ (обязательно)

**ФОРМАТ:** `ФАЙЛ:путь/к/файлу.py`

**ТРЕБОВАНИЯ:**
- Полный путь к файлу относительно корня проекта
- Должен быть реальным файлом, существующим в проекте
- Используй прямые слеши `/` (не обратные `\`)

**ПРИМЕРЫ:**
- ✅ `ФАЙЛ:src/mesh_extractor.py`
- ✅ `ФАЙЛ:lib/utils.py`
- ❌ `ФАЙЛ:mesh_extractor.py` (неполный путь)
- ❌ `ФАЙЛ:src\mesh_extractor.py` (обратные слеши)

---

### 2. КООРДИНАТА (обязательно)

**ФОРМАТ:** `КООРДИНАТА:строка:номер`

**ТРЕБОВАНИЯ:**
- Номер строки должен быть числом
- Должен указывать на конкретную строку в файле
- Должен быть в пределах файла (не отрицательный, не больше размера файла)

**ПРИМЕРЫ:**
- ✅ `КООРДИНАТА:120`
- ✅ `КООРДИНАТА:145`
- ❌ `КООРДИНАТА:строка 120` (лишний текст)
- ❌ `КООРДИНАТА:-5` (отрицательное число)

---

### 3. ФУНКЦИЯ (обязательно)

**ФОРМАТ:** `ФУНКЦИЯ:имя_функции`

**ТРЕБОВАНИЯ:**
- Имя функции должно быть точным (как в коде)
- Должно включать имя метода, если это метод класса
- Используй точное имя из кода, не перефразируй

**ПРИМЕРЫ:**
- ✅ `ФУНКЦИЯ:extract_meshes`
- ✅ `ФУНКЦИЯ:_extract_mesh_for_part`
- ✅ `ФУНКЦИЯ:MeshExtractor.process`
- ❌ `ФУНКЦИЯ:функция извлечения мешей` (перефразирование)
- ❌ `ФУНКЦИЯ:extract` (неполное имя)

---

### 4. ПРИЧИНА (обязательно)

**ФОРМАТ:** `ПРИЧИНА:краткое обоснование с цитатами кода/логов (минимум 3-5 строк кода или точная цитата лога)`

**ТРЕБОВАНИЯ:**
- Должна содержать цитаты кода или логов
- Цитаты должны быть оформлены как code blocks: ```language\nкод\n``` или `код`
- Минимум 3-5 строк кода или точная цитата лога
- Должна объяснять, почему это место подозрительно
- Должна быть связана с описанием проблемы из задачи

**ВАЛИДАЦИЯ REASON (критически важно):**

1. **Наличие обратных кавычек (code blocks):**
   - ✅ Цитата кода должна быть оформлена как code block: ```language\nкод\n``` или `код`
   - ✅ Цитата лога должна быть оформлена как code block или в кавычках
   - ❌ ЗАПРЕЩЕНО: Принимать REASON без обратных кавычек в цитатах
   - ❌ ЗАПРЕЩЕНО: Принимать REASON где код/лог описан словами без цитаты
   - **Проверка:** REASON должен содержать минимум один code block (``` или `) или строку в кавычках

2. **Соответствие логам (если в описании проблемы есть лог ошибки):**
   - ✅ Если в описании проблемы упомянут лог ошибки, REASON должен содержать цитату этого лога или связанного кода
   - ❌ ЗАПРЕЩЕНО: Принимать REASON с общими фразами ("possible issue", "might be wrong") без конкретных цитат

**ПРИМЕРЫ:**

✅ **ПРАВИЛЬНО (с цитатой кода):**
```
ПРИЧИНА:Функция обрабатывает большой список без проверки на пустоту. В логах видно, что `eligible_part_ids` может быть пустым:
```python
for part_id in eligible_part_ids:
    mesh = extract_mesh_for_part(part_id)
```
Если `eligible_part_ids` пуст, функция ничего не делает, но пробы не отправляются.
```

✅ **ПРАВИЛЬНО (с цитатой лога):**
```
ПРИЧИНА:Функция использует `model.by_id()` без обработки исключений. В логах видна ошибка:
```
KeyError: 'IFC_ENTITY_12345'
```
Код:
```python
product = model.by_id(ifc_id)
```
Нет проверки на существование entity перед использованием.
```

❌ **НЕПРАВИЛЬНО (без цитат):**
```
ПРИЧИНА:Функция может обрабатывать пустой список, что может привести к проблемам.
```

❌ **НЕПРАВИЛЬНО (общие фразы):**
```
ПРИЧИНА:Возможная проблема в обработке данных.
```

---

## 📋 РАЗДЕЛИТЕЛЬ

**ФОРМАТ:** `---`

**ТРЕБОВАНИЯ:**
- Используй три дефиса `---` для разделения записей
- Каждая запись должна быть отделена разделителем
- Последняя запись НЕ должна иметь разделитель после себя

**ПРИМЕР:**
```
ФАЙЛ:src/file1.py
КООРДИНАТА:10
ФУНКЦИЯ:func1
ПРИЧИНА:...
---
ФАЙЛ:src/file2.py
КООРДИНАТА:20
ФУНКЦИЯ:func2
ПРИЧИНА:...
```

---

## 📋 КРИТЕРИИ КАЧЕСТВА

**ОБЯЗАТЕЛЬНО:**
- Минимум 3 подозрительных места (рекомендуется 5-10)
- Каждое место должно иметь все 4 поля (ФАЙЛ, КООРДИНАТА, ФУНКЦИЯ, ПРИЧИНА)
- REASON должен содержать цитаты кода/логов, а не общие фразы
- Все координаты должны быть валидными (существующие файлы и строки)

**РЕКОМЕНДУЕТСЯ:**
- Приоритизация: сначала самые подозрительные места
- Группировка по файлам (если несколько мест в одном файле)
- Связь с описанием проблемы из задачи

---

## 🚨 ВАЛИДАЦИЯ И ОШИБКИ

### Если формат не соответствует

**ОШИБКА:**
```
ERROR: Summary от архитектора не соответствует формату. Требуется: ФАЙЛ:КООРДИНАТА:ФУНКЦИЯ:ПРИЧИНА
```

**ДЕЙСТВИЯ:**
- ❌ Запроси повтор через `new_task` с уточнением формата
- ❌ НЕ продолжай к следующей фазе без валидного summary
- ❌ НЕ пытайся "исправить" summary самостоятельно

### Частые ошибки

1. **Отсутствие цитат в REASON:**
   - ❌ `ПРИЧИНА:Возможная проблема в обработке данных.`
   - ✅ `ПРИЧИНА:Функция обрабатывает данные без проверки:
```python
result = process(data)
```
Нет проверки на None.`

2. **Неполные пути:**
   - ❌ `ФАЙЛ:mesh_extractor.py`
   - ✅ `ФАЙЛ:src/mesh_extractor.py`

3. **Неточные имена функций:**
   - ❌ `ФУНКЦИЯ:функция извлечения`
   - ✅ `ФУНКЦИЯ:extract_meshes`

4. **Отсутствие разделителей:**
   - ❌ Две записи подряд без `---`
   - ✅ Записи разделены `---`

---

## 🚨 ШТРАФЫ

**ШТРАФ:** Предоставление summary не в формате ФАЙЛ:КООРДИНАТА:ФУНКЦИЯ:ПРИЧИНА = +15 баллов (CRITICAL FAILURE)

**ШТРАФ:** Предоставление REASON без цитат кода/логов = +10 баллов (CRITICAL FAILURE)

**ШТРАФ:** Отсутствие обязательных полей (ФАЙЛ, КООРДИНАТА, ФУНКЦИЯ, ПРИЧИНА) = +10 баллов за каждое отсутствующее поле

**ШТРАФ:** Невалидные координаты (несуществующие файлы/строки) = +5 баллов

---

## 📚 СВЯЗАННЫЕ МОДУЛИ

- `arch-01-reconnaissance.md` - Детальный протокол разведки (использование формата)
- `roo-02-delegate-recon.md` - Phase 0.2: Делегирование разведки (контекст)
- `roo-03-receive-architect.md` - Phase 0.3: Приемка summary от архитектора (валидация)
- `00-formats-validator.md` - Валидация форматов summary (общие правила)

---

**Последнее обновление:** 2026-01-23
