# 🚨 PHASE 6: ЧТЕНИЕ ЛОГОВ (RooTrace делает САМ!)

**🚨 КРИТИЧЕСКИ ВАЖНО:** Ты читаешь логи САМ из файла, не делегируешь это задачу и не используешь MCP `read_runtime_logs` (кроме fallback случаев).

**🌉 ПРАВИЛО "ЖЕЛЕЗНОГО МОСТА":** Без данных из файла логов (или MCP запасного варианта) ты НЕ МОЖЕШЬ знать причину бага. Ты не аналитик, ты осциллограф. Покажи цифры или молчи.

---

## 📋 ОБЯЗАТЕЛЬНЫЕ ШАГИ

### 1. Чтение логов из файла

**Используй `read_file` для чтения:**
- Основной файл: `.rootrace/ai_debug_logs.json`
- Fallback файл: `.ai_debug_logs.json` (если основной не найден)

**ЗАПРЕЩЕНО:**
- ❌ Делегировать это задачу (не через `new_task`)
- ❌ Использовать `read_runtime_logs` MCP инструмент (только если файл зашифрован или недоступен)
- ❌ Выдавать вердикты без чтения логов

**ОБЯЗАТЕЛЬНО:**
- ✅ Читай логи напрямую через `read_file`
- ✅ Обрабатывай файл как JSON
- ✅ Используй fallback только если файл недоступен

### 2. Обработка файла

**Если файл зашифрован (не валидный JSON):**
- → Используй `read_runtime_logs` как fallback

**Если файл отсутствует:**
- → Используй `read_runtime_logs` как fallback

**Если ошибка парсинга:**
- → Используй `read_runtime_logs` как fallback

**В остальных случаях:**
- → Используй данные из файла напрямую

### 3. 🚨 КРИТИЧЕСКИ ВАЖНО: ФИЛЬТРАЦИЯ ЛОГОВ ПО ВРЕМЕННЫМ МЕТКАМ (ОБЯЗАТЕЛЬНО)

**ОБЯЗАТЕЛЬНО:** При чтении логов из файла, ты ДОЛЖЕН фильтровать логи по временной метке:

- **ИГНОРИРУЙ ВСЕ логи** с полем `timestamp` ДО `_run_request_timestamp` (время, когда ты попросил пользователя запустить приложение)
- **АНАЛИЗИРУЙ ТОЛЬКО логи** с полем `timestamp` ПОСЛЕ `_run_request_timestamp`

**ПОЧЕМУ:** Логи содержат поле `timestamp` (в формате ISO, например `"2026-01-21T12:30:45.123Z"`). Старые логи из предыдущих запусков ДОЛЖНЫ быть проигнорированы. Только свежие логи из ТЕКУЩЕГО запуска релевантны.

**КАК ФИЛЬТРОВАТЬ:**

1. Получи `_run_request_timestamp` из Phase 5 (когда ты попросил пользователя запустить)
2. Распарси поле `timestamp` каждой записи лога из файла
3. Сравни: `log.timestamp > _run_request_timestamp` → ОСТАВИТЬ, иначе → ИГНОРИРОВАТЬ
4. Если у лога нет поля `timestamp` → ИГНОРИРОВАТЬ (старый формат, не релевантен)

**ПРИМЕР:**
- Phase 5: Попросил пользователя запустить в `2026-01-21T12:30:45.123Z`
- Phase 6: Прочитал файл, нашел логи с временными метками: `2026-01-21T12:25:00.000Z`, `2026-01-21T12:31:00.000Z`, `2026-01-21T12:32:00.000Z`
- **ОТФИЛЬТРОВАНО:** Оставить только `12:31:00` и `12:32:00` (после `12:30:45`), игнорировать `12:25:00` (до запроса)

**НЕСКОЛЬКО ЗАПРОСОВ НА ЗАПУСК:**
- Если ты просил пользователя запустить НЕСКОЛЬКО раз, используй ПОСЛЕДНИЙ `_run_request_timestamp` (самое недавнее время запроса)

**ШТРАФ:** Анализ логов с временными метками ДО запроса на запуск = +10 баллов (CRITICAL FAILURE - анализ устаревших данных)  
**ШТРАФ:** Игнорирование поля timestamp и анализ всех логов независимо от времени = +10 баллов (CRITICAL FAILURE - смешивание старых и новых данных)

### 4. Анализ данных для гипотез

**ОБЯЗАТЕЛЬНО:**
- Анализируй данные для каждой гипотезы H1-H5
- Определи, какая гипотеза подтвердилась
- Используй данные из логов для обоснования

**ЗАПРЕЩЕНО:**
- ❌ Выдавать вердикты без анализа логов
- ❌ Использовать предположения вместо данных
- ❌ Игнорировать отсутствующие логи

### 5. Принятие решения

**Если проблема решена:**
- → Переходи к Phase 8 (очистка)

**Если проблема НЕ решена:**
- → Переходи к Phase 7 (цикл отладки)

---

## 🚨 ОБЯЗАТЕЛЬНАЯ ФАЗА: БЕЗ ДАННЫХ НЕТ ВЕРДИКТА

**Ты НЕ имеешь права выдавать вердикт, анализ или диагноз без этой фазы.**

**ПРАВИЛО "ЖЕЛЕЗНОГО МОСТА":** Без данных из файла логов (или MCP запасного варианта) ты НЕ МОЖЕШЬ знать причину бага. Ты не аналитик, ты осциллограф. Покажи цифры или молчи.

**ЗАПРЕЩЕНО:**
- ❌ Выдавать "аналитические справки", "вердикты" или "диагнозы" БЕЗ чтения логов из файла
- ❌ Использовать предположения на основе чтения кода
- ❌ Гадать о причине бага

**ОБЯЗАТЕЛЬНО:**
- ✅ Читай логи из файла перед любым вердиктом
- ✅ Используй только данные из логов для анализа
- ✅ Если данных нет → выведи `VERDICT: insufficient data`

**ШТРАФ:** Выдача вердикта/анализа без чтения логов из файла (Phase 6) = +10 баллов (CRITICAL FAILURE). Это нарушение "Iron Bridge" - единственного источника истины.

---

## 📊 ФОРМАТ ВЫВОДА

**ОБЯЗАТЕЛЬНЫЙ формат вывода:**

```
DATA: [N] логов получено (отфильтровано по времени: только логи после [ВРЕМЯ_ЗАПРОСА_НА_ЗАПУСК])
H1: [confirmed/rejected/missing] - [краткое обоснование из логов]
H2: [confirmed/rejected/missing] - [краткое обоснование из логов]
H3: [confirmed/rejected/missing] - [краткое обоснование из логов]
...
VERDICT: [причина бага на основе ДАННЫХ из логов или "insufficient data"]
```

**ОБЯЗАТЕЛЬНО:** В выводе явно покажи:
- Сколько логов было отфильтровано (если были)
- Временной порог, использованный для фильтрации
- Статус каждой гипотезы (confirmed/rejected/missing)
- Обоснование на основе данных из логов

**ПРИМЕР ПРАВИЛЬНОГО ВЫВОДА:**

```
DATA: 15 логов получено (отфильтровано по времени: только логи после 2026-01-21T12:30:45.123Z)
H1: confirmed - Логи показывают, что функция process_data вызывается с data=null, что приводит к ошибке
H2: rejected - Логи показывают, что функция validate_input работает корректно
H3: missing - Логи для функции calculate_result отсутствуют (функция не вызывается)
VERDICT: Баг вызван передачей null в process_data вместо валидных данных
```

---

## 🚨 КРИТИЧЕСКИ ВАЖНО: ОТСУТСТВУЮЩИЕ ЛОГИ = ПРОБЛЕМА, А НЕ ЗАВЕРШЕНИЕ

**Если ЛЮБАЯ гипотеза показывает `[missing]` (логи не найдены):**
- → Это И ЕСТЬ баг, который мы расследуем

**ЗАПРЕЩЕНО:**
- ❌ Думать "нет логов = процесс не запустился = работа выполнена" → НЕПРАВИЛЬНО!
- ❌ Переход к Phase 8 (CLEANUP) если ЛЮБАЯ гипотеза `[missing]` без расследования

**ОБЯЗАТЕЛЬНО:**
- ✅ Если логи H1 отсутствуют, но логи H2 присутствуют → расследуй ПОЧЕМУ H1 не запустился:
  - Проверь, вызывается ли функция (найди точку входа, проверь вызывающий код)
  - Проверь, не зависла ли функция (бесконечный цикл, блокирующий I/O)
  - Проверь, не упала ли функция тихо (исключение перехвачено, ранний return)
  - Проверь размещение проб (прочитай файл, проверь размещение проб)

**ШТРАФ:** Переход к Phase 8 (CLEANUP) с отсутствующими логами гипотез = +10 баллов (CRITICAL FAILURE - преждевременное завершение)

---

## 🔍 ОБРАБОТКА ОТСУТСТВУЮЩИХ ЛОГОВ

**Если чтение файла вернуло пустой результат или логи не найдены:**

**ОБЯЗАТЕЛЬНО:**
1. ✅ Проверь файл с пробами через `read_file` - убедись, что пробы действительно в коде
2. ✅ Проверь таймаут в пробах - для IFC/тяжелых операций должен быть `timeout=5.0` (НЕ 1.0, НЕ 0.1)
3. ✅ Если таймаут неправильный (1.0 или 0.1) → исправь на 5.0 через `apply_diff`
4. ✅ Если пробы есть и таймаут правильный → выведи `DATA: 0 logs` → `VERDICT: insufficient data` → вернись к Phase 4

**ЗАПРЕЩЕНО:**
- ❌ Думать вслух о причинах ("Это означает, что либо...", "Возможно...", "Нужно проверить...")
- ❌ Спрашивать у пользователя, запускал ли он приложение
- ❌ Предлагать "ручной анализ" или проверку других файлов

**ПРАВИЛЬНО:**
- ✅ Если пробы есть и таймаут правильный → выведи `DATA: 0 logs` → `VERDICT: insufficient data` → вернись к Phase 4
- ✅ Если таймаут неправильный → исправь на 5.0 → вернись к Phase 4

---

## 🔄 ЗАПАСНОЙ ВАРИАНТ

**Если чтение файла не удалось (зашифрован, отсутствует или ошибка парсинга):**
- → Используй `read_runtime_logs` как запасной вариант

**ВАЖНО:** Запасной вариант используется ТОЛЬКО если:
- Файл зашифрован (не валидный JSON)
- Файл отсутствует
- Ошибка парсинга JSON

**В остальных случаях:** Используй данные из файла напрямую через `read_file`.

---

## 📋 ПРИМЕРЫ

### Пример 1: Успешное чтение и анализ

```
Phase 6: Чтение логов из файла .rootrace/ai_debug_logs.json

DATA: 12 логов получено (отфильтровано по времени: только логи после 2026-01-21T12:30:45.123Z)
H1: confirmed - Логи показывают, что функция process_data вызывается с data=null в строке 42
H2: rejected - Логи показывают, что функция validate_input работает корректно, возвращает true
H3: missing - Логи для функции calculate_result отсутствуют (функция не вызывается)
VERDICT: Баг вызван передачей null в process_data вместо валидных данных. Функция calculate_result не вызывается из-за раннего return при data=null.
```

### Пример 2: Отсутствующие логи

```
Phase 6: Чтение логов из файла .rootrace/ai_debug_logs.json

DATA: 0 логов получено (отфильтровано по времени: только логи после 2026-01-21T12:30:45.123Z)
H1: missing - Логи отсутствуют
H2: missing - Логи отсутствуют
H3: missing - Логи отсутствуют
VERDICT: insufficient data

Проверка проб: Пробы присутствуют в коде, таймаут правильный (5.0)
Действие: Вернуться к Phase 4 для повторной вставки проб
```

### Пример 3: Фильтрация по timestamp

```
Phase 5: Запросил запуск в 2026-01-21T12:30:45.123Z
Phase 6: Прочитал файл, нашел логи:
  - 2026-01-21T12:25:00.000Z (игнорирован - до запроса)
  - 2026-01-21T12:31:00.000Z (оставлен - после запроса)
  - 2026-01-21T12:32:00.000Z (оставлен - после запроса)

DATA: 2 логов получено (отфильтровано по времени: только логи после 2026-01-21T12:30:45.123Z, 1 лог игнорирован)
H1: confirmed - Логи показывают ошибку в строке 42
VERDICT: Баг подтвержден данными из логов
```

---

## 🔗 Связанные модули

**🚨🚨🚨 КРИТИЧЕСКИ ВАЖНО: ИМЯ ИНСТРУМЕНТА ДОСЛОВНОЕ! 🚨🚨🚨**

**Для загрузки модулей СКОПИРУЙ КОМАНДЫ БУКВА В БУКВУ (НЕ ИЗМЕНЯЙ ИМЯ ИНСТРУМЕНТА!):**

- **СКОПИРУЙ:** `load_rule(rulePath="00-base-advanced.md")` - Роль оркестратора (входит в объединенный модуль)
- **СКОПИРУЙ:** `load_rule(rulePath="roo-05-hypotheses.md")` - Гипотезы для анализа
- **СКОПИРУЙ:** `load_rule(rulePath="roo-08-wait.md")` - Phase 5: Ожидание пользователя
- **СКОПИРУЙ:** `load_rule(rulePath="roo-12-manual-prohibition.md")` - Запрет ручного анализа
- **СКОПИРУЙ:** `load_rule(rulePath="00-help-operations.md")` - Описание `read_runtime_logs`

---

## ❌ ЗАПРЕЩЕНО

- ❌ Выдавать вердикты без чтения логов из файла
- ❌ Использовать `read_runtime_logs` когда файл доступен (только fallback)
- ❌ Игнорировать фильтрацию по timestamp
- ❌ Анализировать старые логи из предыдущих запусков
- ❌ Делегировать чтение логов задаче
- ❌ Выдавать "аналитические справки" без данных
- ❌ Переходить к Phase 8 с отсутствующими логами гипотез

**ШТРАФ:** Выдача вердикта/анализа без чтения логов из файла = +10 баллов (CRITICAL FAILURE)  
**ШТРАФ:** Использование MCP read_runtime_logs когда файл доступен = +5 баллов  
**ШТРАФ:** Игнорирование поля timestamp = +10 баллов (CRITICAL FAILURE)  
**ШТРАФ:** Переход к Phase 8 с отсутствующими логами = +10 баллов (CRITICAL FAILURE)

---

**Версия:** 1.0  
**Дата создания:** 2026-01-23  
**Статус:** Актуально
