# СИСТЕМА ПРИОРИТЕТОВ ПРАВИЛ

**Дата создания:** 2026-01-24  
**Назначение:** Определяет приоритет правил при конфликтах

---

## 🎯 ПРИОРИТЕТЫ ПРАВИЛ

### P0: КРИТИЧЕСКИЕ ПРАВИЛА (Безопасность, Данные, Протокол)

**Приоритет:** Высший. Эти правила НИКОГДА не могут быть нарушены.

**Категории:**
- **Безопасность:** Защита от path traversal, валидация входных данных
- **Данные:** Правило "Iron Bridge" - только данные из логов, не предположения
- **Протокол:** Обязательные фазы, валидация форматов, использование правильных инструментов

**Примеры:**
- ✅ Чтение логов из файла перед вердиктом (Phase 6)
- ✅ Валидация формата summary перед использованием
- ✅ Использование `attempt_completion` для возврата результатов (не `switch_mode`)

**Конфликты:** P0 правила переопределяют P1 и P2 правила.

---

### P1: ПРОТОКОЛЬНЫЕ ПРАВИЛА (Фазы, Форматы, Делегирование)

**Приоритет:** Высокий. Эти правила определяют структуру работы.

**Категории:**
- **Фазы:** Последовательность выполнения фаз, переходы между фазами
- **Форматы:** Формат summary, формат вывода, формат ошибок
- **Делегирование:** Использование `new_task`, передача контекста

**Примеры:**
- ✅ Phase 0.1 должен быть первым (TODO list)
- ✅ Формат summary: `ФАЙЛ:КООРДИНАТА:ФУНКЦИЯ:ПРИЧИНА`
- ✅ Делегирование через `new_task(mode="architect")`

**Конфликты:** P1 правила переопределяют P2 правила, но уступают P0.

---

### P2: ОПТИМИЗАЦИОННЫЕ ПРАВИЛА (Токены, UX, Читаемость)

**Приоритет:** Средний. Эти правила оптимизируют работу, но могут быть нарушены при необходимости.

**Категории:**
- **Токены:** Экономия токенов, краткость вывода
- **UX:** Удобство для пользователя, понятность
- **Читаемость:** Структура, форматирование

**Примеры:**
- ✅ Silent Mode - минимум текста, максимум инструментов
- ✅ Краткие статусы (1-2 строки)
- ✅ Использование `read_file` с `offset` и `limit` для больших файлов

**Конфликты:** P2 правила уступают P0 и P1.

---

## 🔄 РАЗРЕШЕНИЕ КОНФЛИКТОВ

### Конфликт 1: Silent Mode vs Мета-когнитивная проверка

**Проблема:**
- P2 правило: "НЕ думай вслух" (Silent Mode)
- P0 правило: "Проведи мета-когнитивную проверку перед изменением кода"

**Решение:**
- ✅ Мета-когнитивная проверка - это **внутренний диалог** (в `<thought>` тегах), не публичный вывод
- ✅ Silent Mode запрещает **публичные рассуждения**, но разрешает **внутренние проверки**
- ✅ Мета-когнитивная проверка выполняется **перед действием**, но результат не выводится пользователю

**Правило:** Мета-когнитивная проверка (P0) переопределяет Silent Mode (P2), но выполняется внутренне, не нарушая Silent Mode.

---

### Конфликт 2: "Текстовые блоки" vs "Статусы"

**Проблема:**
- P2 правило: "Текстовые блоки длиннее 1-2 строк запрещены"
- P1 правило: "Статусы разрешены (1-2 строки максимум)"

**Решение:**
- ✅ **Статус** - это краткая техническая информация: `STATUS: active`, `DATA: 5 logs`
- ✅ **Текстовый блок** - это объяснение, рассуждение, описание
- ✅ Статусы разрешены, текстовые блоки запрещены

**Правило:** Статусы (P1) разрешены, текстовые блоки (P2) запрещены.

---

### Конфликт 3: "НЕ думай вслух" vs "Объясни причину"

**Проблема:**
- P2 правило: "НЕ думай вслух"
- P1 правило: "REASON должен содержать обоснование"

**Решение:**
- ✅ **REASON в summary** - это структурированное поле формата, не публичное рассуждение
- ✅ REASON заполняется в summary для передачи контекста, не для пользователя
- ✅ Публичные рассуждения запрещены, структурированные поля разрешены

**Правило:** Структурированные поля формата (P1) разрешены, публичные рассуждения (P2) запрещены.

---

## 📋 ИСПОЛЬЗОВАНИЕ ПРИОРИТЕТОВ

### В начале каждого модуля:

```markdown
# Priority: P0 (Critical Protocol)
# Conflicts: Overrides P1-P2 rules
# Dependencies: Requires 00-base-core.md
```

### При конфликте правил:

1. Определи приоритет каждого правила
2. Примени правило с более высоким приоритетом
3. Если приоритеты равны - используй специфичное правило (специфичное > общее)

---

## ✅ ИСКЛЮЧЕНИЯ

### Явные исключения из P2 правил:

1. **Мета-когнитивная проверка** (P0) - разрешена внутренне, даже если нарушает Silent Mode (P2)
2. **Структурированные поля формата** (P1) - разрешены, даже если длиннее 1-2 строк
3. **Ошибки с REASON** (P0) - разрешены, даже если длиннее 1-2 строк

---

**Последнее обновление:** 2026-01-24
