# 🔧 PHASE 1.1: ДЕЛЕГИРОВАНИЕ ВСТАВКИ ПРОБ

**🚨 КРИТИЧЕСКИ ВАЖНО:** Ты НЕ вставляешь пробы сам. Ты делегируешь code mode.

**ВЫПОЛНЯЕТСЯ:** ПОСЛЕ Phase 1 (Гипотезы) и стратегий [DEBUG-STRATEGIST] и [SRE-SHIELD], ПЕРЕД Phase 1.2 (получение summary от code mode).

---

## 🎯 НАЗНАЧЕНИЕ

Phase 1.1 - это делегирование вставки проб code mode для инструментации кода согласно гипотезам. Цель - получить инструментированный код с реальными рабочими пробами, которые отправляют данные на сервер RooTrace.

---

## 📋 ОБЯЗАТЕЛЬНЫЕ ШАГИ

### 1. Подготовка инструкций

**ОБЯЗАТЕЛЬНО:** Собери всю необходимую информацию:

- Список гипотез (H1-H5)
- Список координат из summary архитектора (ФАЙЛ:КООРДИНАТА:ФУНКЦИЯ:ПРИЧИНА)
- FINAL_HOST и ACTUAL_PORT из Phase 2 (Обнаружение сети)
- Стратегия [DEBUG-STRATEGIST] и [SRE-SHIELD] (если выполнены)

---

### 2. Вызов new_task

**ИНСТРУМЕНТ:** `new_task(mode="code", message="...")`

**ОБЯЗАТЕЛЬНЫЙ ФОРМАТ СООБЩЕНИЯ:**

```javascript
new_task({
  mode: "code",
  message: `
Вставь пробы RooTrace по списку координат.

КОНТЕКСТ:
- Гипотезы: [H1, H2, H3...]
- Координаты: [файл:строка:функция из summary архитектора]
- Сеть: http://${FINAL_HOST}:${ACTUAL_PORT}/

🚨 КРИТИЧЕСКИ ВАЖНО: ПРОБЫ ДОЛЖНЫ БЫТЬ РЕАЛЬНЫМ КОДОМ, КОТОРЫЙ ОТПРАВЛЯЕТ HTTP-ЗАПРОСЫ НА СЕРВЕР!

СПИСОК ЗАДАЧ ДЛЯ ВСТАВКИ ПРОБ:
- H1: [файл]:[строка] - [описание гипотезы]
- H2: [файл]:[строка] - [описание гипотезы]
- H3: [файл]:[строка] - [описание гипотезы]
- H4: [файл]:[строка] - [описание гипотезы]
- H5: [файл]:[строка] - [описание гипотезы]

🚨 ПЕРВОЕ ДЕЙСТВИЕ: Создай TODO список через \`update_todo_list\` на основе списка задач выше. Это поможет не пропустить ни одной вставки!

🚨 ВТОРОЕ ДЕЙСТВИЕ: Проверь FINAL_HOST и ACTUAL_PORT!
- Если FINAL_HOST и ACTUAL_PORT переданы в КОНТЕКСТЕ выше → используй их
- Если НЕ переданы → ОБЯЗАТЕЛЬНО проверь окружение:
  1. Проверь наличие Docker файлов (Dockerfile, docker-compose.yml) через \`read_file\` или \`list_files\`
  2. Если Docker файлы найдены → используй \`host.docker.internal\` как FINAL_HOST
  3. Если Docker файлов нет → используй \`localhost\` как FINAL_HOST
  4. Для ACTUAL_PORT: прочитай \`.rootrace/debug_port\` или \`.debug_port\` через \`read_file\`, если не найден → используй \`51234\`
  5. **ВАЖНО:** Если работаешь в Docker контейнере, пробы ДОЛЖНЫ использовать \`host.docker.internal\`, а не \`localhost\`!

ЗАДАЧА:
1. **СНАЧАЛА:** Создай TODO список через \`update_todo_list\` с задачами из списка выше (H1, H2, H3, H4, H5)
2. **ПОТОМ:** Проверь и определи FINAL_HOST и ACTUAL_PORT (см. выше)
3. Для каждой гипотезы H1-H5 вставь РЕАЛЬНЫЙ КОД ПРОБЫ в указанное место
3. После каждой вставки обновляй TODO список - отмечай выполненную задачу
4. Используй apply_diff (Block Rewrite) - НЕ inject_probes
5. **ОБЯЗАТЕЛЬНО:** После КАЖДОЙ вставки проверь диагностику:
   - **ПРЕДПОЧТИТЕЛЬНО:** Используй \`get_problems(filePath="путь/к/файлу")\` для проверки ошибок в редактируемом файле
   - **FALLBACK:** Используй \`@problems\` mention если MCP инструмент недоступен
4. Если диагностика показывает ошибки - исправь их немедленно, затем проверь диагностику снова
5. Создай .patch файл только после того, как диагностика не показывает ошибок
6. Используй UUID-маркеры для проб

ОБЯЗАТЕЛЬНЫЕ ТРЕБОВАНИЯ К КОДУ ПРОБЫ:
- ❌ ЗАПРЕЩЕНО: Вставлять только комментарии типа "// ROO-TRACE-PROBE H1: ..." - это НЕПРАВИЛЬНО!
- ✅ ОБЯЗАТЕЛЬНО: Вставь РЕАЛЬНЫЙ РАБОЧИЙ КОД, который отправляет HTTP POST запрос на http://${FINAL_HOST}:${ACTUAL_PORT}/
- ✅ ОБЯЗАТЕЛЬНО: Проба должна реально выполняться и отправлять данные на сервер, а не быть просто комментарием

ПРИМЕРЫ КОДА ПРОБ ПО ЯЗЫКАМ:

Для Python файлов:
\`\`\`python
# RooTrace [id: UUID] H1: [описание гипотезы]
try:
    import http.client, json, socket
    conn = http.client.HTTPConnection("${FINAL_HOST}", ${ACTUAL_PORT})
    conn.sock = socket.create_connection(("${FINAL_HOST}", ${ACTUAL_PORT}), timeout=5.0)
    conn.request("POST", "/", json.dumps({'hypothesisId': 'H1', 'message': '[имя функции] start', 'state': {'key': value}}), {'Content-Type': 'application/json'})
    conn.getresponse()
    conn.close()
except:
    pass
# RooTrace [id: UUID]: end
\`\`\`

Для Go файлов:
\`\`\`go
// RooTrace [id: UUID] H1: [описание гипотезы]
go func() {
    serverURL := fmt.Sprintf("http://%s:%d/", "${FINAL_HOST}", ${ACTUAL_PORT})
    jsonData, _ := json.Marshal(map[string]interface{}{
        "hypothesisId": "H1",
        "message": "[имя функции] start",
        "state": map[string]interface{}{"key": value},
    })
    req, _ := http.NewRequest("POST", serverURL, bytes.NewBuffer(jsonData))
    req.Header.Set("Content-Type", "application/json")
    client := &http.Client{Timeout: 5 * time.Second}
    client.Do(req)
}()
// RooTrace [id: UUID]: end
\`\`\`

Для JavaScript/TypeScript файлов:
\`\`\`javascript
// RooTrace [id: UUID] H1: [описание гипотезы]
try {
    fetch(\`http://${FINAL_HOST}:${ACTUAL_PORT}/\`, {
        method: 'POST',
        headers: {'Content-Type': 'application/json'},
        body: JSON.stringify({hypothesisId: 'H1', message: '[имя функции] start', state: {key: value}})
    }).catch(() => {});
} catch(e) {}
// RooTrace [id: UUID]: end
\`\`\`

КРИТИЧЕСКИЕ ПРАВИЛА:
- Проба ДОЛЖНА отправлять HTTP POST запрос с JSON payload содержащим:
  * hypothesisId: 'H1', 'H2', и т.д.
  * message: описательное сообщение о том, что измеряется
  * state: объект с релевантными переменными/значениями из контекста функции
- Проба ДОЛЖНА быть обернута в try/except (Python) или go func() (Go), чтобы не сломать выполнение
- Проба ДОЛЖНА использовать timeout (5.0 для Python, 5*time.Second для Go) для IFC/тяжелых операций
- Проба ДОЛЖНА использовать значения FINAL_HOST и ACTUAL_PORT (НЕ захардкоженный localhost:51234)
- Проба ДОЛЖНА иметь UUID маркер: [id: UUID] (сгенерируй уникальный UUID для каждой пробы)
- Проба ДОЛЖНА иметь конечный маркер: [id: UUID]: end

ОБЯЗАТЕЛЬНО:
- Backup перед первой вставкой (git commit ИЛИ .bak)
- Проверка линтера после КАЖДОЙ вставки используя @problems mention
- Создание .patch после каждой успешной проверки линтера
- Использование FINAL_HOST и ACTUAL_PORT (НЕ localhost:51234)
- Вставка РЕАЛЬНОГО РАБОЧЕГО КОДА, НЕ просто комментариев!

ПРОТОКОЛ ПРОВЕРКИ ЛИНТЕРА (ОБЯЗАТЕЛЬНО):
- После КАЖДОЙ вставки пробы ты ДОЛЖЕН проверить ошибки используя диагностику
- **ПРЕДПОЧТИТЕЛЬНЫЙ МЕТОД:** Используй MCP инструмент \`get_problems\` для получения диагностики напрямую
  - Вызови \`get_problems()\` для всех диагностик workspace
  - Вызови \`get_problems(filePath="путь/к/файлу")\` для конкретного файла
  - Возвращает ошибки и предупреждения с severity, message, range, source и code
- **FALLBACK МЕТОД:** Используй \`@problems\` mention если MCP инструмент недоступен
  - @problems интегрируется с VSCode Problems panel и показывает все ошибки и предупреждения рабочей области
  - @problems автоматически захватывает диагностику от language servers, линтеров и других диагностических провайдеров
- **DIAGNOSTICS INTEGRATION (ИНТЕГРАЦИЯ С DIAGNOSTICS):**
  - **Автоматическое обнаружение ошибок:** Roo Code автоматически захватывает диагностику перед редактированием, ждет обновления после редактирования и обнаруживает новые проблемы, введенные изменениями. Сообщаются только новые ошибки (не существовавшие ранее).
  - **Уровни серьезности (Severity Levels):**
    - **Error (0):** Синтаксические ошибки, ошибки типов, критические проблемы. Включаются в @problems и автоматическое обнаружение.
    - **Warning (1):** Проблемы качества кода, устаревшие функции, нарушения стиля. Включаются в @problems, НЕ включаются в автоматическое обнаружение (чтобы не отвлекать).
    - **Information (2):** Предложения, подсказки, информационные сообщения. НЕ включаются в @problems и автоматическое обнаружение.
    - **Hint (3):** Незначительные предложения, возможности рефакторинга. НЕ включаются в @problems и автоматическое обнаружение.
  - **Smart Severity Filtering:** Разные функции используют разные фильтры серьезности:
    - **Workspace Problems (@problems):** Показывает ошибки и предупреждения для полной картины здоровья кода
    - **Автоматическое обнаружение:** Показывает только ошибки, чтобы не прерывать workflow не критичными проблемами
  - **Интеграция с Code Actions:** Когда диагностика существует на позиции курсора, появляется действие "Fix with Roo Code" в quick fix меню, включающее детали диагностики в запрос на исправление.
  - **Best Practices:**
    - Используй @problems для контекста: при отладке всегда включай @problems для полной видимости текущих проблем
    - Исправляй ошибки сначала: фокусируйся на исправлении ошибок перед предупреждениями, так как ошибки обычно предотвращают выполнение кода
    - Используй Code Actions: используй quick fix меню для целевых исправлений конкретных диагностик
    - Мониторь автоматическое обнаружение: обращай внимание на новые ошибки, сообщаемые после редактирования, чтобы поймать проблемы рано
- Это БОЛЕЕ НАДЕЖНО, чем ручной запуск команд линтера - используется встроенная диагностическая система VSCode
- Если диагностика показывает ошибки в файле, который ты только что редактировал:
  * ОСТАНОВИСЬ немедленно - НЕ переходи к следующей пробе
  * Исправь ошибки используя apply_diff
  * Проверь диагностику снова, пока ошибки не исчезнут
  * Только после этого переходи к следующей вставке пробы
- Если диагностика не показывает ошибок → переходи к созданию .patch файла
- НЕ пропускай проверку линтера - она обязательна после КАЖДОЙ вставки
- НЕ используй ручные команды линтера (типа \`pylint\`, \`golint\` и т.д.) - используй диагностику вместо этого

ПО ЗАВЕРШЕНИИ:
- **ОБЯЗАТЕЛЬНО:** Используй инструмент \`attempt_completion\` с параметром \`result\`
- **ФОРМАТ result:** Краткое, но полное резюме выполненной работы:
  \`\`\`
  Вставленные пробы:
  - H1: файл:строка (описание гипотезы)
  - H2: файл:строка (описание гипотезы)
  - H3: файл:строка (описание гипотезы)
  ...
  
  Проверка линтера:
  - файл1.py: OK (нет ошибок)
  - файл2.py: OK (нет ошибок)
  ...
  
  Созданные .patch файлы:
  - файл1.patch
  - файл2.patch
  ...
  \`\`\`
- **ТРЕБОВАНИЯ:**
  - Только факты о выполненной работе - НИЧЕГО ДРУГОГО
  - Без лишних приветствий или filler-текста
  - Профессиональный, прямой тон
  - Легко сканируется и понимается
- **ВАЖНО:** Твой контекст будет уничтожен после завершения. Оркестратор получит только summary через \`attempt_completion\`.
  `
})
```

**ВАЖНО:** 
- Включи весь необходимый контекст в `message`
- **🚨 ОБЯЗАТЕЛЬНО:** Укажи четко FINAL_HOST и ACTUAL_PORT в секции КОНТЕКСТ (НЕ хардкодить!)
- **🚨 ОБЯЗАТЕЛЬНО:** Если Phase 2 (Обнаружение сети) еще не выполнен, ОБЯЗАТЕЛЬНО выполни его ПЕРЕД делегированием вставки проб!
- **ОБЯЗАТЕЛЬНО:** Включи СПИСОК ЗАДАЧ ДЛЯ ВСТАВКИ ПРОБ с конкретными координатами (H1: файл:строка, H2: файл:строка и т.д.)
- Напомни, что code mode должен создать TODO список в начале работы на основе списка задач
- Напомни, что code mode должен проверить FINAL_HOST и ACTUAL_PORT, если они не переданы
- Напомни, что code mode должен использовать `attempt_completion` для возврата summary

**ШТРАФ:** Делегирование вставки проб БЕЗ выполнения Phase 2 (Обнаружение сети) = +20 баллов (CRITICAL FAILURE)
**ШТРАФ:** Передача хардкоженного localhost:51234 вместо FINAL_HOST и ACTUAL_PORT = +15 баллов (CRITICAL FAILURE)

---

### 3. Ожидание завершения

**ОБЯЗАТЕЛЬНО:**
- Подзадача code mode работает в изолированном контексте
- Ты получаешь только summary через `attempt_completion`
- Обнови todo: `[DONE] Вставка проб делегирована`

**ВАЖНО:** Не пытайся вставлять пробы сам - это нарушение протокола!

---

## 🚨 ШТРАФЫ

**ШТРАФ:** Выполнение вставки проб напрямую вместо делегирования = +15 баллов (CRITICAL FAILURE)

**ШТРАФ:** Пропуск Phase 1.1 (делегирование вставки проб) = +20 баллов (CRITICAL FAILURE)

**ШТРАФ:** Передача хардкоженного localhost:51234 вместо FINAL_HOST и ACTUAL_PORT = +15 баллов (CRITICAL FAILURE)

**ШТРАФ:** Неполный контекст в message для code mode = +5 баллов

---

## 📚 СВЯЗАННЫЕ МОДУЛИ

**🚨🚨🚨 КРИТИЧЕСКИ ВАЖНО: ИМЯ ИНСТРУМЕНТА ДОСЛОВНОЕ! 🚨🚨🚨**

**Для загрузки модулей СКОПИРУЙ КОМАНДЫ БУКВА В БУКВУ (НЕ ИЗМЕНЯЙ ИМЯ ИНСТРУМЕНТА!):**

- **СКОПИРУЙ:** `load_rule(rulePath="roo-05-hypotheses.md")` - Phase 1: Формулирование гипотез
- **СКОПИРУЙ:** `load_rule(rulePath="roo-06-network.md")` - Phase 2: Обнаружение сети
- **СКОПИРУЙ:** `load_rule(rulePath="code-00-role.md")` - Роль кодера
- **СКОПИРУЙ:** `load_rule(rulePath="code-04-block-rewrite.md")` - Block Rewrite протокол
- **СКОПИРУЙ:** `load_rule(rulePath="code-03-linter-protocol.md")` - Протокол проверки линтера
- **СКОПИРУЙ:** `load_rule(rulePath="code-05-probe-examples.md")` - Примеры проб
- **СКОПИРУЙ:** `load_rule(rulePath="code-06-probe-spec.md")` - Спецификация проб
- **СКОПИРУЙ:** `load_rule(rulePath="code-08-python-indent.md")` - Python индентация
- **СКОПИРУЙ:** `load_rule(rulePath="00-help-operations.md")` - Справочник по `new_task`

---

**Последнее обновление:** 2026-01-23
