# 📥 PHASE 1.6: ПРИЕМКА СТРАТЕГИИ ОТ АРХИТЕКТОРА (ТОЧКИ ИНЪЕКЦИИ ПРОБ)

**🚨 КРИТИЧЕСКИ ВАЖНО:** Ты получаешь только summary, не весь контекст подзадачи.

**ВЫПОЛНЯЕТСЯ:** ПОСЛЕ Phase 1.5 (делегирование стратегии инъекции проб), ПЕРЕД Phase 1.1 (делегирование вставки проб кодера).

---

## 🎯 НАЗНАЧЕНИЕ

Phase 1.6 - это приемка и валидация summary от архитектора с точками инъекции проб для каждой гипотезы H1-H5. Цель - получить структурированный план инъекций в формате "Гипотеза -> Файл:Линия -> Что логируем" для дальнейшей работы.

**ADHD-ФРЕНДЛИ ФОРМАТ:** Результат должен быть легко сканируемым: каждая гипотеза четко связана с точкой инъекции и тем, что нужно логировать. Формат "H1 -> main.py:42 -> log: user_id" работает как визуальный якорь.

---

## 📋 ОБЯЗАТЕЛЬНЫЕ ШАГИ

### 1. Получение summary

**🚨🚨🚨 КРИТИЧЕСКИ ВАЖНО: Ты НЕ вызываешь `attempt_completion` сам! 🚨🚨🚨**

**ИСТОЧНИК:** Summary приходит АВТОМАТИЧЕСКИ от подзадачи архитектора, когда архитектор завершает свою работу через `attempt_completion` в своей подзадаче.

**КАК ЭТО РАБОТАЕТ В ROO CODE:**
1. Ты создал подзадачу через `new_task(mode="architect", message="...")`
2. Твоя задача приостанавливается, архитектор работает в изолированном контексте подзадачи
3. Архитектор вызывает `attempt_completion(result="...")` в своей подзадаче
4. Roo Code автоматически возобновляет твою задачу и передает summary в твой контекст
5. Summary уже доступен в твоем контексте - НЕ нужно вызывать `attempt_completion` самому!
6. Summary - это текст, который архитектор передал в параметре `result` при вызове `attempt_completion`

**🚨 ЗАПРЕЩЕНО:**
- ❌ Вызывать `attempt_completion` самому - это делает архитектор в своей подзадаче!
- ❌ "Зафиксировать результаты" через `attempt_completion` - summary уже пришел от архитектора!
- ❌ Создавать свой summary - используй summary от архитектора, который уже в контексте!

**✅ ОБЯЗАТЕЛЬНО:**
- ✅ Используй summary, который уже доступен в твоем контексте после завершения подзадачи архитектора
- ✅ Summary содержит текст, который архитектор передал в `attempt_completion(result="...")`
- ✅ Валидируй формат полученного summary

---

### 2. Валидация формата summary

**ОБЯЗАТЕЛЬНО:** Проверь наличие всех полей для каждой гипотезы H1-H5:

**ФОРМАТ:** Summary содержит структурированные данные в формате (ADHD-френдли):
```
H1 -> [Файл:Линия:Функция] -> [Что логируем: переменные, состояния, значения]
  [DEBUG-STRATEGIST]: [краткое обоснование зачем именно эти данные - проверяем мутацию стейта, валидацию входных данных, состояние перед критической операцией и т.д.]
  [SRE-SHIELD]: [замедлит ли цикл? частота вызовов? объем логов? нужен ли сэмплинг?]

H2 -> [Файл:Линия:Функция] -> [Что логируем: переменные, состояния, значения]
  [DEBUG-STRATEGIST]: [краткое обоснование зачем именно эти данные]
  [SRE-SHIELD]: [...]

H3 -> [Файл:Линия:Функция] -> [Что логируем: переменные, состояния, значения]
  [DEBUG-STRATEGIST]: [краткое обоснование зачем именно эти данные]
  [SRE-SHIELD]: [...]
```

**КРИТИЧЕСКИ ВАЖНО:** [DEBUG-STRATEGIST] должен содержать краткое обоснование ЗАЧЕМ именно эти данные логируются. Это помогает через время быстро вспомнить, какой профит ожидается от конкретной пробы. Примеры:
- "проверяем мутацию стейта перед вызовом API"
- "валидация входных данных перед обработкой"
- "состояние переменной перед критической операцией"
- "проверяем корректность преобразования данных"

**ОБЯЗАТЕЛЬНЫЕ ПОЛЯ ДЛЯ КАЖДОЙ ГИПОТЕЗЫ:**
- **H1-H5:** (минимум H1-H3) - ОБЯЗАТЕЛЬНО
- **Файл:Линия:Функция** - ОБЯЗАТЕЛЬНО (конкретное место в коде)
- **Что логируем** - ОБЯЗАТЕЛЬНО (переменные, состояния, значения для логирования)
- **[DEBUG-STRATEGIST]** - ОБЯЗАТЕЛЬНО (краткое обоснование ЗАЧЕМ именно эти данные логируются - проверяем мутацию стейта, валидацию, состояние перед операцией и т.д.)
- **[SRE-SHIELD]** - ОБЯЗАТЕЛЬНО (overhead, частота, объем логов, сэмплинг)

**ВАЛИДАЦИЯ:**

1. **Наличие точек инъекции для всех гипотез:**
   - ✅ Минимум H1-H3 должны иметь точки инъекции
   - ❌ ЗАПРЕЩЕНО: Принимать summary без точек инъекции для H1-H3
   - ❌ ЗАПРЕЩЕНО: Принимать summary с неполными данными (только файл без строки/функции)

2. **Наличие стратегии [DEBUG-STRATEGIST]:**
   - ✅ Каждая точка инъекции должна иметь обоснование точки наблюдения
   - ✅ Должно быть указано, какое состояние переменной подтвердит/опровергнет гипотезу
   - ❌ ЗАПРЕЩЕНО: Принимать точки инъекции без обоснования стратегии

3. **Наличие стратегии [SRE-SHIELD]:**
   - ✅ Каждая точка инъекции должна иметь оценку overhead
   - ✅ Должно быть указано, нужен ли сэмплинг (если в цикле >100 итераций или частота >1000 раз/сек)
   - ❌ ЗАПРЕЩЕНО: Принимать точки инъекции без оценки overhead

**ЕСЛИ ФОРМАТ НЕ СООТВЕТСТВУЕТ:**
- ❌ ERROR: "Summary от архитектора не соответствует формату. Требуется: H1-H5 с точками инъекции, [DEBUG-STRATEGIST], [SRE-SHIELD]"
- ❌ Запроси повтор через `new_task` с уточнением формата
- ❌ НЕ продолжай к Phase 1.1 без валидного summary

---

### 3. Парсинг summary

**ОБЯЗАТЕЛЬНО:** Извлеки структурированные данные:

- Для каждой гипотезы H1-H5:
  - Файл, строка, функция (точка инъекции)
  - Обоснование
  - [DEBUG-STRATEGIST] стратегия
  - [SRE-SHIELD] оценка overhead

**ФОРМАТ ХРАНЕНИЯ:**
- Сохрани summary для использования в Phase 1.1 (делегирование вставки проб кодера)
- НЕ ищи точки инъекции заново - используй summary от архитектора

---

### 4. Запись в memory (если доступно)

**ЕСЛИ memory доступен (Deep Debug Mode):**
- Используй `mcp_memory_add_observations` для записи summary
- Наблюдение: `"Injection points summary: ${result}"`
- Теги: `["debug_session", "injection_points"]`

**ЕСЛИ memory НЕ доступен:**
- Пропусти этот шаг
- Сохрани summary в локальной переменной для использования в Phase 1.1

---

### 5. Обновление TODO

**ОБЯЗАТЕЛЬНО:**
- Отметь: `[DONE] Точки инъекции проб получены от архитектора`
- Добавь задачи на основе summary:
  - `Phase 1.1: Делегировать вставку проб кодера (new_task mode="code")`

---

### 6. Валидация содержания

**ОБЯЗАТЕЛЬНО:** Проверь качество summary:

- Если summary пустой → ❌ ERROR и запроси повтор
- Если точки инъекции отсутствуют для H1-H3 → ❌ ERROR и запроси повтор
- Если отсутствует [DEBUG-STRATEGIST] или [SRE-SHIELD] → ❌ ERROR и запроси повтор
- Если все ОК → ✅ Переходи к Phase 1.1

**КРИТЕРИИ КАЧЕСТВА:**
- Минимум 3 точки инъекции (H1-H3)
- Каждая точка должна иметь все поля (файл:строка:функция, обоснование, [DEBUG-STRATEGIST], [SRE-SHIELD])
- [SRE-SHIELD] должен содержать оценку overhead и рекомендации по сэмплированию (если нужно)

---

## 🚨 ШТРАФЫ

**ШТРАФ:** Продолжение без валидации формата summary = +15 баллов (CRITICAL FAILURE)

**ШТРАФ:** Продолжение без валидации содержания summary = +10 баллов (CRITICAL FAILURE)

**ШТРАФ:** Принятие summary без точек инъекции для H1-H3 = +15 баллов (CRITICAL FAILURE)

**ШТРАФ:** Принятие summary без [DEBUG-STRATEGIST] или [SRE-SHIELD] = +10 баллов (CRITICAL FAILURE)

**ШТРАФ:** Принятие summary с [DEBUG-STRATEGIST] без обоснования ЗАЧЕМ логируются данные = +5 баллов

**ШТРАФ:** Продолжение к Phase 1.1 при пустом summary = +20 баллов (CRITICAL FAILURE)

---

## 📚 СВЯЗАННЫЕ МОДУЛИ

**🚨🚨🚨 КРИТИЧЕСКИ ВАЖНО: ИМЯ ИНСТРУМЕНТА ДОСЛОВНОЕ! 🚨🚨🚨**

**Для загрузки модулей СКОПИРУЙ КОМАНДЫ БУКВА В БУКВУ (НЕ ИЗМЕНЯЙ ИМЯ ИНСТРУМЕНТА!):**

- **СКОПИРУЙ:** `load_rule(rulePath="roo-05-delegate-analysis.md")` - Phase 1.5: Делегирование стратегии инъекции
- **СКОПИРУЙ:** `load_rule(rulePath="code-01-probe-insertion.md")` - Phase 1.1: Делегирование вставки проб
- **СКОПИРУЙ:** `load_rule(rulePath="roo-05-hypotheses.md")` - Phase 1: Формулирование гипотез
- **СКОПИРУЙ:** `load_rule(rulePath="00-base-advanced.md")` - Валидация форматов summary (входит в объединенный модуль)
- **СКОПИРУЙ:** `load_rule(rulePath="00-help-operations.md")` - Справочник по `attempt_completion`

---

**Последнее обновление:** 2026-01-24
