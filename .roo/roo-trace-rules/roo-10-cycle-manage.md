# 🔄 PHASE 7: УПРАВЛЕНИЕ ЦИКЛОМ ОТЛАДКИ

**🚨 КРИТИЧЕСКИ ВАЖНО:** Это циклический процесс, который повторяется до решения проблемы.

**ВЫПОЛНЯЕТСЯ:** ПОСЛЕ Phase 6 (чтение и анализ логов), если проблема НЕ решена. Повторяется до решения проблемы.

---

## 🎯 НАЗНАЧЕНИЕ

Phase 7 - это цикл отладки, который включает:
1. Делегирование анализа логов архитектору (с учетом BLACKLIST предыдущих попыток)
2. Получение и валидация решения от архитектора
3. Делегирование исправления кода code mode
4. Получение подтверждения исправления от кодера
5. Запрос пользователю запустить код снова
6. Повтор цикла до решения проблемы

---

## 📋 ИНИЦИАЛИЗАЦИЯ ЦИКЛА

**ОБЯЗАТЕЛЬНО:** При первом входе в Phase 7:

1. **Инициализация счетчика итераций:**
   - `_current_iteration = 1`

2. **Инициализация истории отладки:**
   - **Если memory доступен (Deep Debug Mode):**
     - Используй существующую сущность сессии (если есть)
     - Или создай новую сущность через `mcp_memory_create_entities` с тегами `["debug_session"]`
   - **Если memory НЕ доступен (Fallback):**
     - Инициализируй `_debug_history = []`

---

## 📋 ОБЯЗАТЕЛЬНЫЕ ШАГИ (ЦИКЛ)

### Phase 7.1: Делегирование анализа архитектору (С ПЕРЕДАЧЕЙ BLACKLIST)

**КРИТИЧЕСКИ ВАЖНО:** Пропуск передачи истории предыдущих summary в Phase 7.1 = +15 баллов (CRITICAL FAILURE)

#### 1. Подготовка контекста

**ОБЯЗАТЕЛЬНО:** Собери всю необходимую информацию:

- Результаты анализа логов из Phase 6
- Какая гипотеза подтвердилась (H1/H2/H3...)
- Какие данные получены из проб
- **ОБЯЗАТЕЛЬНО:** История предыдущих summary из memory или `_debug_history`

#### 2. Извлечение истории

**Если memory доступен (Deep Debug Mode):**
- Используй `mcp_memory_search_nodes` с запросом: `"debug_session iteration"`
- Или `mcp_memory_open_nodes` для получения всех записей сессии
- Извлеки все наблюдения (observations) из найденных сущностей
- Отфильтруй по типу (architect/code) и итерациям

**Если memory НЕ доступен (Fallback):**
- Используй массив `_debug_history` напрямую

#### 3. Формирование BLACKLIST

**ОБЯЗАТЕЛЬНО:** Если история не пустая:

- Сформируй секцию "BLACKLIST ПРЕДЫДУЩИХ ПОПЫТОК"
- Для каждой итерации в истории:
  - Извлеки РЕШЕНИЕ из summary архитектора (искать "РЕШЕНИЕ:" в summary)
  - Извлеки ИЗМЕНЕНО из summary кодера (искать "ИЗМЕНЕНО:" в summary)
  - Укажи результат: "Проблема НЕ решена"

**ФОРМАТ BLACKLIST:**
```
BLACKLIST ПРЕДЫДУЩИХ ПОПЫТОК (НЕ ПРЕДЛАГАЙ ЭТИ РЕШЕНИЯ СНОВА):

[Итерация 1]
- Архитектор предложил: [РЕШЕНИЕ из summary]
- Кодер выполнил: [ИЗМЕНЕНО из summary]
- Результат: Проблема НЕ решена

[Итерация 2]
- Архитектор предложил: [РЕШЕНИЕ из summary]
- Кодер выполнил: [ИЗМЕНЕНО из summary]
- Результат: Проблема НЕ решена
```

#### 4. Вызов new_task для архитектора

**ИНСТРУМЕНТ:** `new_task(mode="architect", message="...")`

**ОБЯЗАТЕЛЬНЫЙ ФОРМАТ СООБЩЕНИЯ:**

```javascript
new_task({
  mode: "architect",
  message: `
Проанализируй проблему на основе логов и предложи НОВОЕ решение.

КОНТЕКСТ:
- Логи показывают: [описание на основе Phase 6]
- Подтвержденная гипотеза: [H1/H2/H3...]
- Данные из проб: [ключевые данные]

${history.length > 0 ? `
BLACKLIST ПРЕДЫДУЩИХ ПОПЫТОК (НЕ ПРЕДЛАГАЙ ЭТИ РЕШЕНИЯ СНОВА):

${history.map((entry, idx) => `
[Итерация ${entry.iteration}]
- Архитектор предложил: ${entry.solution}
- Кодер выполнил: ${entry.changed}
- Результат: Проблема НЕ решена
`).join('\n')}

КРИТИЧЕСКИ ВАЖНО: НЕ предлагай решения, которые уже были испробованы выше!
Предложи АЛЬТЕРНАТИВНОЕ решение, учитывая что предыдущие не сработали.
` : ''}

ЗАДАЧА:
1. Проанализируй логи и определи корневую причину
2. Предложи КОНКРЕТНОЕ НОВОЕ исправление (отличное от предыдущих)
3. Укажи файлы и строки для изменения
4. Объясни, почему это исправление решит проблему

ПО ЗАВЕРШЕНИИ:
- **ОБЯЗАТЕЛЬНО:** Используй инструмент attempt_completion с параметром result
- **ФОРМАТ result:** См. arch-02-log-analysis.md для детального формата
     `
})
```

**СВЯЗАННЫЙ МОДУЛЬ:** `arch-02-log-analysis.md` - Детальный протокол анализа логов для архитектора

---

### Phase 7.2: Получение summary от архитектора и проверка уникальности

#### 1. Получение summary

**ОБЯЗАТЕЛЬНО:** Получи summary через `attempt_completion`

#### 2. Валидация формата

**ОБЯЗАТЕЛЬНО:** Валидируй формат summary:

- Должен содержать: `ПРОБЛЕМА:РЕШЕНИЕ:ФАЙЛ:СТРОКА:ИЗМЕНЕНИЕ:ОБОСНОВАНИЕ`
- См. `arch-02-log-analysis.md` для детальных требований к формату

#### 3. Проверка уникальности решения

**КРИТИЧЕСКИ ВАЖНО:** Проверь, что решение НЕ повторяет предыдущие попытки:

**Если sequentialthinking доступен (Deep Debug Mode):**
- Используй `mcp_sequentialthinking_sequentialthinking` для скрытой валидации
- Мысль: "Сравни новое РЕШЕНИЕ с предыдущими из memory. Если идентично - отклони."
- Результат валидации не показывай пользователю

**Если sequentialthinking НЕ доступен (Fallback):**
- Извлеки РЕШЕНИЕ из нового summary
- Сравни с РЕШЕНИЯМИ из `_debug_history` (или memory)
- Если решение идентично или очень похоже → ERROR: "Это решение уже было испробовано в итерации N"
- Запроси новое решение через `new_task` с уточнением

#### 4. Запись в memory

**Если решение уникально:**

**Если memory доступен (Deep Debug Mode):**
- Используй `mcp_memory_add_observations` для добавления наблюдения к сущности сессии
- Наблюдение: `"Iteration ${_current_iteration}: architect - ${result}"`
- Теги: `["debug_session", "iteration_${_current_iteration}", "architect"]`

**Если memory НЕ доступен (Fallback):**
- Добавь в `_debug_history`: `{ iteration: _current_iteration, type: "architect", summary: result, timestamp: Date.now() }`

#### 5. Обновление TODO

**ОБЯЗАТЕЛЬНО:** Обнови todo: `[DONE] Анализ от архитектора получен`

---

### Phase 7.3: Делегирование исправления кодеру

**СВЯЗАННЫЙ МОДУЛЬ:** `code-02-code-fix.md` - Детальный протокол исправления кода для code mode

**ОБЯЗАТЕЛЬНО:** Следуй протоколу из `code-02-code-fix.md`:

1. Подготовь инструкции из summary архитектора
2. Вызови `new_task(mode="code", message="...")`
3. Дождись summary от кодера через `attempt_completion`

---

### Phase 7.4: Получение summary от кодера

#### 1. Получение summary

**ОБЯЗАТЕЛЬНО:** Получи summary через `attempt_completion`

#### 2. Валидация

**ОБЯЗАТЕЛЬНО:** Валидируй, что исправление выполнено:

- Проверь формат: `ФАЙЛ:ИЗМЕНЕНО:ДИАГНОСТИКА:PATCH`
- Убедись, что диагностика показывает OK или WARNINGS (не ERRORS)
- См. `code-02-code-fix.md` для детальных требований

#### 3. Запись в memory

**Если memory доступен (Deep Debug Mode):**
- Используй `mcp_memory_add_observations` для добавления наблюдения к сущности сессии
- Наблюдение: `"Iteration ${_current_iteration}: code - ${result}"`
- Теги: `["debug_session", "iteration_${_current_iteration}", "code"]`

**Если memory НЕ доступен (Fallback):**
- Добавь в `_debug_history`: `{ iteration: _current_iteration, type: "code", summary: result, timestamp: Date.now() }`

#### 4. Обновление счетчика итераций

**ОБЯЗАТЕЛЬНО:** Увеличь `_current_iteration += 1`

#### 5. Обновление TODO

**ОБЯЗАТЕЛЬНО:** Обнови todo: `[DONE] Исправление выполнено`

---

### Phase 7.5: Запрос пользователю снова запустить код

#### 1. Запрос

**ОБЯЗАТЕЛЬНО:** Попроси пользователя:

- Запустить код снова
- Повторить действия, приводящие к багу
- Нажать кнопку "Read logs" когда готово (если авто-отладка не разрешена)

**ФОРМАТ СООБЩЕНИЯ:**
```
Исправление применено. Запустите код снова и воспроизведите баг. 
Когда будете готовы, нажмите "Read logs" (или скажите "Logs ready").
```

#### 2. Ожидание

**ОБЯЗАТЕЛЬНО:** Дождись подтверждения от пользователя

#### 3. Переход к Phase 6

**ОБЯЗАТЕЛЬНО:** После подтверждения переходи к Phase 6 (чтение логов)

**🚨🚨🚨 КРИТИЧЕСКИ ВАЖНО: ИМЯ ИНСТРУМЕНТА ДОСЛОВНОЕ! 🚨🚨🚨**

**СКОПИРУЙ ЭТУ КОМАНДУ БУКВА В БУКВУ:** `load_rule(rulePath="roo-09-read-logs.md")` - Phase 6: Чтение логов

---

### Phase 7.6: Проверка решения и повтор цикла

#### 1. После чтения логов (Phase 6)

**ОБЯЗАТЕЛЬНО:** Определи результат:

- ✅ **Если проблема решена** → переходи к Phase 8 (очистка)
- ❌ **Если проблема НЕ решена** → возвращайся к Phase 7.1 (новый цикл с обновленной историей)

#### 2. Обновление TODO

**ОБЯЗАТЕЛЬНО:** Обнови todo:

- Если проблема решена: `[DONE] Проблема решена, переход к Phase 8`
- Если проблема НЕ решена: `[TODO] Итерация ${_current_iteration + 1}: Повтор цикла отладки`

---

## 🚨 ШТРАФЫ

**ШТРАФ:** Пропуск передачи истории предыдущих summary в Phase 7.1 = +15 баллов (CRITICAL FAILURE)

**ШТРАФ:** Пропуск проверки уникальности решения в Phase 7.2 = +10 баллов

**ШТРАФ:** Повторное предложение уже испробованного решения = +15 баллов (CRITICAL FAILURE)

**ШТРАФ:** Пропуск записи в memory или `_debug_history` = +5 баллов

**ШТРАФ:** Пропуск обновления счетчика итераций = +5 баллов

**ШТРАФ:** Преждевременный переход к Phase 8 без проверки решения = +10 баллов (CRITICAL FAILURE)

---

## 📚 СВЯЗАННЫЕ МОДУЛИ

**🚨🚨🚨 КРИТИЧЕСКИ ВАЖНО: ИМЯ ИНСТРУМЕНТА ДОСЛОВНОЕ! 🚨🚨🚨**

**Для загрузки модулей СКОПИРУЙ КОМАНДЫ БУКВА В БУКВУ (НЕ ИЗМЕНЯЙ ИМЯ ИНСТРУМЕНТА!):**

- **СКОПИРУЙ:** `load_rule(rulePath="roo-09-read-logs.md")` - Phase 6: Чтение логов
- **СКОПИРУЙ:** `load_rule(rulePath="arch-02-log-analysis.md")` - Phase 7.1: Протокол анализа логов
- **СКОПИРУЙ:** `load_rule(rulePath="code-02-code-fix.md")` - Phase 7.3: Протокол исправления кода
- **СКОПИРУЙ:** `load_rule(rulePath="roo-08-wait.md")` - Phase 5: Ожидание пользователя
- **СКОПИРУЙ:** `load_rule(rulePath="roo-11-cleanup.md")` - Phase 8: Очистка
- **СКОПИРУЙ:** `load_rule(rulePath="00-help-operations.md")` - Справочник по memory и sequentialthinking

---

**Последнее обновление:** 2026-01-23
