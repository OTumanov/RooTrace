# 🎯 КРИТИЧЕСКИЕ ОГРАНИЧЕНИЯ

**🚨 КРИТИЧЕСКИ ВАЖНО:** Эти ограничения применяются на ВСЕХ фазах без исключений.

**ВЫПОЛНЯЕТСЯ:** Применяется с самого начала работы (Phase 0) и на всех последующих фазах.

---

## 🎯 НАЗНАЧЕНИЕ

Этот модуль определяет критические ограничения, которые НЕ МОГУТ быть нарушены ни при каких обстоятельствах. Эти правила обеспечивают безопасность, корректность и предсказуемость работы системы.

---

## 📋 КРИТИЧЕСКИЕ ОГРАНИЧЕНИЯ

### 1. TODO LIST FIRST

**🚨 TODO LIST FIRST:**

**ОБЯЗАТЕЛЬНО:** Создай todo list ПЕРВЫМ действием (Phase 0) через `update_todo_list`.

**ПОЧЕМУ:** Без todo list агент игнорирует протокол.

**ШТРАФ:** Пропуск создания todo list = +10 баллов (CRITICAL FAILURE)

**СВЯЗАННЫЙ МОДУЛЬ:** `roo-01-todo-list.md` - Phase 0.1: TODO list

---

### 2. No Manual Execution

**No Manual Execution:**

**ЗАПРЕЩЕНО:** Запускать код. Это делает пользователь.

**ИСКЛЮЧЕНИЕ:** Если пользователь разрешил авто-отладку (проверка `read_runtime_logs` прошла успешно), агент ОБЯЗАН сам запустить код через `execute_command` перед чтением логов.

**ПРИМЕРЫ ЗАПРЕЩЕННЫХ ДЕЙСТВИЙ:**
- ❌ `execute_command("python main.py")` без разрешения авто-отладки
- ❌ `execute_command("npm start")` без разрешения авто-отладки
- ❌ Запуск кода через любые другие инструменты без разрешения

**ПРИМЕРЫ РАЗРЕШЕННЫХ ДЕЙСТВИЙ:**
- ✅ Запрос пользователю запустить код (Phase 5: WAIT)
- ✅ `execute_command("python main.py")` если авто-отладка разрешена
- ✅ `execute_command` для компиляции/проверки синтаксиса

**СВЯЗАННЫЙ МОДУЛЬ:** `roo-08-wait.md` - Phase 5: WAIT (проверка разрешения авто-отладки)

---

### 3. No Dirty Tools

**No Dirty Tools:**

**ЗАПРЕЩЕНО:** Использовать `curl` или `fetch` для взаимодействия с сервером RooTrace.

**ОБЯЗАТЕЛЬНО:** Используй ТОЛЬКО инструменты RooTrace:
- `get_debug_status` - проверка статуса сервера
- `read_runtime_logs` - чтение логов (если разрешено)
- `get_problems` - получение диагностики
- `clear_session` - очистка сессии
- `show_user_instructions` - показ инструкций пользователю

**ПРИМЕРЫ ЗАПРЕЩЕННЫХ ДЕЙСТВИЙ:**
- ❌ `curl http://localhost:51234/`
- ❌ `fetch('http://localhost:51234/')`
- ❌ Любые прямые HTTP-запросы к серверу RooTrace

**ПРИМЕРЫ РАЗРЕШЕННЫХ ДЕЙСТВИЙ:**
- ✅ `get_debug_status()` для проверки сервера
- ✅ `read_runtime_logs()` для чтения логов (если разрешено)
- ✅ Прямые HTTP-запросы в пробах (это нормально, пробы отправляют данные на сервер)

**СВЯЗАННЫЙ МОДУЛЬ:** `00-help-operations.md` - Справочник по всем MCP инструментам

---

### 4. Git Backup Exception

**Git Backup Exception:**

**РАЗРЕШЕНО:** Использовать `execute_command` ТОЛЬКО для:

1. **Git операций:**
   - `git add . && git commit -m "AI Debugger: Pre-instrumentation backup"`
   - `git checkout -- "[file]"` (селективный откат)
   - `git status` (проверка состояния)
   - `git diff` (просмотр изменений)

2. **Компиляции и проверки синтаксиса:**
   - `python3 -m py_compile <файл>` (Python)
   - `tsc --noEmit` (TypeScript)
   - `npm run build` (JavaScript/TypeScript)
   - `gcc -c <файл>` (C/C++)
   - `go build` (Go)

3. **Запуска кода (только если авто-отладка разрешена):**
   - `python <main_file>.py`
   - `node <main_file>.js`
   - `npm start`
   - `go run <main_file>.go`

**ЗАПРЕЩЕНО:** Использовать `execute_command` для:
- Прямых HTTP-запросов к серверу RooTrace (используй MCP инструменты)
- Запуска кода без разрешения авто-отладки
- Любых других операций, не перечисленных выше

---

## 🚨 ШТРАФЫ

**ШТРАФ:** Пропуск создания todo list = +10 баллов (CRITICAL FAILURE)

**ШТРАФ:** Запуск кода без разрешения авто-отладки = +15 баллов (CRITICAL FAILURE)

**ШТРАФ:** Использование `curl` или `fetch` вместо MCP инструментов = +10 баллов (CRITICAL FAILURE)

**ШТРАФ:** Использование `execute_command` для запрещенных операций = +10 баллов

---

## 📚 СВЯЗАННЫЕ МОДУЛИ

- `roo-01-todo-list.md` - Phase 0.1: TODO list (ограничение 1)
- `roo-08-wait.md` - Phase 5: WAIT (ограничение 2 - проверка авто-отладки)
- `00-help-operations.md` - Справочник по MCP инструментам (ограничение 3)
- `roo-12-manual-prohibition.md` - Запрет ручного анализа (дополнительные ограничения)

---

**Последнее обновление:** 2026-01-23
