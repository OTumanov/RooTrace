# 🔍 PHASE 0.2: ДЕЛЕГИРОВАНИЕ РАЗВЕДКИ

**🚨 КРИТИЧЕСКИ ВАЖНО:** Ты НЕ делаешь разведку сам. Ты делегируешь архитектору.

**ВЫПОЛНЯЕТСЯ:** ПОСЛЕ Phase 0.1 (TODO list), ПЕРЕД Phase 0.3 (приемка summary).

---

## 🎯 НАЗНАЧЕНИЕ

Phase 0.2 - это делегирование разведки архитектору для анализа проблемы и поиска подозрительных мест в коде. Цель - получить структурированный список координат (ФАЙЛ:КООРДИНАТА:ФУНКЦИЯ:ПРИЧИНА) для дальнейшей работы.

---

## 📋 ОБЯЗАТЕЛЬНЫЕ ШАГИ

### 1. Подготовка контекста для архитектора

**ОБЯЗАТЕЛЬНО:** Собери всю доступную информацию:

- Описание проблемы от пользователя
- Результаты диагностики (из `get_problems()` или `@problems` mention если использовался)
- Любые упоминания файлов/функций
- Тип проблемы (Crash/Slow/Logic из Phase 0)

---

### 2. Вызов new_task

**🚨🚨🚨 КРИТИЧЕСКИ ВАЖНО: ИМЯ ИНСТРУМЕНТА ДОСЛОВНОЕ! 🚨🚨🚨**

**СКОПИРУЙ ЭТУ КОМАНДУ БУКВА В БУКВУ (НЕ ИЗМЕНЯЙ ИМЯ ИНСТРУМЕНТА!):**
`new_task(mode="architect", message="...")`

**ИНСТРУМЕНТ:** `new_task(mode="architect", message="...")`

**ОБЯЗАТЕЛЬНЫЙ ФОРМАТ СООБЩЕНИЯ:**

```javascript
new_task({
  mode: "architect",
  message: `
Твоя единственная цель — разведка. Составь список файлов и подозрительных мест.

КОНТЕКСТ ПРОБЛЕМЫ:
[Вставить описание проблемы, тип, файлы]

ЗАДАЧА:
1. Проанализируй проблему и найди все релевантные файлы
2. Составь список подозрительных мест (функции, классы, строки)
3. Определи точки, где могут быть проблемы (согласно описанию проблемы в КОНТЕКСТЕ ПРОБЛЕМЫ)
4. НИЧЕГО НЕ ПРАВЬ - только разведка

🚨 КРИТИЧЕСКИ ВАЖНО: НЕ просто броди по структуре проекта! Ты должен ЦЕЛЕНАПРАВЛЕННО искать код, связанный с проблемой.

СТРАТЕГИЯ ПОИСКА (ВЫПОЛНЯЙ В ЭТОМ ПОРЯДКЕ):
1. **ПЕРВЫМ делом используй \`codebase_search\`** для поиска кода по смыслу проблемы:
   - Извлеки ключевые термины из описания проблемы в КОНТЕКСТЕ ПРОБЛЕМЫ
   - Сформулируй семантический запрос на основе этих терминов (например: "функция [ключевая операция]", "медленная обработка [объект]", "производительность при [действие]")
   - НЕ используй общие запросы типа "код" или "функции" - будь конкретным, используя термины из описания проблемы!

2. **ВТОРЫМ используй \`search_files\`** для поиска конкретных паттернов:
   - Извлеки ключевые слова из описания проблемы и ищи функции/классы по этим словам
   - Ищи функции по именам: \`def.*[ключевое_слово]\`, используя термины из проблемы
   - Ищи классы: \`class.*[ключевое_слово]\`, используя термины из проблемы
   - Ищи вызовы функций/методов, упомянутых в описании проблемы

3. **ТРЕТЬИМ читай найденные файлы** через \`read_file\`:
   - Если в задаче упомянут файл/эндпоинт - начни с него
   - Если есть patch файлы с именами функций - это подсказка, где искать
   - Читай только нужные функции/диапазоны строк, не весь файл!

4. **ОПЦИОНАЛЬНО:** Используй \`list_files\` только если нужно понять структуру конкретной директории
5. **ОПЦИОНАЛЬНО:** Используй \`list_code_definition_names\` только для быстрого обзора структуры конкретного файла

ОБЯЗАТЕЛЬНО:
- Используй доступные инструменты для поиска в ПРАВИЛЬНОМ порядке (см. СТРАТЕГИЮ ПОИСКА выше)
- НЕ начинай с \`list_files\` по всему проекту - это пустая трата времени!
- НЕ просто броди по структуре - ищи ЦЕЛЕНАПРАВЛЕННО код, связанный с проблемой!

🚨🚨🚨 КРИТИЧЕСКИ ВАЖНО: ВСЕГДА используй \`attempt_completion\` для возврата результатов! 🚨🚨🚨

ЗАПРЕЩЕНО:
- ❌ Использовать \`switch_mode\` - это НЕ твоя задача
- ❌ Просить пользователя переключиться в другой режим
- ❌ Передавать задачу кодеру - ты должен вернуть summary оркестратору
- ❌ Создавать todo list для реализации - ты только разведка
- ❌ Задавать вопросы о требованиях, целевой производительности, ожиданиях - ты должен искать код, который работает медленно, а не спрашивать о требованиях
- ❌ Прекращать разведку из-за ошибок инструментов - используй другие инструменты для продолжения

ЧТО ДЕЛАТЬ ПРИ ОШИБКАХ ИНСТРУМЕНТОВ:
- Если \`ask_followup_question\` не работает → НЕ задавай вопросы, просто продолжай разведку через \`codebase_search\` и \`search_files\`
- Если один инструмент не работает → используй другие инструменты из списка
- Если все инструменты не работают → используй \`read_file\` для чтения файлов, которые упомянуты в задаче
- НИКОГДА не прекращай разведку из-за ошибок - найди другой способ получить информацию

ОБЯЗАТЕЛЬНО: Используй \`attempt_completion\` с параметром \`result\` - это ЕДИНСТВЕННЫЙ способ вернуть результаты.

ФОРМАТ ВЫВОДА (attempt_completion):
Ты ДОЛЖЕН вернуть summary в строгом формате через \`attempt_completion\`:
ФАЙЛ:путь/к/файлу.py
КООРДИНАТА:строка:номер
ФУНКЦИЯ:имя_функции
ПРИЧИНА:краткое обоснование с цитатами кода/логов (минимум 3-5 строк кода или точная цитата лога)
---
[Повторить для каждого подозрительного места]

ТРЕБОВАНИЯ:
- Только координаты и причины - НИЧЕГО ДРУГОГО
- Без лишних приветствий или filler-текста
- Профессиональный, прямой тон
- Легко сканируется и понимается
- КРИТИЧЕСКИ ВАЖНО: Если ты не предоставишь координаты в формате ФАЙЛ:КООРДИНАТА:ФУНКЦИЯ:ПРИЧИНА, оркестратор отклонит твой отчет!
- ВАЖНО: Твой контекст будет уничтожен после завершения. Оркестратор получит только summary через \`attempt_completion\`.
- КРИТИЧЕСКИ ВАЖНО: НЕ используй \`switch_mode\` - используй ТОЛЬКО \`attempt_completion\`!
  `
})
```

**ВАЖНО:** 
- Включи весь необходимый контекст в `message`
- Укажи четко формат вывода (ФАЙЛ:КООРДИНАТА:ФУНКЦИЯ:ПРИЧИНА)
- Напомни, что архитектор должен использовать `attempt_completion` для возврата summary

---

### 3. Ожидание завершения

**ОБЯЗАТЕЛЬНО:**
- Подзадача архитектора работает в изолированном контексте
- Ты получаешь только summary через `attempt_completion`
- Обнови todo: `[DONE] Разведка делегирована`

**ВАЖНО:** Не пытайся выполнять разведку сам - это нарушение протокола!

---

## 🚨 ШТРАФЫ

**ШТРАФ:** Выполнение разведки напрямую вместо делегирования = +15 баллов (CRITICAL FAILURE)

**ШТРАФ:** Пропуск Phase 0.2 (делегирование разведки) = +20 баллов (CRITICAL FAILURE)

**ШТРАФ:** Неполный контекст в message для архитектора = +5 баллов

---

## 📚 СВЯЗАННЫЕ МОДУЛИ

**🚨🚨🚨 КРИТИЧЕСКИ ВАЖНО: ИМЯ ИНСТРУМЕНТА ДОСЛОВНОЕ! 🚨🚨🚨**

**Для загрузки модулей СКОПИРУЙ КОМАНДЫ БУКВА В БУКВУ (НЕ ИЗМЕНЯЙ ИМЯ ИНСТРУМЕНТА!):**

- **СКОПИРУЙ:** `load_rule(rulePath="roo-01-todo-list.md")` - Phase 0.1: TODO list
- **СКОПИРУЙ:** `load_rule(rulePath="roo-03-receive-architect.md")` - Phase 0.3: Приемка summary
- **СКОПИРУЙ:** `load_rule(rulePath="arch-00-role.md")` - Роль архитектора
- **СКОПИРУЙ:** `load_rule(rulePath="arch-01-reconnaissance.md")` - Протокол разведки
- **СКОПИРУЙ:** `load_rule(rulePath="arch-03-format-recon.md")` - Формат вывода
- **СКОПИРУЙ:** `load_rule(rulePath="00-help-operations.md")` - Справочник по `new_task`

---

**Последнее обновление:** 2026-01-23
