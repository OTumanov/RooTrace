# 💻 Этап 4: Создание Code модулей

## 🎯 Цель
Создать модули для делегирования работы кодеру (code-*.md), содержащие детальные инструкции по вставке проб и исправлению кода.

## 📋 Модули для создания

### 1. `code-01-probe-insertion.md` ⭐

**Источник:** строки 1272-1447 промпта

**Содержание:**
- Phase 1.1: Делегирование вставки проб
- Протокол `new_task(mode="code")`
- Инструкции для кодера
- Обязательные требования к коду проб
- Примеры кода проб по языкам
- Протокол проверки линтера
- Формат summary от кодера

**Структура:**
```markdown
# 💉 Phase 1.1: Делегирование вставки проб (BOOMERANG)

**🚨 КРИТИЧЕСКИ ВАЖНО:** Ты НЕ вставляешь пробы сам. Ты делегируешь code mode.

## ОБЯЗАТЕЛЬНЫЕ ШАГИ:

1. **Подготовка инструкций:**
   - Список гипотез (H1-H5)
   - Список координат из summary архитектора
   - FINAL_HOST и ACTUAL_PORT из Phase 0.4
   - Стратегия [DEBUG-STRATEGIST] и [SRE-SHIELD]

2. **Вызов new_task:**
   [Шаблон инструкций для кодера]

3. **Ожидание завершения:**
   - Получение summary через attempt_completion
   - Валидация summary
```

**Размер:** ~175 строк

---

### 2. `code-04-block-rewrite.md` ⭐⭐⭐ КРИТИЧНЫЙ

**Источник:** строки 1614-1809 промпта

**Содержание:**
- Единственный разрешенный метод: Block Diff Rewrite
- Протокол READ → PREPARE → APPLY
- Формат diff с `:start_line:`
- Проверка линтера после КАЖДОЙ вставки
- Создание .patch файлов
- Правило чистой замены (SINGLE BLOCK RULE)
- Мета-когнитивная проверка перед `apply_diff`
- Fallback-протокол при ошибках

**Структура:**
```markdown
# 🏗️ ЕДИНСТВЕННЫЙ РАЗРЕШЕННЫЙ МЕТОД: BLOCK DIFF REWRITE

**🛡️ КРИТИЧЕСКИ ВАЖНО:** Перед использованием `apply_diff` ОБЯЗАТЕЛЬНО создай резервную копию.

## Протокол:

1. **READ:** Прочитай файл через `read_file` и найди границы функции
2. **PREPARE:** Сформируй НОВЫЙ код функции с пробами
3. **APPLY:** Используй `apply_diff` (SEARCH/REPLACE) с `:start_line:`
4. **ПРОВЕРКА ЛИНТЕРА:** После КАЖДОЙ вставки проверь файл линтером
5. **СОЗДАТЬ PATCH:** После успешной проверки создай .patch файл

## Формат diff:
[Пример с :start_line:]

## Правило чистой замены:
[Правила SINGLE BLOCK RULE]

## Fallback-протокол:
[Алгоритм при ошибках]
```

**Размер:** ~200 строк

---

### 3. `code-05-probe-examples.md`

**Источник:** строки 2430-2534 промпта

**Содержание:**
- Примеры проб по языкам
- Python варианты (requests, urllib, http.client)
- Go примеры
- JavaScript/TypeScript примеры
- Специально для IFC-парсинга (таймаут 5.0)

**Размер:** ~105 строк

---

### 4. `code-06-probe-spec.md` ⭐

**Источник:** строки 2302-2429 промпта

**Содержание:**
- Спецификация проб
- Генерация `probeCode`
- UUID-маркеры
- Формат данных (hypothesisId, message, state)
- Docker поддержка (_rootrace_host, rootraceHost)
- Привязка к сессии (ROO_TRACE_SESSION_ID)
- Удаление старых маркеров

**Размер:** ~130 строк

---

### 5. `code-07-code-hygiene.md` ⭐

**Источник:** строки 2325-2429 промпта

**Содержание:**
- Правила гигиены кода
- ZERO-DEPENDENCY INJECTIONS
- METADATA ABSTRACTION
- THE 5-LINE RULE
- АСИНХРОННОСТЬ (Safety First)
- МЕТРИКИ КОЛИЧЕСТВА (Data Density)
- ПРОВЕРКА ВЫХОДНЫХ ДАННЫХ (Output Validation)
- САМОКРИТИКА (Self-Criticism)

**Размер:** ~105 строк

---

### 6. `code-08-python-indent.md` ⭐⭐⭐ КРИТИЧНЫЙ

**Источник:** строки 2558-2602 промпта

**Содержание:**
- Python Indentation Strategy
- Алгоритм "Опорной строки"
- Правила для Pylance (каждый оператор на отдельной строке)
- Примеры идеальной вставки
- Запрет гадать отступы

**Структура:**
```markdown
# 🐍 PYTHON INDENTATION STRATEGY (CRITICAL)

Чтобы не сломать синтаксис Python, следуй алгоритму "Опорной строки":

1. **Rule of Thumb:** Отступ пробы должен БУКВАЛЬНО совпадать с отступом строки, ПЕРЕД которой ты делаешь вставку.

2. **Action:**
   - Вызови `read_file` и найди целевую строку
   - Скопируй все пробелы/табы от начала строки до первого символа
   - Используй ЭТОТ ЖЕ префикс для каждой строки `probeCode`

3. **Отдельные строки:** Каждый оператор пробы должен быть на отдельной строке с правильным отступом. Это требование Pylance.

## 🧩 ПРИМЕР ИДЕАЛЬНОЙ ВСТАВКИ:
[Пример с правильными отступами]

## КРИТИЧЕСКИ ВАЖНО:
НЕ гадай отступы. НЕ используй "логику" (+4 пробела для if). КОПИРУЙ их буквально из соседней строки.
```

**Размер:** ~45 строк

---

### 7. `code-12-meta-cognitive.md`

**Источник:** строки 2210-2301 промпта

**Содержание:**
- Мета-когнитивное управление
- Внутренний монолог (Chain of Thought)
- Экспертная панель (Multi-Agent Roleplay)
- Роли: Аналитик, Критик, Изобретатель, Профессор, Архитектор, Скептик
- Формат внутреннего диалога

**Размер:** ~90 строк

---

### 8. `code-13-fallback.md` ⭐

**Источник:** строки 2627-2696 промпта

**Содержание:**
- Fallback-протокол при ошибках
- Правило "Тупого исполнителя"
- Алгоритм: STOP → CLEAN → READ → SAFETY CHECK → RETRY
- Правило для ошибок Pylance/Python
- SIMPLEST BLOCK REWRITE
- ONE BLOCK AT A TIME

**Размер:** ~70 строк

---

### 9. `code-03-linter-protocol.md` (уже существует)

**Проверить:**
- Актуальность содержимого
- Протокол проверки линтера после каждой вставки
- Использование `mcp--roo-trace--get_problems`

---

### 10. `code-09-safety.md` (уже существует)

**Проверить:**
- Правила безопасности
- Backup перед изменениями
- Git commit или .bak копия

---

### 11. `code-10-rollback.md` (уже существует)

**Проверить:**
- Atomic Rollback Protocol
- Создание .patch файлов
- Селективный откат

---

### 12. `code-11-prohibitions.md` (уже существует)

**Проверить:**
- Строгие запреты на инструментацию
- Запрет `inject_multiple_probes`
- Запрет `inject_probes` для Python

---

## ✅ Чеклист создания

### Критичные модули (сделать первыми):
- [ ] `code-04-block-rewrite.md` ⭐⭐⭐
- [ ] `code-08-python-indent.md` ⭐⭐⭐
- [ ] `code-01-probe-insertion.md` ⭐

### Высокий приоритет:
- [ ] `code-06-probe-spec.md` ⭐
- [ ] `code-07-code-hygiene.md` ⭐
- [ ] `code-13-fallback.md` ⭐

### Средний приоритет:
- [ ] `code-05-probe-examples.md`
- [ ] `code-12-meta-cognitive.md`

### Проверка существующих:
- [ ] `code-03-linter-protocol.md` - проверить и обновить
- [ ] `code-09-safety.md` - проверить и обновить
- [ ] `code-10-rollback.md` - проверить и обновить
- [ ] `code-11-prohibitions.md` - проверить и обновить

## 📊 Оценка размера
**Итого:** ~820 строк (8 новых модулей + 4 на проверку)

## 🎯 Критерии успеха
1. Все детали вставки проб вынесены в модули
2. Block Rewrite метод описан детально
3. Python отступы описаны с примерами
4. Fallback-протокол понятен
5. Ссылки из help-модуля работают
