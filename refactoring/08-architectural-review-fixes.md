# 🔧 Исправления по результатам архитектурного ревью

## 📋 Критические проблемы и исправления

### ✅ Проблема 1: Отсутствие help-модуля в базовом промпте

**Статус:** ТРЕБУЕТ ИСПРАВЛЕНИЯ

**Исправление:**
Добавить явную ссылку на help-модуль в `RoleManager.loadCustomInstructions()` перед секцией AVAILABLE MODULES.

**Код для добавления:**
```typescript
content += `\n\n## 📚 HELP: Операции и инструменты

**🛡️ КРИТИЧЕСКИ ВАЖНО:** Для получения информации об операциях загрузи:
\`mcp--roo-trace--load_rule(rulePath="00-help-operations.md")\`

Этот модуль содержит:
- Описание всех Roo Code инструментов (new_task, attempt_completion, apply_diff и др.)
- Описание всех RooTrace MCP инструментов (get_debug_status, read_runtime_logs и др.)
- Ссылки на детальные модули для каждой операции
- Быструю навигацию по модулям

**🚨 START HERE:** При первом запуске сессии ОБЯЗАТЕЛЬНО загрузи help-модуль:
\`mcp--roo-trace--load_rule(rulePath="00-help-operations.md")\`

`;
```

**Файл для изменения:** `src/role-manager.ts`, строка ~44 (перед AVAILABLE MODULES)

---

### ✅ Проблема 2: Help-модуль не в STARTUP PROTOCOL

**Статус:** ТРЕБУЕТ ИСПРАВЛЕНИЯ

**Исправление:**
Добавить help-модуль в STARTUP PROTOCOL как первый шаг.

**Обновленный STARTUP PROTOCOL:**
```markdown
**🛡️ STARTUP PROTOCOL:** При первом запуске сессии рекомендуется загрузить модули в следующем порядке:

**ШАГ 0: Help-модуль (ОБЯЗАТЕЛЬНО ПЕРВЫМ!)**
0. \`mcp--roo-trace--load_rule(rulePath="00-help-operations.md")\` ⭐ START HERE

**ШАГ 1: Базовые модули (рекомендуется)**
1. \`mcp--roo-trace--load_rule(rulePath="00-base-language.md")\`
2. \`mcp--roo-trace--load_rule(rulePath="00-base-output.md")\`
3. \`mcp--roo-trace--load_rule(rulePath="00-base-error-handling.md")\`
4. \`mcp--roo-trace--load_rule(rulePath="roo-00-role.md")\`
5. \`mcp--roo-trace--load_rule(rulePath="00-formats-validator.md")\`

Эти модули содержат критически важные инструкции, которые должны быть загружены для корректной работы.
```

**Файл для изменения:** `src/role-manager.ts`, строка ~95 (STARTUP PROTOCOL)

---

### ✅ Проблема 3: Несоответствие в определении критичных модулей

**Статус:** ТРЕБУЕТ РЕШЕНИЯ

**Варианты решения:**

#### Вариант A: Явная инструкция (рекомендуется)
Оставить базовые модули как НЕ критичные, но добавить явную инструкцию в базовый промпт.

**Плюсы:**
- Сохраняет полную lazy loading
- Агент сам решает, когда загружать
- Гибкость

**Минусы:**
- Агент может забыть загрузить

#### Вариант B: Сделать базовые критичными
Изменить `RulesLoader.isCriticalRule()` чтобы базовые модули были critical.

**Плюсы:**
- Гарантированная загрузка
- Не нужно помнить

**Минусы:**
- Нарушает концепцию полностью lazy loading
- Увеличивает базовый промпт

#### Вариант C: Флаг `recommendedAtStartup` (лучший)
Добавить новый флаг в `LoadedRule` и логику загрузки.

**Плюсы:**
- Гибкость
- Явное указание рекомендаций
- Не нарушает lazy loading

**Минусы:**
- Требует изменения кода

**Рекомендация:** Вариант A (явная инструкция) + обновление STARTUP PROTOCOL

**Файл для изменения:** 
- `src/role-manager.ts` - обновить STARTUP PROTOCOL
- `src/rules-loader.ts` - оставить как есть (базовые НЕ критичные)

---

### ✅ Проблема 4: Отсутствие валидации ссылок в help-модуле

**Статус:** ТРЕБУЕТ ДОБАВЛЕНИЯ

**Исправление:**
1. Создать скрипт валидации ссылок
2. Добавить в план тестирования
3. Использовать относительные пути (только имя файла)

**Скрипт валидации (создать):**
```typescript
// scripts/validate-help-module-links.ts
// Проверяет, что все ссылки в help-модуле указывают на существующие файлы
```

**Добавить в план тестирования:**
- [ ] Проверить все ссылки в help-модуле через скрипт валидации
- [ ] Убедиться, что все модули существуют
- [ ] Проверить, что пути правильные

**Файл для изменения:** `refactoring/06-testing.md`

---

### ✅ Проблема 5: Потенциальная проблема с путями модулей

**Статус:** ТРЕБУЕТ СТАНДАРТИЗАЦИИ

**Исправление:**
Стандартизировать формат: использовать только имя файла в ссылках.

**Правило:**
- В help-модуле: `"00-base-language.md"` (только имя файла)
- В базовом промпте: `"00-base-language.md"` (только имя файла)
- `mcp--roo-trace--load_rule` поддерживает все форматы, но лучше унифицировать

**Обновить в:**
- `refactoring/01-help-module.md` - использовать только имена файлов
- `src/role-manager.ts` - использовать только имена файлов в примерах

---

## 📝 Дополнительные улучшения

### 1. Модульная зависимость

**Добавить в help-модуль:**
```markdown
## Зависимости модулей

**Базовые модули (независимые):**
- `00-base-language.md`
- `00-base-output.md`
- `00-base-error-handling.md`
- `00-base-penalties.md`
- `roo-00-role.md`

**Фазовые модули (зависят от базовых):**
- `roo-01-todo-list.md` → рекомендуется: `roo-00-role.md`
- `roo-02-delegate-recon.md` → рекомендуется: `roo-00-role.md`, `00-formats-validator.md`
- `roo-09-read-logs.md` → рекомендуется: `roo-00-role.md`
...
```

### 2. Версионирование модулей

**Добавить frontmatter в каждый модуль:**
```markdown
---
version: 1.0.0
updated: 2026-01-23
---

# Модуль
```

**Добавить в help-модуль:**
```markdown
## Версии модулей

- `00-base-language.md` - v1.0.0
- `00-base-output.md` - v1.0.0
...
```

### 3. Метрики и мониторинг

**Добавить логирование:**
```typescript
// В mcp-handler.ts при загрузке модуля
console.log(`[RooTrace] Module loaded: ${rulePath} at ${new Date().toISOString()}`);
```

**Создать файл для статистики:**
- `.rootrace/module-usage-stats.json`

---

## ✅ Чеклист исправлений

### Критично (сделать перед началом):
- [ ] Добавить явную ссылку на help-модуль в базовый промпт (`src/role-manager.ts`)
- [ ] Добавить help-модуль в STARTUP PROTOCOL как первый шаг
- [ ] Решить вопрос с критичностью базовых модулей (выбрать вариант A)
- [ ] Создать скрипт валидации ссылок (`scripts/validate-help-module-links.ts`)

### Высокий приоритет:
- [ ] Стандартизировать формат путей (только имена файлов)
- [ ] Добавить описание зависимостей модулей в help-модуль
- [ ] Обновить план тестирования с валидацией ссылок

### Средний приоритет:
- [ ] Добавить версионирование модулей (frontmatter)
- [ ] Добавить логирование загрузки модулей
- [ ] Создать план обновления модулей при изменении промпта

---

## 📊 Приоритеты исправлений

1. **Критично:** Проблемы 1, 2, 3 (help-модуль в базовом промпте)
2. **Высокий:** Проблемы 4, 5 (валидация и стандартизация)
3. **Средний:** Дополнительные улучшения (зависимости, версионирование)

---

## 🎯 Итоговая оценка после исправлений

**До исправлений:** 8.5/10 (проект НЕ готов к реализации)

**После исправлений:** 9.5/10 (проект готов к реализации)

**Оставшиеся риски:**
- Низкие (метрики, версионирование - можно добавить позже)

---

## ✅ Дополнительные замечания (проверено)

### 1. Модуль `roo-00-roo-code-features.md`
- **Статус:** Помечен как "НОВЫЙ" в планах - это ожидаемо, модуль еще не создан
- **Действие:** Создать при выполнении этапа 4 (остальные модули)

### 2. Форматы путей в `role-manager.ts`
- **Статус:** ✅ Исправлено - теперь везде используются только имена файлов
- **Было:** Строка 27 содержала пример с полным путем `.roo/roo-trace-rules/имя-модуля.md`
- **Стало:** Все примеры используют только имена файлов для единообразия
- **Примечание:** `mcp--roo-trace--load_rule` поддерживает оба формата, но для консистентности используем только имена файлов

### 3. Скрипт валидации
- **Статус:** ✅ Корректно использует `process.cwd()` для запуска из корня проекта
- **Проверено:** Скрипт правильно определяет пути к help-модулю и директории правил
