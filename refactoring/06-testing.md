# 🧪 Этап 6: Тестирование рефакторинга

## 🎯 Цель
Протестировать модульную систему, убедиться, что все работает корректно после рефакторинга.

## 📋 План тестирования

### 1. Тестирование базового промпта

**Проверки:**
- [ ] Базовый промпт загружается при старте режима `ai-debugger`
- [ ] Промпт содержит ссылку на help-модуль
- [ ] Промпт содержит список доступных модулей
- [ ] Промпт не содержит детальных описаний инструментов
- [ ] Размер базового промпта < 200 строк

**Метод:**
1. Активировать режим `ai-debugger`
2. Проверить system prompt через DevTools или логи
3. Убедиться, что содержимое соответствует ожиданиям

---

### 2. Тестирование help-модуля

**Проверки:**
- [ ] Help-модуль доступен через `mcp--roo-trace--load_rule`
- [ ] Help-модуль содержит описание всех операций
- [ ] Для каждой операции есть ссылка на детальный модуль
- [ ] Примеры использования корректны
- [ ] Быстрая навигация работает

**Метод:**
```javascript
// Загрузить help-модуль
mcp--roo-trace--load_rule(rulePath="00-help-operations.md")

// Проверить содержимое
// Проверить все ссылки на модули
```

**Ожидаемый результат:**
- Help-модуль загружается успешно
- Содержит ~200-300 строк
- Все ссылки указывают на существующие модули

---

### 3. Тестирование загрузки детальных модулей

**Проверки для каждого модуля:**
- [ ] Модуль доступен через `mcp--roo-trace--load_rule`
- [ ] Модуль содержит актуальную информацию
- [ ] Модуль самодостаточен (можно использовать без других модулей)
- [ ] Нет дублирования с другими модулями

**Метод:**
```javascript
// Загрузить модуль
mcp--roo-trace--load_rule(rulePath="roo-01-todo-list.md")

// Проверить содержимое
// Сравнить с соответствующим разделом промпта
```

**Критичные модули для проверки:**
- `roo-01-todo-list.md` ⭐⭐⭐
- `roo-09-read-logs.md` ⭐⭐⭐
- `code-04-block-rewrite.md` ⭐⭐⭐
- `code-08-python-indent.md` ⭐⭐⭐

---

### 4. Тестирование ссылок в help-модуле

**Проверки:**
- [ ] Все ссылки на модули в help-модуле указывают на существующие файлы
- [ ] Пути к модулям правильные (используется только имя файла)
- [ ] Можно загрузить модуль по ссылке из help
- [ ] **НОВОЕ:** Автоматическая валидация через скрипт

**Метод:**
1. Запустить скрипт валидации: `npm run validate-help-links` (создать)
2. Прочитать help-модуль
3. Извлечь все ссылки на модули (регулярное выражение для `mcp--roo-trace--load_rule`)
4. Проверить существование каждого файла в `.roo/roo-trace-rules/`
5. Попробовать загрузить каждый модуль через MCP

**Скрипт валидации:**
```typescript
// scripts/validate-help-module-links.ts
// 1. Читает help-модуль
// 2. Извлекает все ссылки на модули
// 3. Проверяет существование файлов
// 4. Выводит отчет о валидности ссылок
```

**Ожидаемый результат:**
- Все ссылки валидны
- Все модули загружаются успешно
- Скрипт валидации проходит без ошибок

---

### 5. Тестирование workflow агента

**Сценарий 1: Старт сессии**
- [ ] Агент загружает базовый промпт
- [ ] Агент может найти help-модуль
- [ ] Агент может загрузить нужные модули по требованию

**Сценарий 2: Phase 0.1 - TODO list**
- [ ] Агент загружает `roo-01-todo-list.md` при необходимости
- [ ] Агент создает TODO list корректно
- [ ] TODO list содержит все обязательные фазы

**Сценарий 3: Phase 1.1 - Делегирование вставки проб**
- [ ] Агент загружает `code-01-probe-insertion.md` для инструкций кодера
- [ ] Инструкции для кодера корректны
- [ ] Кодер получает все необходимые детали

**Сценарий 4: Phase 6 - Чтение логов**
- [ ] Агент загружает `roo-09-read-logs.md` при необходимости
- [ ] Агент следует протоколу "Железного моста"
- [ ] Агент не выдает вердиктов без данных

---

### 6. Тестирование экономии токенов

**Проверки:**
- [ ] Базовый промпт значительно меньше исходного
- [ ] Модули загружаются только по требованию
- [ ] Неиспользуемые модули не загружаются

**Метрики:**
- Размер базового промпта: должно быть < 200 строк (было ~300)
- Загружаются только нужные модули
- Экономия токенов: ~80-90%

---

### 7. Тестирование обратной совместимости

**Проверки:**
- [ ] Существующие модули в `.roo/rules-ai-debugger/` не сломаны
- [ ] Roo Code может загружать правила из `.roo/rules-ai-debugger/` (если нужно)
- [ ] Нет конфликтов между директориями

---

### 8. Тестирование копирования модулей

**Проверки:**
- [ ] `copyPromptModules()` копирует модули в `.roo/roo-trace-rules/`
- [ ] Help-модуль копируется
- [ ] Все модули копируются при активации расширения

**Метод:**
1. Удалить `.roo/roo-trace-rules/` в workspace
2. Активировать расширение
3. Проверить, что модули скопированы

---

## 🧪 Тестовые сценарии

### Сценарий 1: Агент ищет информацию об операции

**Шаги:**
1. Агент получает задачу
2. Агент не знает, как использовать `new_task`
3. Агент загружает help-модуль: `mcp--roo-trace--load_rule(rulePath="00-help-operations.md")`
4. Агент находит описание `new_task` в help-модуле
5. Агент загружает детальный модуль: `mcp--roo-trace--load_rule(rulePath="roo-02-delegate-recon.md")`
6. Агент использует `new_task` корректно

**Ожидаемый результат:** ✅ Агент находит информацию и использует операцию правильно

---

### Сценарий 2: Агент выполняет Phase 0.1

**Шаги:**
1. Агент получает задачу
2. Агент оценивает данные (Phase 0)
3. Агент загружает `roo-01-todo-list.md` (если нужно)
4. Агент создает TODO list через `update_todo_list`
5. TODO list содержит все обязательные фазы

**Ожидаемый результат:** ✅ TODO list создан корректно, содержит все фазы

---

### Сценарий 3: Агент делегирует вставку проб

**Шаги:**
1. Агент сформулировал гипотезы (Phase 1)
2. Агент загружает `code-01-probe-insertion.md` (если нужно)
3. Агент создает `new_task(mode="code")` с инструкциями
4. Кодер получает детальные инструкции
5. Кодер вставляет пробы корректно

**Ожидаемый результат:** ✅ Пробы вставлены корректно, код компилируется

---

### Сценарий 4: Агент читает логи

**Шаги:**
1. Агент попросил пользователя запустить код (Phase 5)
2. Пользователь запустил код
3. Агент загружает `roo-09-read-logs.md` (если нужно)
4. Агент читает логи из файла `.rootrace/ai_debug_logs.json`
5. Агент фильтрует логи по timestamp
6. Агент анализирует данные
7. Агент выдает вердикт ТОЛЬКО после чтения логов

**Ожидаемый результат:** ✅ Вердикт основан на данных, не на предположениях

---

## ✅ Чеклист тестирования

### Базовое тестирование:
- [ ] Базовый промпт загружается
- [ ] Help-модуль доступен
- [ ] Детальные модули загружаются по требованию
- [ ] Все ссылки работают

### Функциональное тестирование:
- [ ] Агент может найти операцию через help
- [ ] Агент может загрузить детальный модуль
- [ ] Агент следует протоколу из модулей
- [ ] Workflow работает корректно

### Тестирование производительности:
- [ ] Экономия токенов (базовый промпт меньше)
- [ ] Lazy loading работает (модули загружаются только по требованию)
- [ ] Нет избыточной загрузки модулей

### Тестирование совместимости:
- [ ] Обратная совместимость с существующими модулями
- [ ] Roo Code корректно работает с новой структурой
- [ ] Нет конфликтов между директориями

## 🐛 Известные проблемы и решения

### Проблема 1: Модуль не загружается через MCP
**Решение:** Проверить путь, убедиться, что файл существует в `.roo/roo-trace-rules/`

### Проблема 2: Ссылка в help-модуле не работает
**Решение:** Проверить имя файла, убедиться, что модуль создан

### Проблема 3: Агент не находит операцию
**Решение:** Убедиться, что help-модуль содержит описание операции

## 📊 Метрики успеха

### До рефакторинга:
- Базовый промпт: ~300 строк
- Монолитный промпт: 2765 строк (не используется)
- Все модули загружаются автоматически

### После рефакторинга:
- Базовый промпт: < 200 строк ✅
- Help-модуль: ~200-300 строк
- Модули загружаются только по требованию ✅
- Экономия токенов: ~80-90% ✅

## 🎯 Критерии успеха
1. ✅ Все модули доступны через MCP
2. ✅ Help-модуль помогает найти операции
3. ✅ Агент может выполнить полный workflow
4. ✅ Экономия токенов достигнута
5. ✅ Нет регрессий в функциональности
